{
  "_comment": "OAuth account registry. Copied to configs/oauth_accounts.json on first setup (configs/oauth_accounts.json is gitignored — it is per-user). Edit the live file to declare YOUR accounts. Each account needs provider, client_credentials (a path to an OAuth client json under credentials/, itself gitignored), and scopes. account_id is the key (referenced by from_account / acting_as).",
  "accounts": {
    "google_user_primary": {
      "provider": "google",
      "display_name": "My Google Workspace",
      "client_credentials": "credentials/google_workspace.json",
      "scope_set": "workspace",
      "scopes": [
        "https://www.googleapis.com/auth/calendar",
        "https://www.googleapis.com/auth/gmail.readonly",
        "https://www.googleapis.com/auth/gmail.send",
        "https://www.googleapis.com/auth/gmail.modify",
        "https://www.googleapis.com/auth/tasks",
        "https://www.googleapis.com/auth/documents",
        "https://www.googleapis.com/auth/drive"
      ]
    }
  }
}
