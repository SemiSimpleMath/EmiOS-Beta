{
  "_doc": "Routine manager settings. Routine ENTRIES live in configs/routines/public/*.json (tracked, universal) and configs/routines/private/*.json (gitignored, personal). Each routine is one JSON file named <id>.json. See skills/extending-emi-routines/SKILL.md for trigger types, policies, active windows, on_error backoff, and watchdog config.",
  "schema_version": 3,
  "enabled": true,
  "max_workers": 20,
  "state_resource_file": "resource_routine_status.json"
}
