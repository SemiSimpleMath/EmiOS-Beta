{
  "id": "image_reconcile",
  "enabled": true,
  "name": "Image pods — weekly stored_path health check",
  "runner": "function",
  "spec": {
    "function_name": "image_reconcile_run"
  },
  "run_policy": {
    "type": "weekly",
    "day_of_week": "Sunday",
    "time_local": "05:10"
  },
  "max_run_seconds": 600,
  "on_error": {
    "max_failures": 3,
    "backoff_base_seconds": 300,
    "backoff_max_seconds": 3600,
    "then": "disable_with_ticket",
    "auto_retry_after_seconds": 3600
  },
  "notes": "Weekly reconcile pass (2026-07-08 pod audit R4 — the reconciler existed but was never scheduled): data/images gets repair-only sync (refresh drifted stored_paths, flag orphan sidecars), resources/identity gets full reconcile (stamp + mint what the user dropped in). A moved/deleted backing file surfaces here instead of at a send_email attach."
}
