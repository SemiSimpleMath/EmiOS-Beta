# Bug list — found while certifying the architecture docs (2026-09-18)

Everything here was found by reading a file end to end in order to document it, then
checking its callers. **Use each entry's status: later repair passes fixed some findings.** Each entry says what is
wrong, how it was established, and how bad it actually is.

The documentation errors these reads surfaced are already fixed and are not listed here —
this is code only.

**Verification pass, 2026-09-18 (Fable 5.1).** Every entry below was re-checked against the
code with fresh searches, widened to all file types and to agent forms, since an agent's
structured output lands on the blackboard with no `update_state_value` call. Result: 13 of 13
stand. Two were overstated (4a's on-screen window, 4b's consequence), one was understated
(4b has callers that cannot set the block timeout at all), two needed wording corrected
(LIVE 2's callers, ROT 3's "no producers"), and one bug was found that was not on the list
(LIVE 6). Each entry carries a `Verified:` line saying what was re-read.

**Repair pass, 2026-09-18 (Opus 5).** Seven entries FIXED with tests, four HELD for a
decision that is yours, two closed as documentation. Status is marked on each entry. The
dayflow suite went 341 → 346 → 371 across the pass, all additions; the 20 failures
elsewhere are the known playwright/MCP set and fail identically on a clean tree, proven by
stashing.

| status | entries |
|---|---|
| FIXED | LIVE 1, LIVE 2, LIVE 4a, LIVE 4b, LIVE 6, LIVE 7, BANNED 1, BANNED 2 |
| HELD — needs your call | ROT 1, ROT 2, ROT 3 |
| CLOSED as documented | ROT 4, ROT 5, LIVE 5 (rides on ROT 3) |

LIVE 7 was found on 2026-09-18 while acting on LIVE 6, and LIVE 4b's root cause was
resolved by owner decision the same day. Both are recorded below.

Severity is about consequence, not effort:

- **LIVE** — wrong behaviour reachable today.
- **ROT** — dead or unreachable code that misleads a reader or a future refactor.
- **BANNED-PATTERN** — works, but violates a standing rule in this repo.

---

## LIVE

### 1. `/work` reports the wrong database — FIXED (66cd48ec)

Reports `store.path` now. No default on the attribute read: both branches of `_get_store`
construct a `WorkStore`, so a missing `path` would be a real breakage and should surface.

`work_objects/ui/blueprint.py` — `api_work_list` returns `{"db": _WORK_DB}`, the
dev-file constant `work_objects/work.db`. But `_get_store()` **prefers the live dayflow
store** (`emi.db`) and only drops back to the dev file when the app is not importable. So
the UI tells you which database you are looking at, and is wrong about it whenever it is
working normally.

Established: read both functions in the same file.
Verified: `_get_store` lines 48–52 prefer `get_dayflow_work_store()`; line 96 returns `_WORK_DB`.
Consequence: someone debugging real work objects believes they are looking at a scratch DB,
or vice versa. Cheap fix; needs a path accessor on the store.

### 2. The batch item writer performs no transition validation — FIXED (16af8e84)

Existing rows are read-merge-written and a state change goes through `_validate_transition`,
matching the single writer. One SELECT per batch, not per item. Payload validation moved
ahead of the transaction so a bad batch writes none of itself. Five tests, including the
`new → artifact` move triage actually makes and the same-state write the state_mover needs.

`dayflow_orchestrator/dayflow_item_writer.py` — `write_dayflow_item` validates every state
change against `ALLOWED_TRANSITIONS`. `write_dayflow_items_batch` does not: it upserts via
`on_conflict_do_update` and never calls `_validate_transition`. It also defaults new items
to `important_open`.

Three live callers, and they differ in what they risk. `ingestion.py` and
`entertainment_advisor_stage.py` create NEW rows, where there is no prior state to validate
against, so the missing check costs nothing there. **`triage_persist_node._persist_admitted`
updates EXISTING rows** — `new → artifact`, with no check. That transition happens to be
allowed, so nothing is violated today; nothing enforces it either. The `post_room_finalize_node`
calls the earlier version of this entry named are dead code (ROT 3).

A second difference from `write_dayflow_item`: the batch upsert **replaces `metadata_json`
wholesale** rather than read-merge-write, so a caller that passes a partial dict silently drops
every key it did not include.

Established: read both writers.
Verified: searched every `write_dayflow_items_batch(` call in `app/` and `work_objects/`
outside tests; read the entertainment stage's call site to confirm it creates rather than updates.
Consequence: one live update path with no enforcement, and a clobber semantics nobody has
documented. CLAUDE.md says transitions are "enforced by `write_dayflow_item`", which is true and
reads as though it covers all writes.

### 3. Same-status writes skip the transition machine — UNCHANGED, still open by decision

`work_objects/store.py` — the transition check runs only when the target status differs from
the current one, so `dispatched → dispatched` bypasses validation. Already recorded as
"dispatch claim is not a fence" and not approved for fixing.

Worth noting here because **two subsystems now deliberately rely on it**: the state_mover
updates `reactivate_at` with a same-status write, and the evaluator folds originating intake
into a goal's content the same way. A CAS claim op would have to keep those working.

Verified: `store.py` line 498, `if target != node.status:` guards the `TRANSITIONS` lookup; the
terminal-reason check at line 510 is guarded the same way.

### 4a. A reply after the wait-timeout is silently discarded — FIXED (3d068265)

`TicketManager.can_transition` is the read-only half of the state machine, so `respond`
checks before it writes: nothing is saved, nothing is published, and the result carries a
typed `not_answerable` flag. The API answers 409 and the popup stops rolling back the
optimistic removal, showing a notice instead. **It does not recover the answer** — by then
the call has returned "user not reached" and the finalizer has judged the node, so
`result_recorder` would refuse a second outcome regardless. The reply is lost either way;
it is now visibly lost. Whether the tool should expire its ticket at wait-timeout at all is
a lifecycle decision and is HELD.

Found while pinning it: PENDING is displayable but cannot reach ACCEPTED, so a ticket left
in PENDING would be shown and then refuse the answer. Unreachable only because all eleven
`create_ticket` callers happen to call `mark_proposed`, which that function's docstring
calls the caller's responsibility. Pinned by a test, not fixed.

Two of my own assumptions were wrong and the tests caught both: `pending` and `snoozed` do
not accept a reply. Only `proposed` does.

#### Original finding, kept for the reasoning

`ticket_manager.py` — `_ALLOWED_TRANSITIONS[EXPIRED]` is an **empty frozenset**, so `expired`
is fully terminal. `_transition_state_in_session` logs a warning and returns `False`. It does
not raise, and no caller of `mark_accepted` checks the return value in a way the user would
ever see.

The sequence: `_wait_for_ticket_response` times out → `mark_expired(reason="wait_timeout")`
fires immediately. A reply that lands after that attempts `expired → accepted`, is refused, and
`result_for_ticket` reports "user not reached".

**Corrected on verification — the on-screen window is much smaller than first filed.** The web
popup polls `/api/tickets/pending`, which lists `pending`/`proposed` only, and the tool publishes
`proactive_suggestion_update` on timeout, which triggers an immediate re-fetch. So on the web
UI the ticket disappears on the next refresh, not at `valid_until`. The exposure is the race
between the timeout and that refresh, plus any surface that does not re-render (a Slack or
Telegram message stays in the channel with its buttons).

**Two things the first filing missed make the drop uglier than a race.** `TicketService.respond`
writes `user_text` and `user_action` onto the ticket row *before* attempting the transition, so
the late answer **is persisted, on an expired ticket, and never read** — `result_for_ticket`
only consults `user_text` when the state is a responded one. And the refusal is invisible:
`respond` returns `success=False` with no `error`, the API turns that into `200 {"success":
false}`, and `proactive_popup.js` never checks the flag.

The dayflow lane's own window is consistent by arithmetic: the arguments node passes
`valid_hours = ASK_WINDOW_HOURS` and `wait_timeout_seconds = ASK_WINDOW_HOURS * 3600`, so the
block and the validity window end together.

Established: read `TicketState`, `_ALLOWED_TRANSITIONS`, `_transition_state_in_session`,
`mark_expired`, and the tool's timeout path.
Verified: read `TicketService.respond` in full, the `/api/tickets/respond` and
`/api/tickets/pending` routes, and the popup's `proactive_suggestion_update` handler and
`checkForSuggestions`. Searched for every `.respond(` caller: all three are in `ticket_api.py`.
Fix shape: the tool should not `mark_expired` on wait timeout at all — the ticket's own
`valid_until` already bounds it, and a late reply should land through the same recorder the
boot re-arm uses. Failing that, refuse to expire while `valid_until` is in the future.

### 4b. The ticket tool's two timeout defaults disagree — PARTLY FIXED (c0af0690)

`wait_timeout_seconds` is now in the tool form and the contract, so a routed call can set
it, and the tool logs a warning naming both numbers when the block is shorter than the
validity window. **Defaults and the expire-on-timeout behaviour deliberately unchanged:**
deriving the block from `valid_hours` would make the scheduler-conflict ask block a thread
for 48 hours, which is worse than the bug.

**Root cause RESOLVED (3348a2c9), by decision.** The tool conflated *creating* a ticket with
*waiting* for the answer, and the two fire-and-forget callers got a ten-minute block they
never asked for. The tool now takes `wait`, default true so the dayflow lane is untouched,
and both callers pass false. Owner picked this over the two alternatives.

That block was doing real harm once the `"info"` bug was fixed, because the camera
escalation runs inside an event routine: 600s of blocking tripped the routine watchdog's
120s soft limit, producing a spurious "this routine is stuck" ticket during an emergency,
and then expired the emergency ticket at ten minutes against its four-hour validity.

`lib/tools/create_dayflow_ticket/create_dayflow_ticket.py` — `valid_hours` defaults to 4
(the DB validity window) while `wait_timeout_seconds` defaults to 600 (how long the call
blocks). The dayflow lane is safe: `dayflow_switchboard_arguments_node` passes both from
`ASK_WINDOW_HOURS`, and its comment says this is exactly why.

**Corrected on verification, in both directions.** The consequence is not "on screen for three
and a half hours" — per 4a the ticket vanishes from the web popup on the next refresh. The
real consequence is that **a caller's four-hour question is silently withdrawn after ten
minutes**: the tool expires the ticket at the wait timeout and reports "user not reached",
against a validity the caller explicitly asked for.

And it is worse than "any other caller is exposed": **the tool form has no
`wait_timeout_seconds` field at all** (`tool_forms.py` carries `valid_hours: int = 4` and nothing
for the block), so an LLM-invoked call cannot set it even if it wanted to. Every route that
reaches this tool through a switchboard — master_room's, whose prompt sends "remind me" and
"ask me whether" here, and `entertainment_manager`, which lists it in `allowed_tools` — gets a
600-second ask with a 4-hour label.

Two programmatic callers pass `valid_hours` (camera escalation 4 h, scheduler-conflict ask
48 h) and no block timeout, so they look like the worst cases — but they never reach the
timeout at all. See LIVE 6.

Established: read the tool and the arguments node that compensates for it.
Verified: searched every reference to the tool across `app/`, `configs/` and `work_objects/`;
read `tool_forms.py`; distinguished the callers that invoke the tool's `execute` (which waits
and expires) from the many that call `ticket_manager.create_ticket` directly (which do neither
and are not exposed).

### 5. `created_ticket_id` is recovered by scanning prose — CLOSED, rides on ROT 3

`post_room_finalize_node._persist_action_selector_action_logs` — if `result_payload` has no
`ticket_id`, it searches the result TEXT for the words "ticket" and "Created", then takes the
first whitespace token containing `dayflow_orchestrator_`.

That is a wording-based identity join, which this repo bans outright
(`feedback_no_wording_based_identity`: ids and deterministic joins only). Currently
unreachable because the block is dead (see ROT 3), so it is filed LIVE only in the sense that
it would fire the moment that lane were revived.

Verified: re-read lines 814–824; the block is inside `_persist_action_selector_action_logs`,
whose only caller path depends on `action_result_events`, which has no producer.

### 6. The camera escalation and the conflict ask never created a ticket — FIXED (072c2176)

`notify` for the camera event, `decision` for the conflict. The camera success test now
reads `result_type` instead of looking for an `error_code` field that error results never
carry. Pinned by an AST walk over both modules plus two tests of the success path.

`ring_analysis/camera_dispatcher.py` (line 545) and `subconscious/scheduler_arbiter_persist.py`
(line 220) both call the ticket tool's `execute` with **`ticket_kind: "info"`**. The tool's
`_ui_policy_for_ticket_kind` accepts exactly `notify`, `decision`, `advice` and **raises** on
anything else — and it runs *before* `create_ticket`. The raise is caught by `execute`'s outer
`except`, which returns an error `ToolResult`. So the high-stakes door-event escalation and the
"the arbiter punted, ask the user" ticket both fail every time, with an ERROR log line and no
ticket.

`camera_dispatcher` then makes it invisible: its success test is
`result_type in {...} or (isinstance(data, dict) and not data.get("error_code"))`. The error
result carries `data={}`, an empty dict with no `error_code`, so the second clause is **true**
and the function returns `True`. The caller believes the escalation went out.

**Latent, not yet fired.** Searched all 4,317 retained log files for `Got: 'info'` and for any
`camera_dispatcher` / `arbiter_persist` ticket line: zero. Neither path has executed in any
retained log. It will fail the first time a significant door event or a scheduling conflict
occurs.

Established: read both call sites, the tool's `execute` up to the policy check, and
`_ui_policy_for_ticket_kind`.
Verified: the tool form declares `ticket_kind: str` with no `Literal`, so nothing upstream
rejects `"info"` either.
Fix shape: both callers should pass `notify` (a camera event) or `decision` (a conflict needing
the user's call), and `camera_dispatcher`'s success test should check `result_type == "error"`
rather than the absence of a field that error results never carry.

### 7. No front-door camera pod has ever reached dayflow — FIXED (3348a2c9)

Dayflow ingests pods by an allowlist of `(kind, source_kind)` pairs, in the dayflow room's
`ROOM.md` frontmatter under `access.ingestion_pod_kinds`. It asks for
`ring_doorbell_significant`. **No camera on this machine produces that name.** The front
door mints `ring_doorbell_event`, Downstairs mints `ring_downstairs_notable`; only the
Bedroom's `ring_bedroom_notable` is in the list. So every front-door pod has been minted
and then silently ignored by dayflow.

Found while implementing the owner's chosen escalation route for LIVE 6, which was "write it
to a pod and let the orchestrator get it" — a plan that would have quietly done nothing.

Fixed with `pod_policy.source_kind_by_category`: the front door mints the everyday kind
normally and `ring_doorbell_significant` for `emergency`, so the escalating category enters
dayflow intake and ordinary events stay out. One pod under the right kind, rather than a
second pod for the same frame. Allowlisting the everyday kind instead would have made every
passing delivery dayflow intake at one triage LLM call each.

Tests parse BOTH sides and assert they agree, so renaming one without the other fails.

**Still open, deliberately:** the Downstairs camera's pods reach dayflow no more than before.
Changing what a second camera feeds into the working set is a behaviour decision.

### Historical, resolved — recorded so it is not re-filed

The enum-stringification rejection (`Got: 'ticketkind.notify'`) appears **hundreds of times**
across seven processes in the logs, sometimes three identical failures in a row on one work
node. All seven processes started before commit 42411b30 landed the `.value` unwrap at 22:54 on
2026-09-16 — including process 10104, whose log is dated the 17th because it kept running, and
which is a test harness besides. The live process (started 2026-09-18 16:45) shows two
successful ticket waits and zero rejections. The fix works; the failures are pre-fix. Those
repeated identical results are, incidentally, exactly the pattern `_render_existing_graph`'s
"ALL IDENTICAL" warning was built to expose.

---

## ROT

### 1. `payload.session_id` is written in three places and read by nothing — HELD

Not touched. The entry itself says a decision is needed and it is not mine to make:
restoring a reader and dropping the writes are opposite repairs, and the rewrite notes show
the reader was planned, not forgotten. Entangled with ROT 2 — if a reader comes back, those
helpers become useful again, so removing them first would be the wrong order.

Written by `work_session.open_session`, by `discharge_node` when passed a session id, and
inherited by every child in `store.add_node`. **No reader anywhere.**

The supervisor it exists for, `sweep_stuck_work_nodes`, was rewritten to a pure subtree-idle
rule and consults no session at all. Two docstrings still asserted that "ownership is a graph
fact the supervisor reads"; those are now corrected, but the writes remain.

Established: searched every reference in `app/` and `work_objects/`.
Verified: a widened search across the orchestrator, control nodes, tools, core tools, dayflow
pipelines and `work_objects/` (py/j2/yaml/json) finds the field read in exactly two places, and
both are the writers deciding what to write — `discharge.py` line 81 compares before stamping,
`store.py` line 455 reads a parent's value to inherit it. No consumer exists. A whole-repo
sweep, every file type, found nothing further: the other hits are unrelated session concepts
(doc, plan, geoguessr and task-creation sessions), a stale agent worktree copy of those files,
and recorded tool-result payloads that only show the field is written in production.

Provenance for the decision: the rewrite's own notes, `scratch/WORK_SESSION_REWRITE_PLAN.md`
and `scratch/WS_REWRITE_PROGRESS.md`, name the intended reader — "orphan = dispatched node
whose payload.session_id [is] not a live session", the sweeper re-key listed as step 5. That
reader was replaced by the subtree-idle rule, and the stamping was never taken out with it.
Decision needed: either restore a reader or drop the writes. Right now it is a durable field
that looks load-bearing.

### 2. Four orphaned session helpers — HELD with ROT 1

Three have zero callers and `session_alive` is used by `test_job_thread_dispatch`, so
deleting it would break a passing test that documents real behaviour. Held as one decision
with ROT 1 rather than deleted piecemeal.

- `dispatch_sweeper._session_root_dispatched` — **zero callers**, and it belonged to the
  orphaned/frozen supervision that no longer exists.
- `work_session.session_alive` — no production callers (tests only).
- `work_session.session_started_at` / `session_started_at_by_id` — no callers at all.

Only `session_alive_by_id` is live, used by the boot-time ask re-arm.

Verified: whole-repo search for the three names, all file types, excluding `.venv` and
archives. `_session_root_dispatched`: definition only. `session_started_at` (both forms):
definitions only. `session_alive(`: definition plus tests.

### 3. `post_room_finalize_node` is roughly 600 lines with no live producers — HELD

Not deleted. This is ~600 lines in the node every dayflow path exits through, it has its own
test file, and the entry says two ideas must be preserved deliberately rather than lost in
the deletion. It also needs the legacy `strategic_planner` agent form retired in the same
change or the whole block can silently come back. That is a single coordinated change and
your call on scope, not something to do on the way past.

BANNED 1 was fixed inside this file, because `_format_compact_outcome` is reachable from
master_room and is therefore live even though most of the module is not.

**Wording corrected on verification.** The first filing said the keys had no producers. They
have exactly one, and it is unreachable: the **retired legacy `strategic_planner`'s agent form**
declares `planned_tasks`, `plan_synopses`, `completed_plan_ids` and `closed_task_ids`, and two
`agents/test/*_repro` forms declare `planned_tasks` and `state_mutations`. No manager config
loads any of those agents. The distinction matters: **re-wiring `strategic_planner` would
silently bring this whole block back to life**, so whoever retires it should retire the agent
form in the same change.

| key it reads | producer |
|---|---|
| `planned_tasks` | only the unwired legacy `strategic_planner` form (`state_mover_prep_node` merely enriches a list it reads back) |
| `state_mutations` | only a test repro agent (the live state_mover form has `node_wakes` and `held_work_nodes`, nothing else) |
| `state_mutations_persisted_tf` | none (`state_transition_guard_node` was deleted) |
| `plan_synopses`, `completed_plan_ids`, `closed_task_ids` | only the unwired legacy `strategic_planner` form |
| `action_result_events`, `active_dispatch_records` | none, anywhere |

So `_build_planned_task_messages`, `_persist_action_selector_action_logs`,
`_attach_execution_results`, `_close_completed_plans`, `_cancel_tasks`, the plan-synopsis
diffing, and the auto-synopsis safety net are all unreachable. The `acted_on_item_ids` path
is also inert by design — `work_node_dispatch_node` clears that key before handing off.

Established: searched for `update_state_value` on each key across `app/`.
Verified: re-searched each key with any quoting and any setter idiom across py/j2/yaml, then
listed every field of every live dayflow agent's `agent_form.py` — none of the eight appears —
and confirmed no manager YAML names the legacy planner or the repro agents.
Consequence: this is the single largest block of dead code in the dayflow tick, and it
contains two real ideas worth keeping if the lane is ever revived — the auto-synopsis net for
the "HVAC re-research loop", and the deterministic `changed=false` override. Retiring it
should preserve those deliberately rather than deleting them by accident.

### 4. `register_manager_as_tool` has no callers — CLOSED as documented, NOT deleted

Deliberately kept. Registering a manager as a tool at RUNTIME is exactly the extension hook
the modular initiative's plan doc says the system lacks, so this may be a staged seed rather
than an oversight. Deleting it would remove something on the basis that nothing calls it
yet. Documented as unused in `08_WORK_OBJECTS.md`; delete it on purpose or not at all.

`work_objects/work_tools.py` — it would register a manager-as-tool wrapper at runtime. The two
node managers ship as ordinary static tool directories instead.

Verified: whole-repo search finds the definition and two documentation mentions, nothing else.

### 5. `graph_neighbors` is never exposed — CLOSED as documented

`work_objects/tools.WorkGraphTools.graph_neighbors` exists and works, but there is no
`work_graph_neighbors` entry in `_SPECS`, so no agent can call it. The module docstring lists
it among the read tools. Possibly deliberate; flagged because the docstring implies otherwise.

Verified: the name appears only in `tools.py`; no `work_graph*` static tool directory exists.
Repaired as documentation rather than by exposing a new tool: the module docstring listed it
among the read tools, which implied a `work_graph_neighbors` that does not exist, and both
the docstring and the method now say it is Python-only. Whether an agent should get it is a
capability decision, not a bug.

---

## BANNED-PATTERN

### 1. `_format_compact_outcome` degrades silently instead of failing — FIXED (8dd3f832)

Raises on a missing formatter, a crash, a non-dict answer, or an empty outcome. An empty
`full_result` still returns "" because there is genuinely nothing to summarise. Nothing
depended on the three removed sentences: searched every py, j2 and js file.

`post_room_finalize_node` — on any failure it returns a hedged string:
`"(formatter unavailable; see action_log)"`, `"(formatter errored; see action_log)"`,
`"(formatter returned no data; see action_log)"`. That string is then written into an item's
`execution_result` and read by the planner as though it were an outcome.

The repo's rule is to fail loud, never to substitute a default
(`feedback_no_fallbacks_fail_loud`). The stated intent was to avoid blasting 3KB downstream,
which a raise would also avoid.

Verified: re-read lines 119–180. Note this helper is **live** — `master_room_tool_caller`
imports it — so unlike most of this file the stubs can reach a real item's `execution_result`
on the master_room dispatch path.

### 2. The ticket brief synthesises a ticket on failure — FIXED (8dd3f832)

Raises instead. `execute` turns that into an error ToolResult, the node fails, and the
finalizer judges it — a failed ask the architect can re-plan beats a malformed one the user
has to interpret.

`create_dayflow_ticket._format_brief` — if `ticket_builder_manager` fails, it classifies the
ticket by searching the brief for "ask", "decide", "choice", "confirm", "whether" and takes the
first 60 characters as the title. Both a fallback and a wording-based decision.

Consequence when it fires: the user gets a ticket titled with a truncated fragment, and the
`decision` vs `advice` classification, which decides whether a reply is even possible, is
chosen by substring match.

Verified: re-read lines 224–235.

---

## Historical verification summary (2026-09-18)

Every entry above has been re-checked against the code by a second pass. The last claim
carried on someone else's word — that `EXPIRED` is terminal in the ticket state machine — was
read and confirmed, and then narrowed: the drop is real, but on the web UI its window is one
refresh, not the validity period. See LIVE 4a.


## Shared-runtime follow-up (2026-09-19)

These findings were established during the documentation pass. All six are **OPEN;
no runtime fixes were made in this pass**. Current behavior is now documented;
the stronger guarantees in the old prose are preserved here as proposed repairs.
These are reachable code paths, not claims of observed production incidents.
See [reading coverage and evidence](structure_map_2026-09-19.md).

### R1. Routing validation admits names that cannot be activated — PARTLY FIXED locally; mandatory-signal design remains

Intended guarantee: construction proves route targets exist and return-control
signals have routes. Actual: `MultiAgentManager._validate_strict_routing_config`
unions role-binding keys AND values into `routable`, without checking that targets
were loaded. It checks prefixes only for return-control keys already supplied.
`AgentLoader` loads only the declared agent/node entries; `resolve_role_binding`
performs one lookup and `_run_loop` then asks the registry for that instance.
A missing instance or edge is therefore still a runtime failure. Optional
`tool_return`, `critic`, and `summary` blocks skip subsection checks when non-dicts.

Evidence: full reads of manager, loader, Delegator, factory and agent validator;
an isolated execution of the actual validation method accepted
`role_bindings={alias: missing}` with `state_map={planner: alias}` and no
`planner_return_control` edge. Boot's unused-agent check is a reference scan,
not graph reachability. Proposed repair: validate resolved targets against loaded
instances and distinguish mandatory signal edges from optional ones.

### R2. Default error exit is labeled final_answer — FIXED locally

Intended guarantee: aborts are distinguishable from completions by result type.
Actual: `handle_default_error_exit` returns `final_answer`, error prose, and
`data_list=[{}]`, without structured abort data. It is reached when graceful-exit
handling cannot finish. Cancellation and a successful graceful abort report do
use `manager_aborted`.

Evidence: full manager and exit-node reads; isolated execution of the actual
fallback method confirms its type and missing structured abort payload. Proposed
repair: give every failed exit a consistent abort envelope and check consumers
before changing the result contract.

### R3. Mailbox queues can survive an ended invocation — FIXED locally

Intended guarantee in the comments: unregister and TTL prevent lingering queues.
Actual: unregister clears the queue once. `Mailbox.post` does not check liveness,
so a later post recreates it. TTL is checked only by `drain`; `peek_count` does not
prune and there is no expiry worker in this module. An ended id will normally
never be drained again.

Evidence: full mailbox, registry and invoker reads; isolated execution of the real
Mailbox class confirms post-after-clear recreation and TTL enforcement only on
drain. Proposed repair: make queue closure/posting coordinate with invocation
lifecycle, or add explicit expiry reclamation. Preserve legitimate steering
posted while long synchronous tool calls are running.

### R4. Tool visibility filtering is weaker than its comments promised — FIXED locally

Intended guarantees: the narrower sees only permitted candidates, and visible
means executable under the task's restrictions. Actual in
`ToolScopeService.initialize_scope`:

- `ranked_for_narrower` is copied before scope filtering; `_run_narrower` receives
  that copy. The final visible output IS filtered by scope afterwards, so this
  finding does not establish unauthorized tool execution.
- The ranked path filters scope allow/block and authority, but does not separately
  apply `task_allowed_tools` / `task_except_tools`. ScopeAdapter projects those
  restrictions into request data; ToolCaller checks them at execution. A tool can
  therefore be shown and subsequently refused. A non-empty precomputed visible
  list takes a different path through ingress filtering.

Evidence: full scope adapter, visibility service, manager and ToolCaller reads.
Proposed repair: apply the effective task/scope policy to candidate input and final
visibility, preserving the distinction between relevance ranking and permission.

### R5. Mixed final-answer forms lose sibling domain fields — FIXED for matching terminal envelopes; limitations below

Intended guarantee in the developer recipe: a form containing both `outcome` and
`final_answer_*` returns the domain outcome to programmatic consumers. Actual:
FinalAnswerNode and ManagerExitNode prefer their constructed `final_fields` dict
when an envelope field is populated. Carry-through adds summary/pod references,
but arbitrary siblings such as `outcome` and `revision_log_id` are not copied.
`final_answer_raw` stores that selected payload, so it does not restore siblings.
ManagerExitNode's terminal-agent capture only runs when final fields are absent.

Evidence: full reads of both exit nodes, FinalAnswerNormalizer, manager exit
handling, and the example `kg_mutation/final_answer/agent_form.py`. Proposed
repair: retain the winning agent's full structured output separately from the
human envelope, with tests for mixed forms and stale earlier-agent results.
Current documentation tells callers to verify the returned shape and use explicit
final-answer data items or a domain-only terminal form when needed.

### R6. Budget checks can override completion at the boundary — FIXED locally

Intended behavior to review: a successful last permitted activation should be able
to finish. Actual `_run_loop` checks cancellation, agent budget, iteration cap,
then `exit`, then `error`. If the last budgeted agent sets `exit=True`, the next
boundary returns `max_cycles` before checking it. A control node setting exit on
the final permitted iteration encounters the same ordering. Cancellation priority
may be deliberate; budget-versus-completion priority needs an explicit decision.

Evidence: full manager loop read. No production occurrence established and no
behavior change made. Proposed repair: define precedence and cover completion
exactly at each budget boundary before reordering the checks.


## Work-object follow-up (2026-09-19)

The original audit corrected documentation only; later repair statuses are marked per entry. Stronger
promises from the old comments are preserved here for later implementation work.
These are code-path findings, not claims of observed production incidents. See
[coverage and checks](structure_map_work_objects_2026-09-19.md). The non-exclusive
same-status dispatch claim is already LIVE 3 above and is not a new finding.

### WO1. Result recording is only partly epoch-fenced — FIXED for result commits; broader worker-write fencing remains

Intended: a stale result cannot alter its successor, and completion always has its
evidence. Actual `record_tool_result` loads once, may dispatch-hop and attach a pod,
then fences `set_status`, then appends evidence in another transaction. A stale result
can attach its pod before being rejected; a crash after status commit leaves no result
evidence, and later retries skip ended nodes. Concurrent same-epoch recorders can also
pass the initial open check and append duplicate results. The method catches every
ValueError from the status write, not just stale epochs; False does not imply no writes.

Evidence: full recorder/store reads and an isolated execution of the actual recorder
against an in-memory WorkStore: expected epoch 0 versus current 1 returned False but
persisted the stale pod. No app bootstrap or production DB was used. Proposed repair:
a single store operation that checks incarnation and eligible state, then atomically
writes pod, outcome and evidence, with an explicit duplicate-result policy.

### WO2. The audit log cannot rebuild graph state — OPEN

Intended: events are authoritative and the graph is replayable. Actual `apply` logs
original input data; generated goal/node/edge/action IDs and implicit cascade results
are not fully captured. There is no replay method in WorkStore. Boot migrations and
closure repair also write graph state outside ordinary mutation events.

Evidence: full model/store/accessor reads; in-memory creation confirms its generated
goal ID is absent from event data. Proposed repair: choose a complete deterministic
event contract and replay verification, or explicitly adopt graph state plus audit log
as the permanent persistence design. Current docs describe the latter behavior.

### WO3. Graph validation does not enforce the promised contract — FIXED for new writes; historical validation remains

Intended: acyclic dependency graph, inherited authority ceilings and validated lifecycle
entry. Actual validation checks ownership cycles and edge endpoints, but permits
self/duplicate/cyclic dependencies. `add_node` compares explicit authority only against
an explicit immediate-parent value; a parent with None bypasses an ancestor ceiling.
Initial status strings bypass transition and reason checks. Budget/deadline/approval
fields are metadata in this writer, and actor/licensed are caller-supplied assertions.
This finding does not establish a bypass of the separate tool execution scope gates.

Evidence: full model/store/tools reads; in-memory probes accepted a dependency cycle,
authority 99 below an authority-1 ancestor through a None parent, and a newly created
closed node without a reason. Proposed repair: define creation validation, effective
inherited authority, dependency constraints and the trusted writer boundary before
adding enforcement. Preserve legitimate same-status metadata updates (LIVE 3).

### WO4. Closure can disagree with the goal and miss ask-only repairs — PARTLY FIXED; historical repair held

Intended: container closure has a consistent goal state and repairs all closure zombies.
Actual `_rollup` changes only a dispatched goal to done before cascading startable nodes.
A proposed goal with satisfied children becomes abandoned while its container and final
epitaph say done. `repair_terminal_zombies` selects terminal containers with startable
nodes, omitting a container whose only remaining in-flight node is a user_reply ask,
even though the cascade helper would abandon that ask if invoked.

Evidence: full model/store reads; an in-memory object with a proposed goal and one
born-closed child produced container done / goal abandoned. Repair selection verified
by its SQL and cascade predicate. Proposed repair: define goal mirroring for all closure
entry points and align repair selection with the cascade's complete predicate.

### WO5. Legacy standalone driver no longer meets the dispatch contract — OPEN

Intended: `drive_work` advances a standalone graph to completion. Actual automatic
branch selects ready nodes but calls `discharge_node` without first claiming them;
that callee requires dispatched and raises. The explicit-node branch also requires a
preclaim, and neither branch invokes finalization. Evidence: full discharge module
read, including both caller and callee. Proposed repair: use the standard dispatch /
record / finalize lifecycle for scenarios, or retire the obsolete automatic helper.

### WO6. UI object-abandon action always lacks the required reason — FIXED locally

Repair: the endpoint supplies "owner abandoned via /work". A Flask route regression
checks HTTP 200, the persisted reason and the unstarted-child cascade. Running threads
are still not cancelled by this endpoint. Not yet published.

Intended: the manual editor can abandon an object through the validated writer.
Actual `api_work_abandon` sends only work_id and status; `_op_set_work_status` requires
a nonempty reason. `_apply` converts the resulting ValueError to HTTP 400. Evidence:
full blueprint/store reads and an in-memory execution of the same store request.
Proposed repair: supply an explicit user/manual-action reason. Do not imply graph
closure cancels a running worker or ticket; that requires a separate lifecycle policy.

### WO7. Store setup can conceal live-store failures — FIXED locally

Intended: UI dev fallback is for an unavailable app, and successful initialization
means migrations succeeded. Actual `_get_store` catches any live-store exception and
opens WORK_DB; the accessor's two migrations log and swallow errors before the singleton
may be cached. This can show a different DB or continue with unmigrated rows.

Evidence: full UI/accessor reads. Proposed repair: limit dev fallback to an explicit
standalone mode and define which migration errors must block initialization. Preserve
`store.path` reporting (LIVE 1 was already fixed).

### WO8. Wake metadata does not guarantee a real readiness gate — FIXED locally

Intended: deferred nodes carry a usable revisit condition. Actual `defer_node` validates
wake-kind vocabulary but not required time/reference combinations. Generic `is_ready`
checks status, future wake_at and dependencies only; a waiting event/signal reference
does not itself keep a node unready. Higher-level matching must supply that policy.

Evidence: full model/store/tool reads; an in-memory node with an unfulfilled event
reference and no future timestamp was ready. Proposed repair: define the division
between generic readiness and wake matching, validate required wake fields, and audit
callers before changing the predicate. No claim that this probe caused a production
redispatch is made.


### WO9. Invalid deferred timestamps can make a graph unreadable — FIXED locally

Repair: defer_node validates ISO strings/datetime values before assignment, accepts
null to clear the wake, and treats naive timestamps as UTC, matching persistence
conventions. Regressions assert malformed input leaves both graph and events unchanged,
and cover UTC/offset/naive round trips and clearing. This prevents new invalid writes;
it does not repair existing malformed rows. No schema migration. Not yet published.

`_op_defer_node` assigns `data["wake_at"]` directly onto an existing WorkNode.
Assignment is not Pydantic-validated; structural validation does not check timestamps,
and `_iso` persists an invalid string as supplied. A subsequent `WorkStore.load`
reconstructs the Pydantic model and raises ValidationError. One malformed defer can
therefore commit successfully and prevent normal reads and later apply calls for that
work object. This is stronger than the missing wake-field checks in WO8.

Evidence: model/store reads and an isolated in-memory probe: create an object, defer
its goal with wake_kind=time and wake_at=not-a-date; apply succeeds and the next load
raises ValidationError. No production data was touched. Proposed repair: parse and
validate wake timestamps before mutating/persisting, define timezone normalization,
and reject malformed input without committing an event or graph change.


## End-to-end Dayflow logic review (2026-09-19)

Evidence below is source tracing, not claims of observed production incidents or new
passing tests. All DF findings are OPEN. The full flow, boundaries and repair order are
in `dayflow_end_to_end_review_2026-09-19.md`. Existing findings retain their status.

### Existing findings: additional integration evidence

- **LIVE 3 (held decision):** dispatch's `_claim` is a same-status-capable `set_status`,
  not a compare-and-set claim. `open_session` registers by work/node, not incarnation.
  Do not describe either as exclusive. `work_node_dispatch_node` also accepts a qualified
  model-produced reference without proving membership in the offered candidates.
- **WO1:** normal `DayflowToolCaller.action_handler` ignores the boolean returned by
  `record_tool_result`, then enters `WorkFinalizerNode`. Recovery checks that boolean.
  Finalizer lookup and `_apply` carry no expected epoch/state; a delayed judgment can
  act on a successor (e.g. fail a newly dispatched attempt). Its multi-write retry
  sequence is also interruptible. `node_result` combines evidence from all attempts,
  without epoch selection. `sweep_stuck_work_nodes` and dispatch `_fail_node` likewise
  fail a node from a prior observation without incarnation/state comparison. A fenced
  recorder alone will not repair this lifecycle. Repeated-failure escalation also uses
  the pre-LLM goal count, so concurrent judgments can both miss the threshold.
- **WO8 / WO9:** the WO9 store validation repair remains valid, but architect `_to_dt`
  converts invalid timestamps to None before the store sees them, removing the time gate
  (DF6). This is a distinct caller defect, not proof that end-to-end deferred execution
  is fixed. State mover `_apply_node_wakes` accepts refs outside its offered waiting set,
  includes user_reply, and lacks a status/epoch guard; clearing a dispatched wait and
  restoring dispatched with evidence can create another epoch. `_park_held` retains
  user_reply on legacy asks, while the precise scheduler arms only time wakes.

### DF1. Restart recovery does not match the normal ticket producer — FIXED locally (2026-09-19; limitations below)

`work_session.re_arm_inflight_asks` queries suggestion_type=work_notify, within 48 hours
and 200 rows. Normal `DayflowSwitchboardArgumentsNode` supplies a brief, and
`CreateDayflowTicketTool._format_brief` obtains a free category from ticket_builder
(default general); the composer form describes logistics/wellness/scheduling categories.
No deterministic work_notify assignment connects these paths. Normal asks can therefore
be invisible to restart recovery, leaving dispatched nodes for the inactivity sweeper.
The new path also does not stamp wake_kind=user_reply or record the ticket as a work
object action, despite ask-cascade/action-history documentation relying on those fields.
Repair the producer/consumer contract using durable call identity, not model category.

### DF2. Ticket construction drops its graph context — FIXED locally (2026-09-19; limitations below)

`CreateDayflowTicketTool.execute` calls `_format_brief(args["ticket_brief"])`, although
that helper accepts work_node_ref and uses it to give ticket_builder the graph reference.
The dispatch arguments contain trigger_context.work_node but do not forward it here.
A delivery node saying to tell the user a previous step's result can reach the composer
without that result or its lookup reference. Optional research links do not cover general
worker results. Pass the canonical node context through the actual call boundary.

### DF3. Worker provenance exposed as schedulable work — FIXED locally

Repair: `is_work_unit` consistently limits orchestration to direct task children of
the goal. Promotion, waits/wakes, dispatch/session/recovery and supervision exclude
provenance, including previously actionable records. Architect references cannot name
provenance. Worker/finalizer views include full owned history; strategic views show
finalizer summaries. UI labels provenance explicitly. No schema or historical-row
migration. Eleven focused regressions cover this boundary; the Dayflow suite passes
371 tests after updating superseded expectations. No live rollout or push.

User-confirmed ownership contract: worker-created descendants are internal helper
records for the assigned main node, not independently schedulable work. The architect
and steward must never assign an agent to them. Their persistence in the same nodes
table does not grant the orchestrator execution ownership. Missing parent dependency
edges are not the defect and adding them is not the proposed repair. The defect below
is that candidate generation does not enforce this boundary before offering helpers
to the action selector. Actual duplicate execution still requires a concurrent tick
and selection of an exposed helper; no production occurrence was established here.

`ManagerInterface._run_on_child_node` creates a proposed subtask and runs its manager
synchronously without claiming it. `WorkPlanner._reconcile_to_graph` also creates
proposed checklist children. Both state-mover promotion and materialization iterate
all depths; neither excludes descendants of a running worker. The planning gate does
not lock dispatch threads. During a child call, another tick can promote and dispatch
that same child, duplicating work/side effects; queued checklist work can also run out
of the worker's chosen order. The interface comment that nothing can race is false.
Enforce top-level scheduler ownership and explicit nested execution ownership together.

### DF4. A failed finalizer strands recorded results — FIXED locally (2026-09-19)

`WorkFinalizerNode.action_handler` catches judgment/write failures; an unusable verdict
returns an empty result. A tool result already recorded as done stays done. Boot ask
recovery and the inactivity sweeper select dispatched nodes, and planning has no
finalizer stage. There is no deterministic retry of this unjudged result. A crash
between recording and finalizing has the same consequence. Steward intervention is
not recovery of the missing judgment. Persist/rediscover pending adjudication, and
retry judgment without executing the external tool again.

### DF5. Pending replans do not prevent execution — FIXED locally (2026-09-19)

Finalizer retry writes failed then proposed. Architect handles at most three replan
objects per tick and catches per-object errors. State mover subsequently promotes ready
nodes regardless of pending finalizer instructions. A deferred/failed replan can thus
redispatch the unchanged failed step. achieved_plan_changes closes a node and unblocks
dependents before the pending graph revision is applied. Hold affected execution until
the exact instruction has been successfully applied, including replans arriving during
the planning pass.

### DF6. Architect application can expose an incomplete or ungated plan — FIXED locally (2026-09-19; limitations below)

`apply_architect_dag` commits prunes, additions, edges and wakes separately; its caller
logs errors and continues to promotion. An error after additions can leave runnable
nodes missing dependencies/wakes. Missing dependency IDs are silently skipped.
`_to_dt` logs malformed times and returns None, allowing immediate execution instead
of rejecting the plan. Validate the complete delta first and publish it atomically,
or use an explicit non-dispatchable construction state with recovery.

### DF7. Replan acknowledgement can consume the wrong verdict — FIXED locally (2026-09-19)

`WorkArchitectNode` snapshots pending instructions before its LLM call, then consumes
by work_id/node_id only. `_op_consume_finalizer_instruction` stamps whichever finalizer
payload exists at write time. A new verdict during the call can be acknowledged without
being planned. Separately, refused prunes are swallowed by `apply_architect_dag`, so
its return is not proof that the requested revision succeeded, yet instructions are
consumed. Compare instruction identity/version and acknowledge only applied changes.

### DF8. Deduplicating a node leaves its dependents attached to the discarded copy — FIXED locally (2026-09-19)

`apply_architect_dag` validates a surviving twin and abandons the duplicate, but does
not redirect depends_on edges. `WorkObject.is_ready` requires each named dependency to
be satisfied; an abandoned tool-success subtask is not. A downstream node can remain
blocked after the surviving twin succeeds. Rewire/validate affected dependencies as
part of the same revision. This is distinct from general dependency validation (WO3).

### DF9. Admitted intake has no durable evaluator inbox — FIXED locally

Triage persists ADMIT as artifact. `IntakeTriagePrepNode` offers new items, while the
active evaluator config consumes this tick's admitted_artifacts, not an outstanding
admitted backlog. Failure after admission but before work creation can strand actionable
intake outside later evaluator inputs. Existing artifact display in triage is not a
reliable evaluator handoff. The same gap affects admitted work deliberately left for
later. Keep admitted-unconsumed intake in the evaluator's durable input set.

### DF10. Intake handoff loses source detail at creation/change boundaries — FIXED locally for durable source handoff

`StrategicPlannerWoPersistNode._fold_intake_into_goals` stores source summaries/pod IDs
in goal.content, but catches failures; `_close_consumed_items` closes items anyway.
Initial `WorkArchitectNode` receives created["objective"] plus rationale and a portfolio
built before creation, not the enriched goal. Thus even a successful fold does not
supply the first architect with the promised source handles. Later `persist_steward_output`
changes replace goal.content with objective, removing folded provenance. Preserve source
context separately, pass the stored goal on creation, and close intake only after a
successful durable transfer. Partial creation failure also discards the returned created
list in the persist node, leaving already-created goals without this tick's decomposition.

### DF11. Ticket publication can race reply-listener registration — FIXED locally (2026-09-19; limitations below)

`CreateDayflowTicketTool.execute` publishes the suggestion (and optionally TTS) before
`_wait_for_ticket_response` registers its listener. The normal wait does not re-read
the durable ticket after registration or timeout. A response in the gap can therefore
be recorded in the DB but reported to the graph as timeout. Recovery's extra read
corrects the result only after waiting out the timeout. Register/reconcile against the
stored ticket so an already-arrived answer completes promptly. This is separate from
LIVE 4a's already-fixed handling of a genuinely late reply.

### DF12. Recovery tickets are not bound to dispatch incarnation — FIXED locally (2026-09-19; limitations below)

`re_arm_inflight_asks` chooses the newest ticket by node reference and records it using
the node's current epoch. The ticket binding contains no captured dispatch epoch. If
an earlier attempt has a settled ticket and a successor crashes before minting its own,
boot can apply the old answer to the new attempt. Session IDs also omit incarnation.
Persist the ticket/call association and recover only the matching attempt (WO1 related).

### DF13. A hold during a precise wake has no replacement precise wake — FIXED locally (2026-09-19; limitations below)

`DayflowScheduler._fire_work_node` invokes the wake manager and returns. If state mover
parks the node at a new reactivate_at, `_arm_work_node_wakes` is not called there; it
runs after planning ticks only. The consumed one-shot job has no replacement until
another tick, potentially the 30-minute ceiling. Re-arm the new due time after a wake
pass, including failure handling. Stale per-node jobs are not removed when gates change;
callback checks do not establish that the original time gate still applies.

### DF14. Scheduler stop leaves timed execution jobs alive — FIXED locally (2026-09-19; limitations below)

`DayflowScheduler.stop` removes the main JOB_ID only. Per-node jobs installed by
`_arm_work_node_wakes` remain; `_fire_work_node` does not check _started. Calling stop
while the timing engine continues can still invoke a wake manager and dispatch work.
Remove the owned jobs and guard callbacks, including jobs already waiting for the gate.

### DF15. Cadence overwrites newly written watermarks and chat blocking — FIXED locally (2026-09-19; limitations below)

`dayflow_orchestrator_cadence_tick` loads status before ingestion, then persists that old
dict after manager invocation. Ingestion writes chat/pod watermarks into the same resource.
`load_orchestrator_status` returns a copy and `ResourceManager.update_resource` replaces
the value; it does not merge. Successful ticks overwrite their new cursors. Master-room
blocking updates made while the manager runs can be lost too. Use synchronized field
updates rather than a long-lived whole-resource snapshot. Persistence file errors are
also logged/swallowed by ResourceManager, so return does not prove durable status.

### DF16. Ingestion advances cursors before saving intake — FIXED locally (2026-09-19; limitations below)

`ingestion._ingest_chat` and `_ingest_pods` persist their watermarks before
`run_dayflow_ingestion` writes its destination batch. A later source/write failure or
process crash leaves cursors beyond missing intake. The old-snapshot overwrite in DF15
sometimes hides this on successful ticks; it is not recovery after an interrupted tick.
Commit items before advancing, with idempotent restart or a transactional cursor design.

### DF17. Bounded source queries can skip or starve intake — FIXED locally (2026-09-19; limitations below)

Pod ingestion asks `PodStore.query` for 200 newest rows, filters kinds afterwards, then
advances to the newest matching timestamp. Older unprocessed eligible pods can be left
behind the cursor. Chat ingestion takes 500 ascending rows before Python noise/mode
filtering: 500 excluded rows can yield no messages and no cursor progress, repeatedly
hiding later legitimate chat. Timestamp-only `>` pagination can also skip rows sharing
a batch-boundary timestamp; failed row conversion can be skipped once a later row
advances the cursor. Use stable paginated cursors and explicit failure disposition.
DF15's rewind can mask some cursor progression symptoms while causing repeated scans.

### DF18. Delegation ingestion acknowledgement is not repaired after a crash — FIXED locally (2026-09-19; limitations below)

`_ingest_delegation_requests` returns raw requests to acknowledge only when their item
IDs are new. If the batch commits but marking the requests ingested fails, subsequent
runs deduplicate them and never retry that acknowledgement. The request remains pending
until some separate lifecycle cleanup. Reconcile existing destination IDs as successful
handoffs too; do not recreate the work just to acknowledge the request.

### DF19. Short intake IDs can collide after the reset threshold — FIXED locally (2026-09-19; limitations below)

`run_dayflow_ingestion` resets max(existing short_id)+1 to 1 at 10000 without checking
existing occupancy. Subsequent batches can keep resetting while older maximum IDs
remain. Triage and consumption accept short IDs via maps, so two live artifacts can
become ambiguous or the wrong one can be consumed. Use unique stable references or an
allocator that excludes every still-addressable ID. This verifies the collision mechanism
behind historical comments about duplicate short IDs; no production count was measured.

### DF20. Finalizer directive and evaluator success criteria — FIXED locally for directive and criteria delivery

The finalizer now receives full node.content, its success-condition kind and the full
owned provenance history, alongside the returned result. The prompt tells it to summarize
attempts, failures, accomplishments and unresolved work for the strategic agents. The
separate evaluator success_criteria persistence gap below remains OPEN.

`WorkFinalizerNode._judge` sends full result text and node.title. Its task is
`render_work_portfolio`, whose subtask rows use `_t` (title first, capped at 90 chars),
not the node's full content. The goal's full content is present, but the particular
step's detailed constraints are absent. A plausible result can be judged achieved
without checking the actual instruction. Include immutable node content and explicit
success conditions alongside the attempt's result. Evaluator `success_criteria` is also
emitted by WorkObjectSpec but never persisted by `persist_steward_output`; this loses
criteria that were not repeated in objective.

### DF21. Returned manager failures are not treated as failed runs — FIXED locally (2026-09-19; limitations below)

Cadence ignores its manager ToolResult; scheduler health records ok unless an exception
escapes. `_run_dispatch_session` likewise ignores its manager result; an aborted dispatch
before recording can leave the node dispatched until the inactivity sweep. Internal
control nodes often log and continue, further masking incomplete stages. Interpret the
manager's exit contract and report the failed stage; preserve the distinction between
re-running a tool and retrying result/finalizer persistence. See R2/R5 for shared envelope
problems rather than duplicating them here.

### DF22. Queued nodes bypass a fresh readiness check — FIXED locally (2026-09-19)

`WorkNodeMaterializerNode._node_items` tests actionable only, not current dependencies
or time/event gates. Dispatch `_claim` does not recheck them either. If a replan adds a
dependency to an already-actionable node, it can still execute before that dependency.
Promotion validated an earlier graph, not the graph being dispatched. Validate readiness
and eligible ownership atomically with the actual claim (LIVE 3/WO8 related).

### Additional concerns retained for follow-up — SUSPECTED / scope limited

- Scheduler setup-incomplete/no-history early returns consume the one-shot tick without
  rearming. A later external poke can restart it; verify onboarding emits that poke
  before classifying this as a user-visible permanent stall.
- `_situational_context` and state-mover ticket templates omit acknowledged responses;
  evaluator uses the complete flattened list. Recent-completed reply extraction filters
  created_by=reply, whereas current recorders use the tool name or ask. These are confirmed
  context omissions, but the practical loss depends on the other projections supplied.
- `result_for_ticket` treats snoozed as live, while a snooze response event ends the normal
  wait. Confirm the intended snooze semantics before choosing the canonical result.
- Inactivity sweep fails without finalizer judgment/instructions. The architect's prune
  license comes from finalizer or steward-classified user direction; trace the desired
  automatic timeout repair policy before broadening its authority.
- Automatic goal rollup does not call the explicit steward's concern-feedback helper.
  Verify other concern reconciliation before asserting a lost belief-system update.
- WorkPlanner's status projection can become inconsistent after scheduler promotion:
  `_transition_subtask` does not handle actionable when resuming/completing checklist
  children. Address with DF3 rather than adding permissive transitions in isolation.


### DF23. Internal failures inflated the main goal failure count — FIXED locally

`_count_unmet_attempt` previously counted every non-goal record entering failed. A
worker's internal unsuccessful attempt could therefore increase goal_unmet_attempts
and trigger an ask-user escalation even though its assigned main task had not failed.
The counter now counts direct main tasks only; helper-local failure_count still records
the internal attempt. A focused regression reproduced the inflated count before repair.
Existing stored counts are not rewritten. This was found while implementing DF3.


### DF24. Failure counters are driven by status transitions, not finalizer judgment — FIXED (2026-09-19)

User clarification: the finalizer owns incrementing a main node's failure count.
Actual `_op_set_status` increments payload.failure_count whenever a record enters
failed, regardless of actor; `_count_unmet_attempt` also increments the goal count for
main tasks. Tool recording, dispatch errors and sweeper failures can therefore count
before adjudication. WorkFinalizerNode._apply explicitly assumes an already-failed
node was already counted, and adds no judgment count in that case. A later achieved
verdict does not undo the premature failure count. The DF23 repair excludes internal
helpers from the goal tally but does not implement finalizer ownership of these counts.
Repair should count a not-achieved finalizer judgment once per dispatch attempt, with
incarnation/idempotency protection (WO1); execution errors remain result evidence.
Evidence: store.py `_op_set_status` and `_count_unmet_attempt`, and finalizer `_apply`
source reads. No repair or historical counter rewrite in this clarification pass.


### DF25. Failed worker-context refresh retains stale decision input — FIXED locally (2026-09-19)

WorkPlanner.construct_prompt refreshes work_projection because resume routing can skip
WorkObjectRenderNode. Its `_refresh_work_projection` logs a store/render error but neither
clears the old projection nor stops prompt construction. A worker can therefore make its
next decision using previous graph state after the fresh read failed. Required context
checks alone would not catch this because the stale value is nonempty. Preparation must
fail the decision explicitly when required graph context cannot be refreshed. Evidence:
WorkPlanner.construct_prompt/_refresh_work_projection source. No live incident measured.

### DF26. Dormant WorkFinalAnswer contradicts the current judgment contract — DORMANT

WorkFinalAnswer's docstring calls it the finalizer; `_write_node_termination` can overwrite
the assigned node's content with result_summary and set a terminal status. This conflicts
with the immutable directive and separate recorder/WorkFinalizerNode design. Its refresh
also retains old work_projection on failure. No current agent YAML selects this class,
and no other Python reference was found beyond the render-node comment: this is dormant
code/documentation risk, not an established defect in the active finalizer path. Do not
reuse it as the implementation for prompt standardization. Explicitly retire or reconcile
it in a separate cleanup; no runtime change made during this assessment.


#### 2026-09-19 repair verification: DF24 and partial WO1

`record_result` now commits its epoch check, evidence, pod reference and result status
in one store transaction. Stale or duplicate results are no-ops at the caller boundary;
unexpected storage errors propagate. Three isolated checks cover stale results, duplicate
results and rollback after an injected persistence failure.

`finalize_task` now counts unmet outcomes and commits judgment/status atomically, once
per dispatch epoch. Execution status changes no longer count failures. Five checks cover
failed calls, achieved judgments after errors, stale/duplicate judgments and escalation.
Historical counters have not been rewritten. WO1 remains open for cross-connection
serialization, exclusive claims and outstanding recovery callers.

Additional confirmed defect found during this repair: the previous finalizer attempted
`failed -> closed`, which the generic transition table rejected. A tool error could never
receive an achieved verdict. The judgment transaction now performs the result-to-close
transition atomically; the achieved-after-error regression covers it.

Projection verification found and corrected a draft-template regression: obsolete wake
metadata on abandoned/superseded tasks must not render as an active wait. Strategic
renderers now share Jinja and have a focused terminal-wake check.


#### 2026-09-19 continued repair verification

- **WO1:** a two-connection regression reproduced graph mutation from a stale read.
  `WorkStore.apply` now executes `BEGIN IMMEDIATE` before loading. The regression and
  atomic-result/finalizer checks pass. Remaining epoch handoff and timeout writers
  still require review; this entry is not a claim that all WO1 paths are repaired.
- **DF4:** cadence now resumes saved, unjudged main-task results through the actual
  `WorkFinalizerNode`, without rerunning tools. It skips live sessions, fences the
  dispatch epoch and leaves already-judged legacy records alone. Three isolated
  recovery checks pass; recovered asks use the same finalization helper.
- **DF5 / DF22 / LIVE 3:** `claim_task` is exclusive and checks current readiness in
  the store transaction. Pending finalizer revision instructions gate main tasks.
  The materializer checks current dependencies and external/time gates too. A losing
  claimant cannot fail the current owner's call. Six before/after regressions and
  the existing dispatch/thread tests pass.
- **DF6:** node creation, pruning, dependency writes and wakes now commit in one
  graph batch. Invalid wake timestamps and missing dependencies raise; refused
  pruning aborts the revision. Snapshot version checks reject stale revisions.
  General dependency-cycle validation remains in WO3; do not infer it is fixed.
- **DF7:** exact finalizer instruction comparison prevents acknowledging a newer
  judgment. Acknowledgement and graph changes now commit in the same batch.
  Successful and stale-instruction rollback checks pass.
- **DF8:** deduplication transfers both prerequisites and consumers to the retained
  main task in the same revision, rejecting cyclic merges. The previously failing
  dependency-transfer check passes.
- **DF25:** failed worker-context refresh clears the stale projection and raises;
  the render pre-node also stops instead of supplying fallback instructions.
  A before/after fake-store regression verifies that the decision cannot proceed.
- **DF11:** ticket waiting reconciles the durable row after subscribing, after the
  wait and around expiry. Both reproduced reply-gap/expiry-race checks pass.
  Concurrent ticket transition writes still need the separate review below.
- **DF2:** normal ticket formatting now receives `trigger_context.work_node`.
- **DF10 (partial):** initial architect input now reads the stored enriched goal.
  Durable intake handoff and changed-goal provenance retention remain open.

**New review concern, ticket state concurrency (not yet reproduced):**
`TicketManager._transition_state_in_session` validates an ORM snapshot then assigns
state/history without a compare-and-set on the old state. Separate transactions
may both validate an earlier state and overwrite a concurrent reply/expiry. The
waiter repair above reconciles the actual row; it does not prove the writer race
is impossible. Inspect the session transaction policy and reproduce before repair.

**Prompt inspection artifact:** `docs/design/examples/dayflow_complex_work_prompts.md`
is generated from actual architect/steward templates using synthetic data. The
architect Message is captured through its replan control node with writes and LLM
calls mocked. It covers all main-task statuses, pending finalizer instructions,
full directives, dependency satisfaction and failure counts, and excludes raw
worker provenance. Worker and finalizer use separate full-history Jinja views.


#### 2026-09-19 ticket, timer, intake and graph repair verification

- **DF1 / DF2 / DF12:** tickets carry the claimed dispatch epoch and are bound to the
  task before publication. Restart recovery joins on work/node/epoch across all
  Dayflow tickets, without a category or 48-hour cutoff. Old tickets lacking an
  epoch are deliberately not attached to an unproven attempt; no historical data
  migration was performed. Actual ticket execution and stale-recovery tests pass.
- **DF11:** durable-row reconciliation covers replies before subscription and at
  expiry. The separately suspected concurrent TicketManager writer race remains
  unverified; this fix does not claim to resolve that separate concern.
- **DF21:** returned dispatch aborts and thrown exceptions use the common result
  recorder and finalizer when the attempt has no saved result. Cadence and wake
  managers inspect returned failure results too. Shared-runtime false-success
  labeling remains R2, not covered by this boundary check.
- **DF13 / DF14:** wake passes re-arm precise jobs, obsolete jobs are removed,
  stop removes all owned jobs, and callbacks recheck started state. Schedule writes
  are serialized with stop. Failed wake passes wait at least 120 seconds before
  retrying overdue jobs instead of entering a two-second failure loop. Sixteen
  focused scheduler/wake checks pass. Event subscription lifecycle still needs review.
- **DF15–DF19:** status writers patch only owned fields under a process lock;
  ingestion acknowledges cursors after destination writes; complete inclusive
  source windows avoid truncation and timestamp ties; conversion failures raise;
  delegation acknowledgements retry even when the destination exists; short IDs
  no longer wrap at 10,000. Eight isolated intake checks pass. Complete-window
  scans use memory proportional to the backlog. The item-map's seven-day window
  still needs review for historical short-ID uniqueness and deduplication.
- **WO3:** new dependency writes reject self-links, duplicates and cycles; new
  nodes validate initial lifecycle status and every explicit ancestor authority
  ceiling. These are write checks, not a migration or repair of old graphs.
- **WO4 (partial):** rollup marks a satisfied goal done before abandonment of
  outstanding work. Repair of historical ask-only terminal objects is still open.
- **New confirmed main-task satisfaction bypass:** all_owned_children_done or
  verified_by on a main task could satisfy it without finalizer closure. Main
  assignments now require closed for every success kind; helper completion stays
  internal provenance. Seven graph regressions pass (27 with architect/tool checks).
- **DF9 / DF20 (partial):** admission is durable item metadata and the evaluator
  reloads its pending inbox without age truncation. Creation retains success
  criteria. Source handoff and changed-goal criteria remain under repair.

Latest complete Dayflow run at this checkpoint: 421 passed, two old context-shape
assertions failed because trigger_context now includes dispatch_epoch. Those
assertions were updated to verify the epoch; their focused suite passes. A final
whole-suite run is still required after the remaining repairs.

## Latest repair checkpoint — 2026-09-19

This checkpoint supersedes earlier OPEN/partial descriptions where a repair is listed.
The original behavior and evidence above are retained as audit history, not descriptions
of the current implementation. All repairs remain local and unpublished.

- R1: aliases resolve to configured/loaded instances, optional routing blocks must
  be mappings; removed 25 obsolete tool_selector bindings. Mandatory return-control
  edges for arbitrary dynamic forms are not automatically inferred.
- R2/R6: fallback errors return manager_aborted with structured abort data; completion
  wins over budget boundaries, and terminal control nodes can run after the last agent.
- R3: registration opens mailboxes, unregister closes them, late posts cannot recreate
  them, and peek prunes expired entries. Existing tests now honor registration and
  the already-existing structured, timestamped agent-injection contract.
- R4: narrower input and final visibility both apply task and scope restrictions.
- R5: matching latest terminal envelopes retain sibling domain fields in the raw
  result. Earlier mismatched results are not searched as a fallback. This does not
  prove every possible custom flow with stale final fields and later domain-only output.
- WO1: result status, evidence and pod are atomic with epoch checks; finalizer judgment
  is atomic and counted once. Sweeper checks idle timestamps again inside that commit.
  Timeout results can be finalized despite a still-running old thread. Worker helper
  mutations through WorkContext are not yet fenced by a captured dispatch epoch.
- WO3/WO8: new writes validate dependency cycles, ancestor authority, lifecycle status
  and required wake data; external event/signal gates are unready until explicitly matched.
- WO4: goal completion rolls up consistently. Historical ask-only terminal-container
  repair remains held because it rewrites stored data; no such migration was performed.
- WO7: UI/store initialization failures propagate; no silent alternate database.
- DF9/DF10/DF20: durable admitted inbox survives restart; full source content and pod
  handles are stored before intake closes, including changed-goal criteria. Reconciliation
  closes already-transferred intake, and empty graphs recover decomposition from stored goals.
- DF11: the previously suspected ticket writer race was reproduced with SQLite. A stale
  detached reply snapshot could resurrect an expired ticket. Reply fields now commit
  with the state transition under BEGIN IMMEDIATE; no preliminary snapshot merge.
- DF15–DF19: all retained intake IDs now participate in a compact identity index;
  seven-day prompt windows no longer control deduplication or short-ID allocation.
- Session registration rejects a duplicate same-epoch worker, cleans up failed starts,
  and recovery tests liveness for the exact epoch. A legacy fixture was updated to carry it.
- Scheduler restart defect CONFIRMED and FIXED: EventHub rejects duplicate subscriptions;
  repeat startup now reuses its subscription and partial failures roll back registered handlers.
- Prompt projection: actual architect replan messages and steward templates are rendered
  in examples/dayflow_complex_work_prompts.md. Main tasks show statuses, dependency
  satisfaction, wake gates, attempt numbers, judgment-pending markers and full finalizer
  summaries. Worker provenance is excluded from these strategic views. Discharge and
  legacy work-signal prompt formatting were also moved to Jinja.

Verification: 547 tests passed across Dayflow, manager_runtime, manager_classes and
selected ticket suites. After the final scheduler subscription repair, 16 scheduler /
projection checks passed. After moving legacy signal text to Jinja, 20 visibility checks
passed. One requests dependency-version warning persists. These are isolated offline
checks, not fresh-install, upgrade, live-model or whole-application release evidence.

### Remaining findings and decisions

- WO2: audit events are not a replay-complete persistence format; adopting event sourcing
  is a design change. Current graph state remains authoritative.
- WO5: the old standalone automatic drive_work scenario still omits claim/finalization.
  It is not used by Dayflow; migrate that scenario to the standard lifecycle or retire it.
- DF26: dormant WorkFinalAnswer remains unwired and contradicts the current contract.
- Architect _dedupe_prunes still ignores invalid mappings instead of rejecting the whole
  requested revision. This can acknowledge a revision without the requested consolidation;
  recorded as a remaining extension of DF6, not claimed fixed by atomic batch writes.
- Ticket same-state transition returns success before applying fields. A concurrent second
  same-state reply may be reported as successful without saving its distinct text; needs
  an explicit duplicate-response contract and regression. This is separate from expiry repair.
- MailboxDispatcher counts unknown message types as dispatched even though their handler
  ignores them. This is a diagnostic count defect; supported steering messages still apply.
- Complete/abandon writes in work_persist still log individual failures and continue;
  durable source-handoff fixes do not establish all-or-nothing steward output application.

No historical failure counters or epoch-less tickets were rewritten. No commit or push.

## Scenario review: fridge research and third-party approval — 2026-09-19

### DF27. External-event wake loses reliable source linkage — OPEN

StateMoverPrepNode._build_work_object_waits renders only sender, subject and summary
for emails, truncates the candidate intake list to 30, and renders node context from
(title or content) capped at 140 characters. It does not supply a structured source
item/pod ID, message/thread ID or full task directive to that matching interface.
The state_mover therefore judges a prose wake condition from incomplete input and
cannot bind its selected match to an exact durable email through its current output.
For the fridge scenario, an unrelated or ambiguous the approver reply must not authorize the
brother email. Conservative prompt instructions help but are not a structural guarantee.
The 30-item cap can omit a relevant reply; actual starvation depends on source ordering.
Evidence: direct reads of prep method and state_mover system prompt. No live incident
or reproduction test claimed. Preserve source identity and explicit approval content
through matching and worker execution; do not treat any reply as approval.

### DF28. External wake is neither atomic nor separate from task identity — OPEN

StateMoverPersistNode._apply_node_wakes clears the external gate in one store.apply,
then appends model-produced evidence to node.content in another. A crash or failure
between commits leaves the task eligible without its arrival evidence. Evidence is
also mixed into the original directive rather than stored as an attributed event
record. The method checks main-task identity and wake kind, but does not revalidate
that the condition matches the snapshot judged by the model. Evidence: direct source
read. Proposed repair: one version-checked wake operation storing attributed arrival
provenance and clearing the exact gate atomically; render that record through Jinja.
No runtime change made during this scenario discussion.

## Actual architect render review — 2026-09-19 20:45:59

### DF29. Lifetime failure tally emits a fresh escalation after the user already answered — OPEN

Observed in the user-supplied work_architect MODEL_USER render: work_example_1
contains the user's explicit direction to continue school notifications, take no extra
action, and accept nonresponse. Its closed ask_direct_school_updates_decision task has
an achieved_plan_changes verdict preserving that direction. Nevertheless the portfolio
header says to ask whether to keep going because goal failure count is three. The graph
also shows a subsequent notification abandoned for that tally and another ask dispatched.
The render establishes contradictory context and repeated asks; it does not alone prove
which earlier decision caused each stored mutation. Source confirms portfolio.j2 emits
imperative escalation for every lifetime count >=2, without checking whether the
escalation was answered. Store finalization likewise compares the accumulated tally on
new failures. Preserve historical counts but represent the current unresolved escalation
separately; resolved instructions must not be reissued by a count-based template.

Additional observed projection gaps, not yet traced to their original producers:
- The the account provider task has two subject-only originating intake lines and short IDs in the
  steward brief, but no full email text or pod handle in the pasted render. A model
  cannot verify source-specific recovery details from those subjects alone. Inspect
  stored intake/pod linkage before attributing loss to a particular new writer.
- An attempt-2 dispatched task shows a retry finalizer without an attempt label; this
  legacy summary is not explicitly marked as a previous attempt. Preserve the history
  but label unknown/previous attempt provenance rather than implying current judgment.
- Duplicate dependency entries appear for report_club_decision_status--bf45f6; new
  edge validation does not clean historical duplicates.
- Completed-work lines are clipped (including a cooling-setpoint time ending in '6').
  This weakens comparison with pending similar actions; trace recent-completion producer.
- Current terminal REASON and finalizer OUTCOME frequently repeat identical paragraphs.
  Deduplicate identical display text while preserving distinct reasons and judgments.

No runtime or stored-data changes were made during this render review.

## Live context-engine failure — 2026-09-19 21:00:09

### CE1. Chat scan receives no conversation history and passes a pronoun as a KG seed — CONTEXT HANDOFF FIXED locally

Live log the live application log at 21:00:09.056 records should_activate=True with
seeds=['the user', 'he']. Its reason explicitly says the person is only referred to by
'he' and suggests retrieval may identify them. At 21:00:09.540 activation cannot
resolve 'he' and aborts the whole background run, including the resolved the user seed.
Source: room_session_manager.py's context-engine trigger passes user_message,
primary_user and owner_id, but omits the supported recent_chat_context parameter.
chat_scan wraps its result without entity-resolution validation, and activation
raises for any unresolved label. This is a broken producer/consumer contract:
entity-name lookup cannot resolve discourse pronouns without their antecedents.
Fix direction: supply relevant preceding conversation, resolve references before
activation, and represent unresolved references explicitly without inventing KG
entities or silently pretending full context was obtained. Keep prompt instructions
in Jinja. Background logging followed by re-raising accounts for duplicate traceback
output; this evidence does not establish two separate executions. The failure belongs
to master_room's context pipeline, not the interleaved Dayflow state_mover run.
No runtime changes were made during live monitoring.

### CE1 repair checkpoint

The user explicitly approved giving the scanner the scoped history already prepared
for the room agent, with the constraint that ordinary chat remain fast. The room
trigger now reuses seeded_chat_messages after reply delivery; no extra history query
is performed. The existing background pipeline still owns all scanner/model and KG
work, and the room never joins it. Scanner inputs are structured variables and all
reference-resolution instructions/formatting live in Jinja. The model is instructed
to resolve pronouns from history and decline activation when identity is ambiguous.
KG lookup still fails visibly for unresolved entity names; no invented entities,
pronoun substitution heuristics, or silent partial activation were added.

Twelve focused tests pass: prepared-history handoff, Jinja rendering, nonblocking
background trigger, and existing room runtime regressions. Model output was mocked;
this verifies plumbing and latency isolation, not a live-model pronoun-resolution
success rate. Requires application restart for the Python caller change; no restart,
commit, push, database migration or outbound action performed by this repair.

## State-mover timing repair — 2026-09-19

### DF30. Background work held as an interruption; evening holds moved into bedtime — FIXED locally, model behavior needs live verification

Live logs: at 21:40 state mover held research_coinbase_recovery until 22:15 because
research would supposedly interrupt the user's evening. At 21:35 it moved the
school-update question to 22:15 to avoid wind-down. Another hold used the already-ended
21:00–21:30 dog walk to defer a 21:34 question until 22:00. The old prompt both restricted
holds to interruptions and prohibited next-day holds, encouraging an unsuitable same-night
retry. Both prep paths supplied only a title-first 140-character context, omitting the
full directive needed to distinguish research from user-facing work.

Repair: Jinja explicitly permits background research and scheduled device actions during
quiet/away periods; holds require an actual interruption conflict. Resume times must
resolve the conflict and avoid sleep, allowing the next known available day when needed.
Both tick and wake prep now use one structured candidate with full directive, goal,
status, due gate and prior finalizer judgment. No Python prompt prose or classification
heuristic added. Existing stored wake times were not changed and no process restarted.

Publication check: newly added prompt examples use synthetic data; live personal names and work identifiers in these follow-up notes have been generalized.


### DF31. Durable evaluator inbox replays legacy admitted email history as new intake — FIXED locally, restart required

The durable-inbox repair in 7aacd502 introduced an unbounded compatibility predicate
in state_store.load_admitted_intake: state=artifact AND (evaluator_pending=1 OR
state_reason=triage_admit). Historical artifact rows retain triage_admit even though
that legacy marker does not establish a pending handoff under the new contract.
StrategicPlannerWoPrepNode merges all these rows into admitted_artifacts on each tick.
The strategic_planner_wo user Jinja labels them NEW INTAKE and omits source dates.

Read-only live verification on 2026-09-19 found an early-August family email still
marked artifact/triage_admit with both creation and last-review dates in August,
rendered in September planner context. There are 296 pre-September email rows eligible
under this predicate, reaching back to April 9. This is stored-history replay, not
proof of a new Gmail delivery. The normal email loader's today filter and
get_dayflow_items freshness filter do not govern this separate durable inbox path.

Intended: explicitly pending admissions survive restarts until handoff; legacy
context artifacts must not become fresh requests merely because they were once
admitted. Prompts must expose source dates so historical relative deadlines are
interpreted correctly. Consequences: stale requests can create duplicate or obsolete
work and old urgent wording can be interpreted as current. Repair must distinguish
new pending admissions from legacy history while preserving genuinely pending work;
do not blindly expire all pending work. No stored data or runtime code changed during
this diagnosis. Status: open. This refines the DF9/DF20 durable-inbox repair limits.


DF31 repair: removed the legacy triage_admit fallback from load_admitted_intake;
only explicit evaluator_pending=true admissions in artifact state are reloaded.
Read-only verification found zero pre-September email artifacts carrying that explicit
marker, so the 296 legacy emails are excluded without altering stored records.
The steward Jinja now labels the section PENDING INTAKE, renders original source
dates (or an explicit unknown-date label), and anchors relative deadlines to those
dates. Intake triage also renders full source dates rather than time of day alone.
Nine focused tests passed, including exclusion of legacy/cleared/closed admissions,
retention of a 90-day-old explicitly pending admission, restart handoff recovery,
and actual Jinja rendering of historical source dates. git diff --check passed.
Requires restart for the Python query change. Existing work objects were not changed;
this repair prevents replay but does not establish that earlier replay created no work.
