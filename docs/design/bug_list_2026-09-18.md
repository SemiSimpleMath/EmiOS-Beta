# Bug list — found while certifying the architecture docs (2026-09-18)

Everything here was found by reading a file end to end in order to document it, then
checking its callers. **Use each entry's status: later repair passes fixed some findings.** Each entry says what is
wrong, how it was established, and how bad it actually is.

The documentation errors these reads surfaced are already fixed and are not listed here —
this is code only.

**Verification pass, 2026-09-18 (Fable 5.1).** Every entry below was re-checked against the
code with fresh searches, widened to all file types and to agent forms, since an agent's
structured output lands on the blackboard with no `update_state_value` call. Result: 13 of 13
stand. Two were overstated (4a's on-screen window, 4b's consequence), one was understated
(4b has callers that cannot set the block timeout at all), two needed wording corrected
(LIVE 2's callers, ROT 3's "no producers"), and one bug was found that was not on the list
(LIVE 6). Each entry carries a `Verified:` line saying what was re-read.

**Repair pass, 2026-09-18 (Opus 5).** Seven entries FIXED with tests, four HELD for a
decision that is yours, two closed as documentation. Status is marked on each entry. The
dayflow suite went 341 → 346 → 371 across the pass, all additions; the 20 failures
elsewhere are the known playwright/MCP set and fail identically on a clean tree, proven by
stashing.

| status | entries |
|---|---|
| FIXED | LIVE 1, LIVE 2, LIVE 4a, LIVE 4b, LIVE 6, LIVE 7, BANNED 1, BANNED 2 |
| HELD — needs your call | ROT 1, ROT 2, ROT 3 |
| CLOSED as documented | ROT 4, ROT 5, LIVE 5 (rides on ROT 3) |

LIVE 7 was found on 2026-09-18 while acting on LIVE 6, and LIVE 4b's root cause was
resolved by owner decision the same day. Both are recorded below.

Severity is about consequence, not effort:

- **LIVE** — wrong behaviour reachable today.
- **ROT** — dead or unreachable code that misleads a reader or a future refactor.
- **BANNED-PATTERN** — works, but violates a standing rule in this repo.

---

## LIVE

### 1. `/work` reports the wrong database — FIXED (66cd48ec)

Reports `store.path` now. No default on the attribute read: both branches of `_get_store`
construct a `WorkStore`, so a missing `path` would be a real breakage and should surface.

`work_objects/ui/blueprint.py` — `api_work_list` returns `{"db": _WORK_DB}`, the
dev-file constant `work_objects/work.db`. But `_get_store()` **prefers the live dayflow
store** (`emi.db`) and only drops back to the dev file when the app is not importable. So
the UI tells you which database you are looking at, and is wrong about it whenever it is
working normally.

Established: read both functions in the same file.
Verified: `_get_store` lines 48–52 prefer `get_dayflow_work_store()`; line 96 returns `_WORK_DB`.
Consequence: someone debugging real work objects believes they are looking at a scratch DB,
or vice versa. Cheap fix; needs a path accessor on the store.

### 2. The batch item writer performs no transition validation — FIXED (16af8e84)

Existing rows are read-merge-written and a state change goes through `_validate_transition`,
matching the single writer. One SELECT per batch, not per item. Payload validation moved
ahead of the transaction so a bad batch writes none of itself. Five tests, including the
`new → artifact` move triage actually makes and the same-state write the state_mover needs.

`dayflow_orchestrator/dayflow_item_writer.py` — `write_dayflow_item` validates every state
change against `ALLOWED_TRANSITIONS`. `write_dayflow_items_batch` does not: it upserts via
`on_conflict_do_update` and never calls `_validate_transition`. It also defaults new items
to `important_open`.

Three live callers, and they differ in what they risk. `ingestion.py` and
`entertainment_advisor_stage.py` create NEW rows, where there is no prior state to validate
against, so the missing check costs nothing there. **`triage_persist_node._persist_admitted`
updates EXISTING rows** — `new → artifact`, with no check. That transition happens to be
allowed, so nothing is violated today; nothing enforces it either. The `post_room_finalize_node`
calls the earlier version of this entry named are dead code (ROT 3).

A second difference from `write_dayflow_item`: the batch upsert **replaces `metadata_json`
wholesale** rather than read-merge-write, so a caller that passes a partial dict silently drops
every key it did not include.

Established: read both writers.
Verified: searched every `write_dayflow_items_batch(` call in `app/` and `work_objects/`
outside tests; read the entertainment stage's call site to confirm it creates rather than updates.
Consequence: one live update path with no enforcement, and a clobber semantics nobody has
documented. CLAUDE.md says transitions are "enforced by `write_dayflow_item`", which is true and
reads as though it covers all writes.

### 3. Same-status writes skip the transition machine — UNCHANGED, still open by decision

`work_objects/store.py` — the transition check runs only when the target status differs from
the current one, so `dispatched → dispatched` bypasses validation. Already recorded as
"dispatch claim is not a fence" and not approved for fixing.

Worth noting here because **two subsystems now deliberately rely on it**: the state_mover
updates `reactivate_at` with a same-status write, and the evaluator folds originating intake
into a goal's content the same way. A CAS claim op would have to keep those working.

Verified: `store.py` line 498, `if target != node.status:` guards the `TRANSITIONS` lookup; the
terminal-reason check at line 510 is guarded the same way.

### 4a. A reply after the wait-timeout is silently discarded — FIXED (3d068265)

`TicketManager.can_transition` is the read-only half of the state machine, so `respond`
checks before it writes: nothing is saved, nothing is published, and the result carries a
typed `not_answerable` flag. The API answers 409 and the popup stops rolling back the
optimistic removal, showing a notice instead. **It does not recover the answer** — by then
the call has returned "user not reached" and the finalizer has judged the node, so
`result_recorder` would refuse a second outcome regardless. The reply is lost either way;
it is now visibly lost. Whether the tool should expire its ticket at wait-timeout at all is
a lifecycle decision and is HELD.

Found while pinning it: PENDING is displayable but cannot reach ACCEPTED, so a ticket left
in PENDING would be shown and then refuse the answer. Unreachable only because all eleven
`create_ticket` callers happen to call `mark_proposed`, which that function's docstring
calls the caller's responsibility. Pinned by a test, not fixed.

Two of my own assumptions were wrong and the tests caught both: `pending` and `snoozed` do
not accept a reply. Only `proposed` does.

#### Original finding, kept for the reasoning

`ticket_manager.py` — `_ALLOWED_TRANSITIONS[EXPIRED]` is an **empty frozenset**, so `expired`
is fully terminal. `_transition_state_in_session` logs a warning and returns `False`. It does
not raise, and no caller of `mark_accepted` checks the return value in a way the user would
ever see.

The sequence: `_wait_for_ticket_response` times out → `mark_expired(reason="wait_timeout")`
fires immediately. A reply that lands after that attempts `expired → accepted`, is refused, and
`result_for_ticket` reports "user not reached".

**Corrected on verification — the on-screen window is much smaller than first filed.** The web
popup polls `/api/tickets/pending`, which lists `pending`/`proposed` only, and the tool publishes
`proactive_suggestion_update` on timeout, which triggers an immediate re-fetch. So on the web
UI the ticket disappears on the next refresh, not at `valid_until`. The exposure is the race
between the timeout and that refresh, plus any surface that does not re-render (a Slack or
Telegram message stays in the channel with its buttons).

**Two things the first filing missed make the drop uglier than a race.** `TicketService.respond`
writes `user_text` and `user_action` onto the ticket row *before* attempting the transition, so
the late answer **is persisted, on an expired ticket, and never read** — `result_for_ticket`
only consults `user_text` when the state is a responded one. And the refusal is invisible:
`respond` returns `success=False` with no `error`, the API turns that into `200 {"success":
false}`, and `proactive_popup.js` never checks the flag.

The dayflow lane's own window is consistent by arithmetic: the arguments node passes
`valid_hours = ASK_WINDOW_HOURS` and `wait_timeout_seconds = ASK_WINDOW_HOURS * 3600`, so the
block and the validity window end together.

Established: read `TicketState`, `_ALLOWED_TRANSITIONS`, `_transition_state_in_session`,
`mark_expired`, and the tool's timeout path.
Verified: read `TicketService.respond` in full, the `/api/tickets/respond` and
`/api/tickets/pending` routes, and the popup's `proactive_suggestion_update` handler and
`checkForSuggestions`. Searched for every `.respond(` caller: all three are in `ticket_api.py`.
Fix shape: the tool should not `mark_expired` on wait timeout at all — the ticket's own
`valid_until` already bounds it, and a late reply should land through the same recorder the
boot re-arm uses. Failing that, refuse to expire while `valid_until` is in the future.

### 4b. The ticket tool's two timeout defaults disagree — PARTLY FIXED (c0af0690)

`wait_timeout_seconds` is now in the tool form and the contract, so a routed call can set
it, and the tool logs a warning naming both numbers when the block is shorter than the
validity window. **Defaults and the expire-on-timeout behaviour deliberately unchanged:**
deriving the block from `valid_hours` would make the scheduler-conflict ask block a thread
for 48 hours, which is worse than the bug.

**Root cause RESOLVED (3348a2c9), by decision.** The tool conflated *creating* a ticket with
*waiting* for the answer, and the two fire-and-forget callers got a ten-minute block they
never asked for. The tool now takes `wait`, default true so the dayflow lane is untouched,
and both callers pass false. Owner picked this over the two alternatives.

That block was doing real harm once the `"info"` bug was fixed, because the camera
escalation runs inside an event routine: 600s of blocking tripped the routine watchdog's
120s soft limit, producing a spurious "this routine is stuck" ticket during an emergency,
and then expired the emergency ticket at ten minutes against its four-hour validity.

`lib/tools/create_dayflow_ticket/create_dayflow_ticket.py` — `valid_hours` defaults to 4
(the DB validity window) while `wait_timeout_seconds` defaults to 600 (how long the call
blocks). The dayflow lane is safe: `dayflow_switchboard_arguments_node` passes both from
`ASK_WINDOW_HOURS`, and its comment says this is exactly why.

**Corrected on verification, in both directions.** The consequence is not "on screen for three
and a half hours" — per 4a the ticket vanishes from the web popup on the next refresh. The
real consequence is that **a caller's four-hour question is silently withdrawn after ten
minutes**: the tool expires the ticket at the wait timeout and reports "user not reached",
against a validity the caller explicitly asked for.

And it is worse than "any other caller is exposed": **the tool form has no
`wait_timeout_seconds` field at all** (`tool_forms.py` carries `valid_hours: int = 4` and nothing
for the block), so an LLM-invoked call cannot set it even if it wanted to. Every route that
reaches this tool through a switchboard — master_room's, whose prompt sends "remind me" and
"ask me whether" here, and `entertainment_manager`, which lists it in `allowed_tools` — gets a
600-second ask with a 4-hour label.

Two programmatic callers pass `valid_hours` (camera escalation 4 h, scheduler-conflict ask
48 h) and no block timeout, so they look like the worst cases — but they never reach the
timeout at all. See LIVE 6.

Established: read the tool and the arguments node that compensates for it.
Verified: searched every reference to the tool across `app/`, `configs/` and `work_objects/`;
read `tool_forms.py`; distinguished the callers that invoke the tool's `execute` (which waits
and expires) from the many that call `ticket_manager.create_ticket` directly (which do neither
and are not exposed).

### 5. `created_ticket_id` is recovered by scanning prose — CLOSED, rides on ROT 3

`post_room_finalize_node._persist_action_selector_action_logs` — if `result_payload` has no
`ticket_id`, it searches the result TEXT for the words "ticket" and "Created", then takes the
first whitespace token containing `dayflow_orchestrator_`.

That is a wording-based identity join, which this repo bans outright
(`feedback_no_wording_based_identity`: ids and deterministic joins only). Currently
unreachable because the block is dead (see ROT 3), so it is filed LIVE only in the sense that
it would fire the moment that lane were revived.

Verified: re-read lines 814–824; the block is inside `_persist_action_selector_action_logs`,
whose only caller path depends on `action_result_events`, which has no producer.

### 6. The camera escalation and the conflict ask never created a ticket — FIXED (072c2176)

`notify` for the camera event, `decision` for the conflict. The camera success test now
reads `result_type` instead of looking for an `error_code` field that error results never
carry. Pinned by an AST walk over both modules plus two tests of the success path.

`ring_analysis/camera_dispatcher.py` (line 545) and `subconscious/scheduler_arbiter_persist.py`
(line 220) both call the ticket tool's `execute` with **`ticket_kind: "info"`**. The tool's
`_ui_policy_for_ticket_kind` accepts exactly `notify`, `decision`, `advice` and **raises** on
anything else — and it runs *before* `create_ticket`. The raise is caught by `execute`'s outer
`except`, which returns an error `ToolResult`. So the high-stakes door-event escalation and the
"the arbiter punted, ask the user" ticket both fail every time, with an ERROR log line and no
ticket.

`camera_dispatcher` then makes it invisible: its success test is
`result_type in {...} or (isinstance(data, dict) and not data.get("error_code"))`. The error
result carries `data={}`, an empty dict with no `error_code`, so the second clause is **true**
and the function returns `True`. The caller believes the escalation went out.

**Latent, not yet fired.** Searched all 4,317 retained log files for `Got: 'info'` and for any
`camera_dispatcher` / `arbiter_persist` ticket line: zero. Neither path has executed in any
retained log. It will fail the first time a significant door event or a scheduling conflict
occurs.

Established: read both call sites, the tool's `execute` up to the policy check, and
`_ui_policy_for_ticket_kind`.
Verified: the tool form declares `ticket_kind: str` with no `Literal`, so nothing upstream
rejects `"info"` either.
Fix shape: both callers should pass `notify` (a camera event) or `decision` (a conflict needing
the user's call), and `camera_dispatcher`'s success test should check `result_type == "error"`
rather than the absence of a field that error results never carry.

### 7. No front-door camera pod has ever reached dayflow — FIXED (3348a2c9)

Dayflow ingests pods by an allowlist of `(kind, source_kind)` pairs, in the dayflow room's
`ROOM.md` frontmatter under `access.ingestion_pod_kinds`. It asks for
`ring_doorbell_significant`. **No camera on this machine produces that name.** The front
door mints `ring_doorbell_event`, Downstairs mints `ring_downstairs_notable`; only the
Bedroom's `ring_bedroom_notable` is in the list. So every front-door pod has been minted
and then silently ignored by dayflow.

Found while implementing the owner's chosen escalation route for LIVE 6, which was "write it
to a pod and let the orchestrator get it" — a plan that would have quietly done nothing.

Fixed with `pod_policy.source_kind_by_category`: the front door mints the everyday kind
normally and `ring_doorbell_significant` for `emergency`, so the escalating category enters
dayflow intake and ordinary events stay out. One pod under the right kind, rather than a
second pod for the same frame. Allowlisting the everyday kind instead would have made every
passing delivery dayflow intake at one triage LLM call each.

Tests parse BOTH sides and assert they agree, so renaming one without the other fails.

**Still open, deliberately:** the Downstairs camera's pods reach dayflow no more than before.
Changing what a second camera feeds into the working set is a behaviour decision.

### Historical, resolved — recorded so it is not re-filed

The enum-stringification rejection (`Got: 'ticketkind.notify'`) appears **hundreds of times**
across seven processes in the logs, sometimes three identical failures in a row on one work
node. All seven processes started before commit 42411b30 landed the `.value` unwrap at 22:54 on
2026-09-16 — including process 10104, whose log is dated the 17th because it kept running, and
which is a test harness besides. The live process (started 2026-09-18 16:45) shows two
successful ticket waits and zero rejections. The fix works; the failures are pre-fix. Those
repeated identical results are, incidentally, exactly the pattern `_render_existing_graph`'s
"ALL IDENTICAL" warning was built to expose.

---

## ROT

### 1. `payload.session_id` is written in three places and read by nothing — HELD

Not touched. The entry itself says a decision is needed and it is not mine to make:
restoring a reader and dropping the writes are opposite repairs, and the rewrite notes show
the reader was planned, not forgotten. Entangled with ROT 2 — if a reader comes back, those
helpers become useful again, so removing them first would be the wrong order.

Written by `work_session.open_session`, by `discharge_node` when passed a session id, and
inherited by every child in `store.add_node`. **No reader anywhere.**

The supervisor it exists for, `sweep_stuck_work_nodes`, was rewritten to a pure subtree-idle
rule and consults no session at all. Two docstrings still asserted that "ownership is a graph
fact the supervisor reads"; those are now corrected, but the writes remain.

Established: searched every reference in `app/` and `work_objects/`.
Verified: a widened search across the orchestrator, control nodes, tools, core tools, dayflow
pipelines and `work_objects/` (py/j2/yaml/json) finds the field read in exactly two places, and
both are the writers deciding what to write — `discharge.py` line 81 compares before stamping,
`store.py` line 455 reads a parent's value to inherit it. No consumer exists. A whole-repo
sweep, every file type, found nothing further: the other hits are unrelated session concepts
(doc, plan, geoguessr and task-creation sessions), a stale agent worktree copy of those files,
and recorded tool-result payloads that only show the field is written in production.

Provenance for the decision: the rewrite's own notes, `scratch/WORK_SESSION_REWRITE_PLAN.md`
and `scratch/WS_REWRITE_PROGRESS.md`, name the intended reader — "orphan = dispatched node
whose payload.session_id [is] not a live session", the sweeper re-key listed as step 5. That
reader was replaced by the subtree-idle rule, and the stamping was never taken out with it.
Decision needed: either restore a reader or drop the writes. Right now it is a durable field
that looks load-bearing.

### 2. Four orphaned session helpers — HELD with ROT 1

Three have zero callers and `session_alive` is used by `test_job_thread_dispatch`, so
deleting it would break a passing test that documents real behaviour. Held as one decision
with ROT 1 rather than deleted piecemeal.

- `dispatch_sweeper._session_root_dispatched` — **zero callers**, and it belonged to the
  orphaned/frozen supervision that no longer exists.
- `work_session.session_alive` — no production callers (tests only).
- `work_session.session_started_at` / `session_started_at_by_id` — no callers at all.

Only `session_alive_by_id` is live, used by the boot-time ask re-arm.

Verified: whole-repo search for the three names, all file types, excluding `.venv` and
archives. `_session_root_dispatched`: definition only. `session_started_at` (both forms):
definitions only. `session_alive(`: definition plus tests.

### 3. `post_room_finalize_node` is roughly 600 lines with no live producers — HELD

Not deleted. This is ~600 lines in the node every dayflow path exits through, it has its own
test file, and the entry says two ideas must be preserved deliberately rather than lost in
the deletion. It also needs the legacy `strategic_planner` agent form retired in the same
change or the whole block can silently come back. That is a single coordinated change and
your call on scope, not something to do on the way past.

BANNED 1 was fixed inside this file, because `_format_compact_outcome` is reachable from
master_room and is therefore live even though most of the module is not.

**Wording corrected on verification.** The first filing said the keys had no producers. They
have exactly one, and it is unreachable: the **retired legacy `strategic_planner`'s agent form**
declares `planned_tasks`, `plan_synopses`, `completed_plan_ids` and `closed_task_ids`, and two
`agents/test/*_repro` forms declare `planned_tasks` and `state_mutations`. No manager config
loads any of those agents. The distinction matters: **re-wiring `strategic_planner` would
silently bring this whole block back to life**, so whoever retires it should retire the agent
form in the same change.

| key it reads | producer |
|---|---|
| `planned_tasks` | only the unwired legacy `strategic_planner` form (`state_mover_prep_node` merely enriches a list it reads back) |
| `state_mutations` | only a test repro agent (the live state_mover form has `node_wakes` and `held_work_nodes`, nothing else) |
| `state_mutations_persisted_tf` | none (`state_transition_guard_node` was deleted) |
| `plan_synopses`, `completed_plan_ids`, `closed_task_ids` | only the unwired legacy `strategic_planner` form |
| `action_result_events`, `active_dispatch_records` | none, anywhere |

So `_build_planned_task_messages`, `_persist_action_selector_action_logs`,
`_attach_execution_results`, `_close_completed_plans`, `_cancel_tasks`, the plan-synopsis
diffing, and the auto-synopsis safety net are all unreachable. The `acted_on_item_ids` path
is also inert by design — `work_node_dispatch_node` clears that key before handing off.

Established: searched for `update_state_value` on each key across `app/`.
Verified: re-searched each key with any quoting and any setter idiom across py/j2/yaml, then
listed every field of every live dayflow agent's `agent_form.py` — none of the eight appears —
and confirmed no manager YAML names the legacy planner or the repro agents.
Consequence: this is the single largest block of dead code in the dayflow tick, and it
contains two real ideas worth keeping if the lane is ever revived — the auto-synopsis net for
the "HVAC re-research loop", and the deterministic `changed=false` override. Retiring it
should preserve those deliberately rather than deleting them by accident.

### 4. `register_manager_as_tool` has no callers — CLOSED as documented, NOT deleted

Deliberately kept. Registering a manager as a tool at RUNTIME is exactly the extension hook
the modular initiative's plan doc says the system lacks, so this may be a staged seed rather
than an oversight. Deleting it would remove something on the basis that nothing calls it
yet. Documented as unused in `08_WORK_OBJECTS.md`; delete it on purpose or not at all.

`work_objects/work_tools.py` — it would register a manager-as-tool wrapper at runtime. The two
node managers ship as ordinary static tool directories instead.

Verified: whole-repo search finds the definition and two documentation mentions, nothing else.

### 5. `graph_neighbors` is never exposed — CLOSED as documented

`work_objects/tools.WorkGraphTools.graph_neighbors` exists and works, but there is no
`work_graph_neighbors` entry in `_SPECS`, so no agent can call it. The module docstring lists
it among the read tools. Possibly deliberate; flagged because the docstring implies otherwise.

Verified: the name appears only in `tools.py`; no `work_graph*` static tool directory exists.
Repaired as documentation rather than by exposing a new tool: the module docstring listed it
among the read tools, which implied a `work_graph_neighbors` that does not exist, and both
the docstring and the method now say it is Python-only. Whether an agent should get it is a
capability decision, not a bug.

---

## BANNED-PATTERN

### 1. `_format_compact_outcome` degrades silently instead of failing — FIXED (8dd3f832)

Raises on a missing formatter, a crash, a non-dict answer, or an empty outcome. An empty
`full_result` still returns "" because there is genuinely nothing to summarise. Nothing
depended on the three removed sentences: searched every py, j2 and js file.

`post_room_finalize_node` — on any failure it returns a hedged string:
`"(formatter unavailable; see action_log)"`, `"(formatter errored; see action_log)"`,
`"(formatter returned no data; see action_log)"`. That string is then written into an item's
`execution_result` and read by the planner as though it were an outcome.

The repo's rule is to fail loud, never to substitute a default
(`feedback_no_fallbacks_fail_loud`). The stated intent was to avoid blasting 3KB downstream,
which a raise would also avoid.

Verified: re-read lines 119–180. Note this helper is **live** — `master_room_tool_caller`
imports it — so unlike most of this file the stubs can reach a real item's `execution_result`
on the master_room dispatch path.

### 2. The ticket brief synthesises a ticket on failure — FIXED (8dd3f832)

Raises instead. `execute` turns that into an error ToolResult, the node fails, and the
finalizer judges it — a failed ask the architect can re-plan beats a malformed one the user
has to interpret.

`create_dayflow_ticket._format_brief` — if `ticket_builder_manager` fails, it classifies the
ticket by searching the brief for "ask", "decide", "choice", "confirm", "whether" and takes the
first 60 characters as the title. Both a fallback and a wording-based decision.

Consequence when it fires: the user gets a ticket titled with a truncated fragment, and the
`decision` vs `advice` classification, which decides whether a reply is even possible, is
chosen by substring match.

Verified: re-read lines 224–235.

---

## Historical verification summary (2026-09-18)

Every entry above has been re-checked against the code by a second pass. The last claim
carried on someone else's word — that `EXPIRED` is terminal in the ticket state machine — was
read and confirmed, and then narrowed: the drop is real, but on the web UI its window is one
refresh, not the validity period. See LIVE 4a.


## Shared-runtime follow-up (2026-09-19)

These findings were established during the documentation pass. All six are **OPEN;
no runtime fixes were made in this pass**. Current behavior is now documented;
the stronger guarantees in the old prose are preserved here as proposed repairs.
These are reachable code paths, not claims of observed production incidents.
See [reading coverage and evidence](structure_map_2026-09-19.md).

### R1. Routing validation admits names that cannot be activated — PARTLY FIXED locally; mandatory-signal design remains

Intended guarantee: construction proves route targets exist and return-control
signals have routes. Actual: `MultiAgentManager._validate_strict_routing_config`
unions role-binding keys AND values into `routable`, without checking that targets
were loaded. It checks prefixes only for return-control keys already supplied.
`AgentLoader` loads only the declared agent/node entries; `resolve_role_binding`
performs one lookup and `_run_loop` then asks the registry for that instance.
A missing instance or edge is therefore still a runtime failure. Optional
`tool_return`, `critic`, and `summary` blocks skip subsection checks when non-dicts.

Evidence: full reads of manager, loader, Delegator, factory and agent validator;
an isolated execution of the actual validation method accepted
`role_bindings={alias: missing}` with `state_map={planner: alias}` and no
`planner_return_control` edge. Boot's unused-agent check is a reference scan,
not graph reachability. Proposed repair: validate resolved targets against loaded
instances and distinguish mandatory signal edges from optional ones.

### R2. Default error exit is labeled final_answer — FIXED locally

Intended guarantee: aborts are distinguishable from completions by result type.
Actual: `handle_default_error_exit` returns `final_answer`, error prose, and
`data_list=[{}]`, without structured abort data. It is reached when graceful-exit
handling cannot finish. Cancellation and a successful graceful abort report do
use `manager_aborted`.

Evidence: full manager and exit-node reads; isolated execution of the actual
fallback method confirms its type and missing structured abort payload. Proposed
repair: give every failed exit a consistent abort envelope and check consumers
before changing the result contract.

### R3. Mailbox queues can survive an ended invocation — FIXED locally

Intended guarantee in the comments: unregister and TTL prevent lingering queues.
Actual: unregister clears the queue once. `Mailbox.post` does not check liveness,
so a later post recreates it. TTL is checked only by `drain`; `peek_count` does not
prune and there is no expiry worker in this module. An ended id will normally
never be drained again.

Evidence: full mailbox, registry and invoker reads; isolated execution of the real
Mailbox class confirms post-after-clear recreation and TTL enforcement only on
drain. Proposed repair: make queue closure/posting coordinate with invocation
lifecycle, or add explicit expiry reclamation. Preserve legitimate steering
posted while long synchronous tool calls are running.

### R4. Tool visibility filtering is weaker than its comments promised — FIXED locally

Intended guarantees: the narrower sees only permitted candidates, and visible
means executable under the task's restrictions. Actual in
`ToolScopeService.initialize_scope`:

- `ranked_for_narrower` is copied before scope filtering; `_run_narrower` receives
  that copy. The final visible output IS filtered by scope afterwards, so this
  finding does not establish unauthorized tool execution.
- The ranked path filters scope allow/block and authority, but does not separately
  apply `task_allowed_tools` / `task_except_tools`. ScopeAdapter projects those
  restrictions into request data; ToolCaller checks them at execution. A tool can
  therefore be shown and subsequently refused. A non-empty precomputed visible
  list takes a different path through ingress filtering.

Evidence: full scope adapter, visibility service, manager and ToolCaller reads.
Proposed repair: apply the effective task/scope policy to candidate input and final
visibility, preserving the distinction between relevance ranking and permission.

### R5. Mixed final-answer forms lose sibling domain fields — FIXED for matching terminal envelopes; limitations below

Intended guarantee in the developer recipe: a form containing both `outcome` and
`final_answer_*` returns the domain outcome to programmatic consumers. Actual:
FinalAnswerNode and ManagerExitNode prefer their constructed `final_fields` dict
when an envelope field is populated. Carry-through adds summary/pod references,
but arbitrary siblings such as `outcome` and `revision_log_id` are not copied.
`final_answer_raw` stores that selected payload, so it does not restore siblings.
ManagerExitNode's terminal-agent capture only runs when final fields are absent.

Evidence: full reads of both exit nodes, FinalAnswerNormalizer, manager exit
handling, and the example `kg_mutation/final_answer/agent_form.py`. Proposed
repair: retain the winning agent's full structured output separately from the
human envelope, with tests for mixed forms and stale earlier-agent results.
Current documentation tells callers to verify the returned shape and use explicit
final-answer data items or a domain-only terminal form when needed.

### R6. Budget checks can override completion at the boundary — FIXED locally

Intended behavior to review: a successful last permitted activation should be able
to finish. Actual `_run_loop` checks cancellation, agent budget, iteration cap,
then `exit`, then `error`. If the last budgeted agent sets `exit=True`, the next
boundary returns `max_cycles` before checking it. A control node setting exit on
the final permitted iteration encounters the same ordering. Cancellation priority
may be deliberate; budget-versus-completion priority needs an explicit decision.

Evidence: full manager loop read. No production occurrence established and no
behavior change made. Proposed repair: define precedence and cover completion
exactly at each budget boundary before reordering the checks.


## Work-object follow-up (2026-09-19)

The original audit corrected documentation only; later repair statuses are marked per entry. Stronger
promises from the old comments are preserved here for later implementation work.
These are code-path findings, not claims of observed production incidents. See
[coverage and checks](structure_map_work_objects_2026-09-19.md). The non-exclusive
same-status dispatch claim is already LIVE 3 above and is not a new finding.

### WO1. Result recording is only partly epoch-fenced — FIXED for result commits; broader worker-write fencing remains

Intended: a stale result cannot alter its successor, and completion always has its
evidence. Actual `record_tool_result` loads once, may dispatch-hop and attach a pod,
then fences `set_status`, then appends evidence in another transaction. A stale result
can attach its pod before being rejected; a crash after status commit leaves no result
evidence, and later retries skip ended nodes. Concurrent same-epoch recorders can also
pass the initial open check and append duplicate results. The method catches every
ValueError from the status write, not just stale epochs; False does not imply no writes.

Evidence: full recorder/store reads and an isolated execution of the actual recorder
against an in-memory WorkStore: expected epoch 0 versus current 1 returned False but
persisted the stale pod. No app bootstrap or production DB was used. Proposed repair:
a single store operation that checks incarnation and eligible state, then atomically
writes pod, outcome and evidence, with an explicit duplicate-result policy.

### WO2. The audit log cannot rebuild graph state — OPEN

Intended: events are authoritative and the graph is replayable. Actual `apply` logs
original input data; generated goal/node/edge/action IDs and implicit cascade results
are not fully captured. There is no replay method in WorkStore. Boot migrations and
closure repair also write graph state outside ordinary mutation events.

Evidence: full model/store/accessor reads; in-memory creation confirms its generated
goal ID is absent from event data. Proposed repair: choose a complete deterministic
event contract and replay verification, or explicitly adopt graph state plus audit log
as the permanent persistence design. Current docs describe the latter behavior.

### WO3. Graph validation does not enforce the promised contract — FIXED for new writes; historical validation remains

Intended: acyclic dependency graph, inherited authority ceilings and validated lifecycle
entry. Actual validation checks ownership cycles and edge endpoints, but permits
self/duplicate/cyclic dependencies. `add_node` compares explicit authority only against
an explicit immediate-parent value; a parent with None bypasses an ancestor ceiling.
Initial status strings bypass transition and reason checks. Budget/deadline/approval
fields are metadata in this writer, and actor/licensed are caller-supplied assertions.
This finding does not establish a bypass of the separate tool execution scope gates.

Evidence: full model/store/tools reads; in-memory probes accepted a dependency cycle,
authority 99 below an authority-1 ancestor through a None parent, and a newly created
closed node without a reason. Proposed repair: define creation validation, effective
inherited authority, dependency constraints and the trusted writer boundary before
adding enforcement. Preserve legitimate same-status metadata updates (LIVE 3).

### WO4. Closure can disagree with the goal and miss ask-only repairs — PARTLY FIXED; historical repair held

Intended: container closure has a consistent goal state and repairs all closure zombies.
Actual `_rollup` changes only a dispatched goal to done before cascading startable nodes.
A proposed goal with satisfied children becomes abandoned while its container and final
epitaph say done. `repair_terminal_zombies` selects terminal containers with startable
nodes, omitting a container whose only remaining in-flight node is a user_reply ask,
even though the cascade helper would abandon that ask if invoked.

Evidence: full model/store reads; an in-memory object with a proposed goal and one
born-closed child produced container done / goal abandoned. Repair selection verified
by its SQL and cascade predicate. Proposed repair: define goal mirroring for all closure
entry points and align repair selection with the cascade's complete predicate.

### WO5. Legacy standalone driver no longer meets the dispatch contract — FIXED locally

Intended: `drive_work` advances a standalone graph to completion. Actual automatic
branch selects ready nodes but calls `discharge_node` without first claiming them;
that callee requires dispatched and raises. The explicit-node branch also requires a
preclaim, and neither branch invokes finalization. Evidence: full discharge module
read, including both caller and callee. Proposed repair: use the standard dispatch /
record / finalize lifecycle for scenarios, or retire the obsolete automatic helper.

### WO6. UI object-abandon action always lacks the required reason — FIXED locally

Repair: the endpoint supplies "owner abandoned via /work". A Flask route regression
checks HTTP 200, the persisted reason and the unstarted-child cascade. Running threads
are still not cancelled by this endpoint. Not yet published.

Intended: the manual editor can abandon an object through the validated writer.
Actual `api_work_abandon` sends only work_id and status; `_op_set_work_status` requires
a nonempty reason. `_apply` converts the resulting ValueError to HTTP 400. Evidence:
full blueprint/store reads and an in-memory execution of the same store request.
Proposed repair: supply an explicit user/manual-action reason. Do not imply graph
closure cancels a running worker or ticket; that requires a separate lifecycle policy.

### WO7. Store setup can conceal live-store failures — FIXED locally

Intended: UI dev fallback is for an unavailable app, and successful initialization
means migrations succeeded. Actual `_get_store` catches any live-store exception and
opens WORK_DB; the accessor's two migrations log and swallow errors before the singleton
may be cached. This can show a different DB or continue with unmigrated rows.

Evidence: full UI/accessor reads. Proposed repair: limit dev fallback to an explicit
standalone mode and define which migration errors must block initialization. Preserve
`store.path` reporting (LIVE 1 was already fixed).

### WO8. Wake metadata does not guarantee a real readiness gate — FIXED locally

Intended: deferred nodes carry a usable revisit condition. Actual `defer_node` validates
wake-kind vocabulary but not required time/reference combinations. Generic `is_ready`
checks status, future wake_at and dependencies only; a waiting event/signal reference
does not itself keep a node unready. Higher-level matching must supply that policy.

Evidence: full model/store/tool reads; an in-memory node with an unfulfilled event
reference and no future timestamp was ready. Proposed repair: define the division
between generic readiness and wake matching, validate required wake fields, and audit
callers before changing the predicate. No claim that this probe caused a production
redispatch is made.


### WO9. Invalid deferred timestamps can make a graph unreadable — FIXED locally

Repair: defer_node validates ISO strings/datetime values before assignment, accepts
null to clear the wake, and treats naive timestamps as UTC, matching persistence
conventions. Regressions assert malformed input leaves both graph and events unchanged,
and cover UTC/offset/naive round trips and clearing. This prevents new invalid writes;
it does not repair existing malformed rows. No schema migration. Not yet published.

`_op_defer_node` assigns `data["wake_at"]` directly onto an existing WorkNode.
Assignment is not Pydantic-validated; structural validation does not check timestamps,
and `_iso` persists an invalid string as supplied. A subsequent `WorkStore.load`
reconstructs the Pydantic model and raises ValidationError. One malformed defer can
therefore commit successfully and prevent normal reads and later apply calls for that
work object. This is stronger than the missing wake-field checks in WO8.

Evidence: model/store reads and an isolated in-memory probe: create an object, defer
its goal with wake_kind=time and wake_at=not-a-date; apply succeeds and the next load
raises ValidationError. No production data was touched. Proposed repair: parse and
validate wake timestamps before mutating/persisting, define timezone normalization,
and reject malformed input without committing an event or graph change.


## End-to-end Dayflow logic review (2026-09-19)

Evidence below is source tracing, not claims of observed production incidents or new
passing tests. All DF findings are OPEN. The full flow, boundaries and repair order are
in `dayflow_end_to_end_review_2026-09-19.md`. Existing findings retain their status.

### Existing findings: additional integration evidence

- **LIVE 3 (held decision):** dispatch's `_claim` is a same-status-capable `set_status`,
  not a compare-and-set claim. `open_session` registers by work/node, not incarnation.
  Do not describe either as exclusive. `work_node_dispatch_node` also accepts a qualified
  model-produced reference without proving membership in the offered candidates.
- **WO1:** normal `DayflowToolCaller.action_handler` ignores the boolean returned by
  `record_tool_result`, then enters `WorkFinalizerNode`. Recovery checks that boolean.
  Finalizer lookup and `_apply` carry no expected epoch/state; a delayed judgment can
  act on a successor (e.g. fail a newly dispatched attempt). Its multi-write retry
  sequence is also interruptible. `node_result` combines evidence from all attempts,
  without epoch selection. `sweep_stuck_work_nodes` and dispatch `_fail_node` likewise
  fail a node from a prior observation without incarnation/state comparison. A fenced
  recorder alone will not repair this lifecycle. Repeated-failure escalation also uses
  the pre-LLM goal count, so concurrent judgments can both miss the threshold.
- **WO8 / WO9:** the WO9 store validation repair remains valid, but architect `_to_dt`
  converts invalid timestamps to None before the store sees them, removing the time gate
  (DF6). This is a distinct caller defect, not proof that end-to-end deferred execution
  is fixed. State mover `_apply_node_wakes` accepts refs outside its offered waiting set,
  includes user_reply, and lacks a status/epoch guard; clearing a dispatched wait and
  restoring dispatched with evidence can create another epoch. `_park_held` retains
  user_reply on legacy asks, while the precise scheduler arms only time wakes.

### DF1. Restart recovery does not match the normal ticket producer — FIXED locally (2026-09-19; limitations below)

`work_session.re_arm_inflight_asks` queries suggestion_type=work_notify, within 48 hours
and 200 rows. Normal `DayflowSwitchboardArgumentsNode` supplies a brief, and
`CreateDayflowTicketTool._format_brief` obtains a free category from ticket_builder
(default general); the composer form describes logistics/wellness/scheduling categories.
No deterministic work_notify assignment connects these paths. Normal asks can therefore
be invisible to restart recovery, leaving dispatched nodes for the inactivity sweeper.
The new path also does not stamp wake_kind=user_reply or record the ticket as a work
object action, despite ask-cascade/action-history documentation relying on those fields.
Repair the producer/consumer contract using durable call identity, not model category.

### DF2. Ticket construction drops its graph context — FIXED locally (2026-09-19; limitations below)

`CreateDayflowTicketTool.execute` calls `_format_brief(args["ticket_brief"])`, although
that helper accepts work_node_ref and uses it to give ticket_builder the graph reference.
The dispatch arguments contain trigger_context.work_node but do not forward it here.
A delivery node saying to tell the user a previous step's result can reach the composer
without that result or its lookup reference. Optional research links do not cover general
worker results. Pass the canonical node context through the actual call boundary.

### DF3. Worker provenance exposed as schedulable work — FIXED locally

Repair: `is_work_unit` consistently limits orchestration to direct task children of
the goal. Promotion, waits/wakes, dispatch/session/recovery and supervision exclude
provenance, including previously actionable records. Architect references cannot name
provenance. Worker/finalizer views include full owned history; strategic views show
finalizer summaries. UI labels provenance explicitly. No schema or historical-row
migration. Eleven focused regressions cover this boundary; the Dayflow suite passes
371 tests after updating superseded expectations. No live rollout or push.

User-confirmed ownership contract: worker-created descendants are internal helper
records for the assigned main node, not independently schedulable work. The architect
and steward must never assign an agent to them. Their persistence in the same nodes
table does not grant the orchestrator execution ownership. Missing parent dependency
edges are not the defect and adding them is not the proposed repair. The defect below
is that candidate generation does not enforce this boundary before offering helpers
to the action selector. Actual duplicate execution still requires a concurrent tick
and selection of an exposed helper; no production occurrence was established here.

`ManagerInterface._run_on_child_node` creates a proposed subtask and runs its manager
synchronously without claiming it. `WorkPlanner._reconcile_to_graph` also creates
proposed checklist children. Both state-mover promotion and materialization iterate
all depths; neither excludes descendants of a running worker. The planning gate does
not lock dispatch threads. During a child call, another tick can promote and dispatch
that same child, duplicating work/side effects; queued checklist work can also run out
of the worker's chosen order. The interface comment that nothing can race is false.
Enforce top-level scheduler ownership and explicit nested execution ownership together.

### DF4. A failed finalizer strands recorded results — FIXED locally (2026-09-19)

`WorkFinalizerNode.action_handler` catches judgment/write failures; an unusable verdict
returns an empty result. A tool result already recorded as done stays done. Boot ask
recovery and the inactivity sweeper select dispatched nodes, and planning has no
finalizer stage. There is no deterministic retry of this unjudged result. A crash
between recording and finalizing has the same consequence. Steward intervention is
not recovery of the missing judgment. Persist/rediscover pending adjudication, and
retry judgment without executing the external tool again.

### DF5. Pending replans do not prevent execution — FIXED locally (2026-09-19)

Finalizer retry writes failed then proposed. Architect handles at most three replan
objects per tick and catches per-object errors. State mover subsequently promotes ready
nodes regardless of pending finalizer instructions. A deferred/failed replan can thus
redispatch the unchanged failed step. achieved_plan_changes closes a node and unblocks
dependents before the pending graph revision is applied. Hold affected execution until
the exact instruction has been successfully applied, including replans arriving during
the planning pass.

### DF6. Architect application can expose an incomplete or ungated plan — FIXED locally (2026-09-19; limitations below)

`apply_architect_dag` commits prunes, additions, edges and wakes separately; its caller
logs errors and continues to promotion. An error after additions can leave runnable
nodes missing dependencies/wakes. Missing dependency IDs are silently skipped.
`_to_dt` logs malformed times and returns None, allowing immediate execution instead
of rejecting the plan. Validate the complete delta first and publish it atomically,
or use an explicit non-dispatchable construction state with recovery.

### DF7. Replan acknowledgement can consume the wrong verdict — FIXED locally (2026-09-19)

`WorkArchitectNode` snapshots pending instructions before its LLM call, then consumes
by work_id/node_id only. `_op_consume_finalizer_instruction` stamps whichever finalizer
payload exists at write time. A new verdict during the call can be acknowledged without
being planned. Separately, refused prunes are swallowed by `apply_architect_dag`, so
its return is not proof that the requested revision succeeded, yet instructions are
consumed. Compare instruction identity/version and acknowledge only applied changes.

### DF8. Deduplicating a node leaves its dependents attached to the discarded copy — FIXED locally (2026-09-19)

`apply_architect_dag` validates a surviving twin and abandons the duplicate, but does
not redirect depends_on edges. `WorkObject.is_ready` requires each named dependency to
be satisfied; an abandoned tool-success subtask is not. A downstream node can remain
blocked after the surviving twin succeeds. Rewire/validate affected dependencies as
part of the same revision. This is distinct from general dependency validation (WO3).

### DF9. Admitted intake has no durable evaluator inbox — FIXED locally

Triage persists ADMIT as artifact. `IntakeTriagePrepNode` offers new items, while the
active evaluator config consumes this tick's admitted_artifacts, not an outstanding
admitted backlog. Failure after admission but before work creation can strand actionable
intake outside later evaluator inputs. Existing artifact display in triage is not a
reliable evaluator handoff. The same gap affects admitted work deliberately left for
later. Keep admitted-unconsumed intake in the evaluator's durable input set.

### DF10. Intake handoff loses source detail at creation/change boundaries — FIXED locally for durable source handoff

`StrategicPlannerWoPersistNode._fold_intake_into_goals` stores source summaries/pod IDs
in goal.content, but catches failures; `_close_consumed_items` closes items anyway.
Initial `WorkArchitectNode` receives created["objective"] plus rationale and a portfolio
built before creation, not the enriched goal. Thus even a successful fold does not
supply the first architect with the promised source handles. Later `persist_steward_output`
changes replace goal.content with objective, removing folded provenance. Preserve source
context separately, pass the stored goal on creation, and close intake only after a
successful durable transfer. Partial creation failure also discards the returned created
list in the persist node, leaving already-created goals without this tick's decomposition.

### DF11. Ticket publication can race reply-listener registration — FIXED locally (2026-09-19; limitations below)

`CreateDayflowTicketTool.execute` publishes the suggestion (and optionally TTS) before
`_wait_for_ticket_response` registers its listener. The normal wait does not re-read
the durable ticket after registration or timeout. A response in the gap can therefore
be recorded in the DB but reported to the graph as timeout. Recovery's extra read
corrects the result only after waiting out the timeout. Register/reconcile against the
stored ticket so an already-arrived answer completes promptly. This is separate from
LIVE 4a's already-fixed handling of a genuinely late reply.

### DF12. Recovery tickets are not bound to dispatch incarnation — FIXED locally (2026-09-19; limitations below)

`re_arm_inflight_asks` chooses the newest ticket by node reference and records it using
the node's current epoch. The ticket binding contains no captured dispatch epoch. If
an earlier attempt has a settled ticket and a successor crashes before minting its own,
boot can apply the old answer to the new attempt. Session IDs also omit incarnation.
Persist the ticket/call association and recover only the matching attempt (WO1 related).

### DF13. A hold during a precise wake has no replacement precise wake — FIXED locally (2026-09-19; limitations below)

`DayflowScheduler._fire_work_node` invokes the wake manager and returns. If state mover
parks the node at a new reactivate_at, `_arm_work_node_wakes` is not called there; it
runs after planning ticks only. The consumed one-shot job has no replacement until
another tick, potentially the 30-minute ceiling. Re-arm the new due time after a wake
pass, including failure handling. Stale per-node jobs are not removed when gates change;
callback checks do not establish that the original time gate still applies.

### DF14. Scheduler stop leaves timed execution jobs alive — FIXED locally (2026-09-19; limitations below)

`DayflowScheduler.stop` removes the main JOB_ID only. Per-node jobs installed by
`_arm_work_node_wakes` remain; `_fire_work_node` does not check _started. Calling stop
while the timing engine continues can still invoke a wake manager and dispatch work.
Remove the owned jobs and guard callbacks, including jobs already waiting for the gate.

### DF15. Cadence overwrites newly written watermarks and chat blocking — FIXED locally (2026-09-19; limitations below)

`dayflow_orchestrator_cadence_tick` loads status before ingestion, then persists that old
dict after manager invocation. Ingestion writes chat/pod watermarks into the same resource.
`load_orchestrator_status` returns a copy and `ResourceManager.update_resource` replaces
the value; it does not merge. Successful ticks overwrite their new cursors. Master-room
blocking updates made while the manager runs can be lost too. Use synchronized field
updates rather than a long-lived whole-resource snapshot. Persistence file errors are
also logged/swallowed by ResourceManager, so return does not prove durable status.

### DF16. Ingestion advances cursors before saving intake — FIXED locally (2026-09-19; limitations below)

`ingestion._ingest_chat` and `_ingest_pods` persist their watermarks before
`run_dayflow_ingestion` writes its destination batch. A later source/write failure or
process crash leaves cursors beyond missing intake. The old-snapshot overwrite in DF15
sometimes hides this on successful ticks; it is not recovery after an interrupted tick.
Commit items before advancing, with idempotent restart or a transactional cursor design.

### DF17. Bounded source queries can skip or starve intake — FIXED locally (2026-09-19; limitations below)

Pod ingestion asks `PodStore.query` for 200 newest rows, filters kinds afterwards, then
advances to the newest matching timestamp. Older unprocessed eligible pods can be left
behind the cursor. Chat ingestion takes 500 ascending rows before Python noise/mode
filtering: 500 excluded rows can yield no messages and no cursor progress, repeatedly
hiding later legitimate chat. Timestamp-only `>` pagination can also skip rows sharing
a batch-boundary timestamp; failed row conversion can be skipped once a later row
advances the cursor. Use stable paginated cursors and explicit failure disposition.
DF15's rewind can mask some cursor progression symptoms while causing repeated scans.

### DF18. Delegation ingestion acknowledgement is not repaired after a crash — FIXED locally (2026-09-19; limitations below)

`_ingest_delegation_requests` returns raw requests to acknowledge only when their item
IDs are new. If the batch commits but marking the requests ingested fails, subsequent
runs deduplicate them and never retry that acknowledgement. The request remains pending
until some separate lifecycle cleanup. Reconcile existing destination IDs as successful
handoffs too; do not recreate the work just to acknowledge the request.

### DF19. Short intake IDs can collide after the reset threshold — FIXED locally (2026-09-19; limitations below)

`run_dayflow_ingestion` resets max(existing short_id)+1 to 1 at 10000 without checking
existing occupancy. Subsequent batches can keep resetting while older maximum IDs
remain. Triage and consumption accept short IDs via maps, so two live artifacts can
become ambiguous or the wrong one can be consumed. Use unique stable references or an
allocator that excludes every still-addressable ID. This verifies the collision mechanism
behind historical comments about duplicate short IDs; no production count was measured.

### DF20. Finalizer directive and evaluator success criteria — FIXED locally for directive and criteria delivery

The finalizer now receives full node.content, its success-condition kind and the full
owned provenance history, alongside the returned result. The prompt tells it to summarize
attempts, failures, accomplishments and unresolved work for the strategic agents. The
separate evaluator success_criteria persistence gap below remains OPEN.

`WorkFinalizerNode._judge` sends full result text and node.title. Its task is
`render_work_portfolio`, whose subtask rows use `_t` (title first, capped at 90 chars),
not the node's full content. The goal's full content is present, but the particular
step's detailed constraints are absent. A plausible result can be judged achieved
without checking the actual instruction. Include immutable node content and explicit
success conditions alongside the attempt's result. Evaluator `success_criteria` is also
emitted by WorkObjectSpec but never persisted by `persist_steward_output`; this loses
criteria that were not repeated in objective.

### DF21. Returned manager failures are not treated as failed runs — FIXED locally (2026-09-19; limitations below)

Cadence ignores its manager ToolResult; scheduler health records ok unless an exception
escapes. `_run_dispatch_session` likewise ignores its manager result; an aborted dispatch
before recording can leave the node dispatched until the inactivity sweep. Internal
control nodes often log and continue, further masking incomplete stages. Interpret the
manager's exit contract and report the failed stage; preserve the distinction between
re-running a tool and retrying result/finalizer persistence. See R2/R5 for shared envelope
problems rather than duplicating them here.

### DF22. Queued nodes bypass a fresh readiness check — FIXED locally (2026-09-19)

`WorkNodeMaterializerNode._node_items` tests actionable only, not current dependencies
or time/event gates. Dispatch `_claim` does not recheck them either. If a replan adds a
dependency to an already-actionable node, it can still execute before that dependency.
Promotion validated an earlier graph, not the graph being dispatched. Validate readiness
and eligible ownership atomically with the actual claim (LIVE 3/WO8 related).

### Additional concerns retained for follow-up — SUSPECTED / scope limited

- Scheduler setup-incomplete/no-history rearming gap confirmed and FIXED locally;
  see DF41 below. External events could restart it, so this was not necessarily permanent.
- `_situational_context` and state-mover ticket templates omit acknowledged responses;
  evaluator uses the complete flattened list. Recent-completed reply extraction filters
  created_by=reply, whereas current recorders use the tool name or ask. These are confirmed
  context omissions, but the practical loss depends on the other projections supplied.
- `result_for_ticket` treats snoozed as live, while a snooze response event ends the normal
  wait. Confirm the intended snooze semantics before choosing the canonical result.
- Inactivity sweep fails without finalizer judgment/instructions. The architect's prune
  license comes from finalizer or steward-classified user direction; trace the desired
  automatic timeout repair policy before broadening its authority.
- Automatic goal rollup concern-feedback gap: confirmed and fixed locally in DF40 below.
- WorkPlanner's status projection can become inconsistent after scheduler promotion:
  `_transition_subtask` does not handle actionable when resuming/completing checklist
  children. Address with DF3 rather than adding permissive transitions in isolation.


### DF23. Internal failures inflated the main goal failure count — FIXED locally

`_count_unmet_attempt` previously counted every non-goal record entering failed. A
worker's internal unsuccessful attempt could therefore increase goal_unmet_attempts
and trigger an ask-user escalation even though its assigned main task had not failed.
The counter now counts direct main tasks only; helper-local failure_count still records
the internal attempt. A focused regression reproduced the inflated count before repair.
Existing stored counts are not rewritten. This was found while implementing DF3.


### DF24. Failure counters are driven by status transitions, not finalizer judgment — FIXED (2026-09-19)

User clarification: the finalizer owns incrementing a main node's failure count.
Actual `_op_set_status` increments payload.failure_count whenever a record enters
failed, regardless of actor; `_count_unmet_attempt` also increments the goal count for
main tasks. Tool recording, dispatch errors and sweeper failures can therefore count
before adjudication. WorkFinalizerNode._apply explicitly assumes an already-failed
node was already counted, and adds no judgment count in that case. A later achieved
verdict does not undo the premature failure count. The DF23 repair excludes internal
helpers from the goal tally but does not implement finalizer ownership of these counts.
Repair should count a not-achieved finalizer judgment once per dispatch attempt, with
incarnation/idempotency protection (WO1); execution errors remain result evidence.
Evidence: store.py `_op_set_status` and `_count_unmet_attempt`, and finalizer `_apply`
source reads. No repair or historical counter rewrite in this clarification pass.


### DF25. Failed worker-context refresh retains stale decision input — FIXED locally (2026-09-19)

WorkPlanner.construct_prompt refreshes work_projection because resume routing can skip
WorkObjectRenderNode. Its `_refresh_work_projection` logs a store/render error but neither
clears the old projection nor stops prompt construction. A worker can therefore make its
next decision using previous graph state after the fresh read failed. Required context
checks alone would not catch this because the stale value is nonempty. Preparation must
fail the decision explicitly when required graph context cannot be refreshed. Evidence:
WorkPlanner.construct_prompt/_refresh_work_projection source. No live incident measured.

### DF26. Dormant WorkFinalAnswer contradicts the current judgment contract — DORMANT

WorkFinalAnswer's docstring calls it the finalizer; `_write_node_termination` can overwrite
the assigned node's content with result_summary and set a terminal status. This conflicts
with the immutable directive and separate recorder/WorkFinalizerNode design. Its refresh
also retains old work_projection on failure. No current agent YAML selects this class,
and no other Python reference was found beyond the render-node comment: this is dormant
code/documentation risk, not an established defect in the active finalizer path. Do not
reuse it as the implementation for prompt standardization. Explicitly retire or reconcile
it in a separate cleanup; no runtime change made during this assessment.


#### 2026-09-19 repair verification: DF24 and partial WO1

`record_result` now commits its epoch check, evidence, pod reference and result status
in one store transaction. Stale or duplicate results are no-ops at the caller boundary;
unexpected storage errors propagate. Three isolated checks cover stale results, duplicate
results and rollback after an injected persistence failure.

`finalize_task` now counts unmet outcomes and commits judgment/status atomically, once
per dispatch epoch. Execution status changes no longer count failures. Five checks cover
failed calls, achieved judgments after errors, stale/duplicate judgments and escalation.
Historical counters have not been rewritten. WO1 remains open for cross-connection
serialization, exclusive claims and outstanding recovery callers.

Additional confirmed defect found during this repair: the previous finalizer attempted
`failed -> closed`, which the generic transition table rejected. A tool error could never
receive an achieved verdict. The judgment transaction now performs the result-to-close
transition atomically; the achieved-after-error regression covers it.

Projection verification found and corrected a draft-template regression: obsolete wake
metadata on abandoned/superseded tasks must not render as an active wait. Strategic
renderers now share Jinja and have a focused terminal-wake check.


#### 2026-09-19 continued repair verification

- **WO1:** a two-connection regression reproduced graph mutation from a stale read.
  `WorkStore.apply` now executes `BEGIN IMMEDIATE` before loading. The regression and
  atomic-result/finalizer checks pass. Remaining epoch handoff and timeout writers
  still require review; this entry is not a claim that all WO1 paths are repaired.
- **DF4:** cadence now resumes saved, unjudged main-task results through the actual
  `WorkFinalizerNode`, without rerunning tools. It skips live sessions, fences the
  dispatch epoch and leaves already-judged legacy records alone. Three isolated
  recovery checks pass; recovered asks use the same finalization helper.
- **DF5 / DF22 / LIVE 3:** `claim_task` is exclusive and checks current readiness in
  the store transaction. Pending finalizer revision instructions gate main tasks.
  The materializer checks current dependencies and external/time gates too. A losing
  claimant cannot fail the current owner's call. Six before/after regressions and
  the existing dispatch/thread tests pass.
- **DF6:** node creation, pruning, dependency writes and wakes now commit in one
  graph batch. Invalid wake timestamps and missing dependencies raise; refused
  pruning aborts the revision. Snapshot version checks reject stale revisions.
  General dependency-cycle validation remains in WO3; do not infer it is fixed.
- **DF7:** exact finalizer instruction comparison prevents acknowledging a newer
  judgment. Acknowledgement and graph changes now commit in the same batch.
  Successful and stale-instruction rollback checks pass.
- **DF8:** deduplication transfers both prerequisites and consumers to the retained
  main task in the same revision, rejecting cyclic merges. The previously failing
  dependency-transfer check passes.
- **DF25:** failed worker-context refresh clears the stale projection and raises;
  the render pre-node also stops instead of supplying fallback instructions.
  A before/after fake-store regression verifies that the decision cannot proceed.
- **DF11:** ticket waiting reconciles the durable row after subscribing, after the
  wait and around expiry. Both reproduced reply-gap/expiry-race checks pass.
  Concurrent ticket transition writes still need the separate review below.
- **DF2:** normal ticket formatting now receives `trigger_context.work_node`.
- **DF10 (partial):** initial architect input now reads the stored enriched goal.
  Durable intake handoff and changed-goal provenance retention remain open.

**New review concern, ticket state concurrency (not yet reproduced):**
`TicketManager._transition_state_in_session` validates an ORM snapshot then assigns
state/history without a compare-and-set on the old state. Separate transactions
may both validate an earlier state and overwrite a concurrent reply/expiry. The
waiter repair above reconciles the actual row; it does not prove the writer race
is impossible. Inspect the session transaction policy and reproduce before repair.

**Prompt inspection artifact:** `docs/design/examples/dayflow_complex_work_prompts.md`
is generated from actual architect/steward templates using synthetic data. The
architect Message is captured through its replan control node with writes and LLM
calls mocked. It covers all main-task statuses, pending finalizer instructions,
full directives, dependency satisfaction and failure counts, and excludes raw
worker provenance. Worker and finalizer use separate full-history Jinja views.


#### 2026-09-19 ticket, timer, intake and graph repair verification

- **DF1 / DF2 / DF12:** tickets carry the claimed dispatch epoch and are bound to the
  task before publication. Restart recovery joins on work/node/epoch across all
  Dayflow tickets, without a category or 48-hour cutoff. Old tickets lacking an
  epoch are deliberately not attached to an unproven attempt; no historical data
  migration was performed. Actual ticket execution and stale-recovery tests pass.
- **DF11:** durable-row reconciliation covers replies before subscription and at
  expiry. The separately suspected concurrent TicketManager writer race remains
  unverified; this fix does not claim to resolve that separate concern.
- **DF21:** returned dispatch aborts and thrown exceptions use the common result
  recorder and finalizer when the attempt has no saved result. Cadence and wake
  managers inspect returned failure results too. Shared-runtime false-success
  labeling remains R2, not covered by this boundary check.
- **DF13 / DF14:** wake passes re-arm precise jobs, obsolete jobs are removed,
  stop removes all owned jobs, and callbacks recheck started state. Schedule writes
  are serialized with stop. Failed wake passes wait at least 120 seconds before
  retrying overdue jobs instead of entering a two-second failure loop. Sixteen
  focused scheduler/wake checks pass. Event subscription lifecycle still needs review.
- **DF15–DF19:** status writers patch only owned fields under a process lock;
  ingestion acknowledges cursors after destination writes; complete inclusive
  source windows avoid truncation and timestamp ties; conversion failures raise;
  delegation acknowledgements retry even when the destination exists; short IDs
  no longer wrap at 10,000. Eight isolated intake checks pass. Complete-window
  scans use memory proportional to the backlog. The item-map's seven-day window
  still needs review for historical short-ID uniqueness and deduplication.
- **WO3:** new dependency writes reject self-links, duplicates and cycles; new
  nodes validate initial lifecycle status and every explicit ancestor authority
  ceiling. These are write checks, not a migration or repair of old graphs.
- **WO4 (partial):** rollup marks a satisfied goal done before abandonment of
  outstanding work. Repair of historical ask-only terminal objects is still open.
- **New confirmed main-task satisfaction bypass:** all_owned_children_done or
  verified_by on a main task could satisfy it without finalizer closure. Main
  assignments now require closed for every success kind; helper completion stays
  internal provenance. Seven graph regressions pass (27 with architect/tool checks).
- **DF9 / DF20 (partial):** admission is durable item metadata and the evaluator
  reloads its pending inbox without age truncation. Creation retains success
  criteria. Source handoff and changed-goal criteria remain under repair.

Latest complete Dayflow run at this checkpoint: 421 passed, two old context-shape
assertions failed because trigger_context now includes dispatch_epoch. Those
assertions were updated to verify the epoch; their focused suite passes. A final
whole-suite run is still required after the remaining repairs.

## Latest repair checkpoint — 2026-09-19

This checkpoint supersedes earlier OPEN/partial descriptions where a repair is listed.
The original behavior and evidence above are retained as audit history, not descriptions
of the current implementation. All repairs remain local and unpublished.

- R1: aliases resolve to configured/loaded instances, optional routing blocks must
  be mappings; removed 25 obsolete tool_selector bindings. Mandatory return-control
  edges for arbitrary dynamic forms are not automatically inferred.
- R2/R6: fallback errors return manager_aborted with structured abort data; completion
  wins over budget boundaries, and terminal control nodes can run after the last agent.
- R3: registration opens mailboxes, unregister closes them, late posts cannot recreate
  them, and peek prunes expired entries. Existing tests now honor registration and
  the already-existing structured, timestamped agent-injection contract.
- R4: narrower input and final visibility both apply task and scope restrictions.
- R5: matching latest terminal envelopes retain sibling domain fields in the raw
  result. Earlier mismatched results are not searched as a fallback. This does not
  prove every possible custom flow with stale final fields and later domain-only output.
- WO1: result status, evidence and pod are atomic with epoch checks; finalizer judgment
  is atomic and counted once. Sweeper checks idle timestamps again inside that commit.
  Timeout results can be finalized despite a still-running old thread. Worker helper
  mutations through WorkContext are not yet fenced by a captured dispatch epoch.
- WO3/WO8: new writes validate dependency cycles, ancestor authority, lifecycle status
  and required wake data; external event/signal gates are unready until explicitly matched.
- WO4: goal completion rolls up consistently. Historical ask-only terminal-container
  repair remains held because it rewrites stored data; no such migration was performed.
- WO7: UI/store initialization failures propagate; no silent alternate database.
- DF9/DF10/DF20: durable admitted inbox survives restart; full source content and pod
  handles are stored before intake closes, including changed-goal criteria. Reconciliation
  closes already-transferred intake, and empty graphs recover decomposition from stored goals.
- DF11: the previously suspected ticket writer race was reproduced with SQLite. A stale
  detached reply snapshot could resurrect an expired ticket. Reply fields now commit
  with the state transition under BEGIN IMMEDIATE; no preliminary snapshot merge.
- DF15–DF19: all retained intake IDs now participate in a compact identity index;
  seven-day prompt windows no longer control deduplication or short-ID allocation.
- Session registration rejects a duplicate same-epoch worker, cleans up failed starts,
  and recovery tests liveness for the exact epoch. A legacy fixture was updated to carry it.
- Scheduler restart defect CONFIRMED and FIXED: EventHub rejects duplicate subscriptions;
  repeat startup now reuses its subscription and partial failures roll back registered handlers.
- Prompt projection: actual architect replan messages and steward templates are rendered
  in examples/dayflow_complex_work_prompts.md. Main tasks show statuses, dependency
  satisfaction, wake gates, attempt numbers, judgment-pending markers and full finalizer
  summaries. Worker provenance is excluded from these strategic views. Discharge and
  legacy work-signal prompt formatting were also moved to Jinja.

Verification: 547 tests passed across Dayflow, manager_runtime, manager_classes and
selected ticket suites. After the final scheduler subscription repair, 16 scheduler /
projection checks passed. After moving legacy signal text to Jinja, 20 visibility checks
passed. One requests dependency-version warning persists. These are isolated offline
checks, not fresh-install, upgrade, live-model or whole-application release evidence.

### Remaining findings and decisions

- WO2: audit events are not a replay-complete persistence format; adopting event sourcing
  is a design change. Current graph state remains authoritative.
- WO5: standalone drive_work now uses atomic claim, normal result recording and the
  existing finalizer; see the WO5 repair checkpoint below.
- DF26: dormant WorkFinalAnswer remains unwired and contradicts the current contract.
- DF6 duplicate-mapping validation is now FIXED locally: malformed pairs or invalid
  graph references reject the entire revision before mutation or finalizer acknowledgment.
  See the DF6 duplicate-validation checkpoint below.
- Ticket same-state transition returns success before applying fields. A concurrent second
  same-state reply may be reported as successful without saving its distinct text; needs
  an explicit duplicate-response contract and regression. This is separate from expiry repair.
- MailboxDispatcher incorrectly counted ignored message types as dispatched — FIXED
  locally: count only messages that append at least one runtime injection. The sole
  production caller ignores this return value, so routing was unaffected.
- Complete/abandon failures are now fail-closed with durable pending closure intent; see DF38.
  This does not make the entire evaluator output one transaction.

No historical failure counters or epoch-less tickets were rewritten. No commit or push.

## Scenario review: fridge research and third-party approval — 2026-09-19

### DF27. External-event wake loses reliable source linkage — FIXED locally; semantic matching remains model judgment

StateMoverPrepNode._build_work_object_waits renders only sender, subject and summary
for emails, truncates the candidate intake list to 30, and renders node context from
(title or content) capped at 140 characters. It does not supply a structured source
item/pod ID, message/thread ID or full task directive to that matching interface.
The state_mover therefore judges a prose wake condition from incomplete input and
cannot bind its selected match to an exact durable email through its current output.
For the fridge scenario, an unrelated or ambiguous the approver reply must not authorize the
brother email. Conservative prompt instructions help but are not a structural guarantee.
The 30-item cap can omit a relevant reply; actual starvation depends on source ordering.
Evidence: direct reads of prep method and state_mover system prompt. No live incident
or reproduction test claimed. Preserve source identity and explicit approval content
through matching and worker execution; do not treat any reply as approval.

### DF28. External wake is neither atomic nor separate from task identity — FIXED locally (including evidence and pre-LLM snapshot fence)

StateMoverPersistNode._apply_node_wakes clears the external gate in one store.apply,
then appends model-produced evidence to node.content in another. A crash or failure
between commits leaves the task eligible without its arrival evidence. Evidence is
also mixed into the original directive rather than stored as an attributed event
record. The method checks main-task identity and wake kind, but does not revalidate
that the condition matches the snapshot judged by the model. Evidence: direct source
read. Proposed repair: one version-checked wake operation storing attributed arrival
provenance and clearing the exact gate atomically; render that record through Jinja.
No runtime change made during this scenario discussion.

## Actual architect render review — 2026-09-19 20:45:59

### DF29. Lifetime failure tally emits a fresh escalation after the user already answered — FIXED locally

Observed in the user-supplied work_architect MODEL_USER render: work_example_1
contains the user's explicit direction to continue school notifications, take no extra
action, and accept nonresponse. Its closed ask_direct_school_updates_decision task has
an achieved_plan_changes verdict preserving that direction. Nevertheless the portfolio
header says to ask whether to keep going because goal failure count is three. The graph
also shows a subsequent notification abandoned for that tally and another ask dispatched.
The render establishes contradictory context and repeated asks; it does not alone prove
which earlier decision caused each stored mutation. Source confirms portfolio.j2 emits
imperative escalation for every lifetime count >=2, without checking whether the
escalation was answered. Store finalization likewise compares the accumulated tally on
new failures. Preserve historical counts but represent the current unresolved escalation
separately; resolved instructions must not be reissued by a count-based template.

Additional observed projection gaps, not yet traced to their original producers:
- The the account provider task has two subject-only originating intake lines and short IDs in the
  steward brief, but no full email text or pod handle in the pasted render. A model
  cannot verify source-specific recovery details from those subjects alone. Inspect
  stored intake/pod linkage before attributing loss to a particular new writer.
- An attempt-2 dispatched task shows a retry finalizer without an attempt label; this
  legacy summary is not explicitly marked as a previous attempt. Preserve the history
  but label unknown/previous attempt provenance rather than implying current judgment.
- Duplicate dependency entries appear for report_club_decision_status--bf45f6; new
  edge validation does not clean historical duplicates.
- Completed-work lines are clipped (including a cooling-setpoint time ending in '6').
  This weakens comparison with pending similar actions; trace recent-completion producer.
- Current terminal REASON and finalizer OUTCOME frequently repeat identical paragraphs.
  Deduplicate identical display text while preserving distinct reasons and judgments.

No runtime or stored-data changes were made during this render review.

## Live context-engine failure — 2026-09-19 21:00:09

### CE1. Chat scan receives no conversation history and passes a pronoun as a KG seed — CONTEXT HANDOFF FIXED locally

Live log the live application log at 21:00:09.056 records should_activate=True with
seeds=['the user', 'he']. Its reason explicitly says the person is only referred to by
'he' and suggests retrieval may identify them. At 21:00:09.540 activation cannot
resolve 'he' and aborts the whole background run, including the resolved the user seed.
Source: room_session_manager.py's context-engine trigger passes user_message,
primary_user and owner_id, but omits the supported recent_chat_context parameter.
chat_scan wraps its result without entity-resolution validation, and activation
raises for any unresolved label. This is a broken producer/consumer contract:
entity-name lookup cannot resolve discourse pronouns without their antecedents.
Fix direction: supply relevant preceding conversation, resolve references before
activation, and represent unresolved references explicitly without inventing KG
entities or silently pretending full context was obtained. Keep prompt instructions
in Jinja. Background logging followed by re-raising accounts for duplicate traceback
output; this evidence does not establish two separate executions. The failure belongs
to master_room's context pipeline, not the interleaved Dayflow state_mover run.
No runtime changes were made during live monitoring.

### CE1 repair checkpoint

The user explicitly approved giving the scanner the scoped history already prepared
for the room agent, with the constraint that ordinary chat remain fast. The room
trigger now reuses seeded_chat_messages after reply delivery; no extra history query
is performed. The existing background pipeline still owns all scanner/model and KG
work, and the room never joins it. Scanner inputs are structured variables and all
reference-resolution instructions/formatting live in Jinja. The model is instructed
to resolve pronouns from history and decline activation when identity is ambiguous.
KG lookup still fails visibly for unresolved entity names; no invented entities,
pronoun substitution heuristics, or silent partial activation were added.

Twelve focused tests pass: prepared-history handoff, Jinja rendering, nonblocking
background trigger, and existing room runtime regressions. Model output was mocked;
this verifies plumbing and latency isolation, not a live-model pronoun-resolution
success rate. Requires application restart for the Python caller change; no restart,
commit, push, database migration or outbound action performed by this repair.

## State-mover timing repair — 2026-09-19

### DF30. Background work held as an interruption; evening holds moved into bedtime — FIXED locally, model behavior needs live verification

Live logs: at 21:40 state mover held research_coinbase_recovery until 22:15 because
research would supposedly interrupt the user's evening. At 21:35 it moved the
school-update question to 22:15 to avoid wind-down. Another hold used the already-ended
21:00–21:30 dog walk to defer a 21:34 question until 22:00. The old prompt both restricted
holds to interruptions and prohibited next-day holds, encouraging an unsuitable same-night
retry. Both prep paths supplied only a title-first 140-character context, omitting the
full directive needed to distinguish research from user-facing work.

Repair: Jinja explicitly permits background research and scheduled device actions during
quiet/away periods; holds require an actual interruption conflict. Resume times must
resolve the conflict and avoid sleep, allowing the next known available day when needed.
Both tick and wake prep now use one structured candidate with full directive, goal,
status, due gate and prior finalizer judgment. No Python prompt prose or classification
heuristic added. Existing stored wake times were not changed and no process restarted.

Publication check: newly added prompt examples use synthetic data; live personal names and work identifiers in these follow-up notes have been generalized.


### DF31. Durable evaluator inbox replays legacy admitted email history as new intake — FIXED locally, restart required

The durable-inbox repair in 7aacd502 introduced an unbounded compatibility predicate
in state_store.load_admitted_intake: state=artifact AND (evaluator_pending=1 OR
state_reason=triage_admit). Historical artifact rows retain triage_admit even though
that legacy marker does not establish a pending handoff under the new contract.
StrategicPlannerWoPrepNode merges all these rows into admitted_artifacts on each tick.
The strategic_planner_wo user Jinja labels them NEW INTAKE and omits source dates.

Read-only live verification on 2026-09-19 found an early-August family email still
marked artifact/triage_admit with both creation and last-review dates in August,
rendered in September planner context. There are 296 pre-September email rows eligible
under this predicate, reaching back to April 9. This is stored-history replay, not
proof of a new Gmail delivery. The normal email loader's today filter and
get_dayflow_items freshness filter do not govern this separate durable inbox path.

Intended: explicitly pending admissions survive restarts until handoff; legacy
context artifacts must not become fresh requests merely because they were once
admitted. Prompts must expose source dates so historical relative deadlines are
interpreted correctly. Consequences: stale requests can create duplicate or obsolete
work and old urgent wording can be interpreted as current. Repair must distinguish
new pending admissions from legacy history while preserving genuinely pending work;
do not blindly expire all pending work. No stored data or runtime code changed during
this diagnosis. Status: open. This refines the DF9/DF20 durable-inbox repair limits.


DF31 repair: removed the legacy triage_admit fallback from load_admitted_intake;
only explicit evaluator_pending=true admissions in artifact state are reloaded.
Read-only verification found zero pre-September email artifacts carrying that explicit
marker, so the 296 legacy emails are excluded without altering stored records.
The steward Jinja now labels the section PENDING INTAKE, renders original source
dates (or an explicit unknown-date label), and anchors relative deadlines to those
dates. Intake triage also renders full source dates rather than time of day alone.
Nine focused tests passed, including exclusion of legacy/cleared/closed admissions,
retention of a 90-day-old explicitly pending admission, restart handoff recovery,
and actual Jinja rendering of historical source dates. git diff --check passed.
Requires restart for the Python query change. Existing work objects were not changed;
this repair prevents replay but does not establish that earlier replay created no work.


### DF32. Shared work prompt loader bypassed canonical path resolver — FIXED

The publication regression guard caught fixed parent-index path arithmetic in
work_context.py. Template discovery now uses get_repo_root(), following the runtime
path contract. The previous expression resolved correctly in this checkout but
bypassed the centralized resolver. No stored data changes.


### DF33. Explicitly pending intake has no evaluator no-action disposition — FIXED locally

Following DF31, explicit evaluator_pending admissions correctly survive age windows.
However, StrategicPlannerWoPersistNode closes only sources transferred to work objects;
intake not cited in a successful handoff remains artifact/pending. The evaluator form
has no intake-level reviewed/no-action disposition and relevance_cleaner is unwired.
Thus an admitted item repeatedly judged not to require work has no automatic exit
from the durable inbox. The ordinary 24-hour view filter does not apply to this inbox.
Consequence: newly admitted non-actionable items can accumulate and be reconsidered
indefinitely. Intended: retain unprocessed admissions through failures/restarts, while
persisting explicit reviewed/no-action decisions separately from work handoffs.
At diagnosis, no runtime or stored-data change had been made.


Original agreed scope: restore an explicit intake cleanup lifecycle. First trace the
former orchestrator summarizer and relevance_cleaner to establish their actual prior
behavior. Then design durable dispositions for transferred-to-work, reviewed/no-action,
and deferred-with-reason intake, with cleanup for stale deferred/context items.
Preserve unprocessed requests through failures/restarts and preserve active work.
Acceptance: reviewed/no-action items stop recurring in pending intake; deferred items
have an explicit reconsideration/retirement policy; unfinished handoffs remain recoverable.
Implemented explicit no-action and timed-deferral reviews for ALL admitted sources,
including camera pods, in the existing evaluator. Every presented item must be transferred
or explicitly reviewed. No-action closes immediately; concrete deferrals return at the first
cycle after their due time. Original sources remain stored. Review batches are transactional
and reject changed source snapshots. Handoffs still close only after durable source transfer.
Also found/fixed: prep merged stale blackboard admissions over stored eligibility, allowing
closed/deferred intake back into the prompt; state mover could treat reviewed no-action
sources as fresh wake candidates. Durable eligibility now wins and event candidates respect
review dispositions. The retired relevance_cleaner remains unwired; its replacement is the
existing evaluator's explicit decisions, without another agent/model call.
A cited work spec with an empty objective is rejected before persistence (work_persist otherwise skips it); it cannot count as a completed handoff.
No live stored-data cleanup or backfill performed.


### DF34. Parent worker re-asks for permission after an explicit email denial — FIXED locally

Audit-inbox review found an email task created from historical intake before DF31's
repair. At 23:20:53 local on September 19, approval_gateway denied send_email and
tool_result_handler explicitly aborted the child manager with approval_rejected.
The parent worker then created an ask_user request at 23:20:57 to obtain permission
for the same acknowledgement. The user answered that the old email was already
handled a month ago. At inspection the work still showed active/dispatched with no
main-task finalizer outcome. The denied send was blocked; no successful delivery
was established by this review. Restart/orphan recovery may explain the persistent
state and needs correlation before assigning that part a root cause.

Intended: propagate explicit denial through nested manager results and stop the
same action without soliciting approval again; record/finalize the task outcome.
Investigate parent handling of manager_aborted and ask_user reply recovery. This
extends the remaining nested abort propagation concerns, rather than indicating
that approval_gateway itself failed to block the send. No runtime/data repair here.

Audit review also confirmed that the previously reported notification re-ask nodes
are now abandoned, with a notification-only node dispatched. DF29's general
failure-count escalation concern remains open. Five older calendar/timing cases
have terminal work objects; that retires those incidents, not proof of a general
scheduling repair. One legacy completed morning-reminder work still retains a
child in dispatched state, consistent with the recorded legacy terminal-container
cleanup limitation; no live data migration was performed.


### UI1. Vertical dashboard header menu buttons overlap — FIXED locally

The vertical template gives hamburger, quick settings, and dashboards buttons the
same menu-toggle-btn base class. emi.css positioned that base at left:1rem but
provided overrides only for the life/dev menus. Settings and dashboards therefore
occupied the hamburger's position. Added explicit right-side slots for settings
and dashboards, matching the existing life/dev spacing. Horizontal CSS is unchanged.


DF29/DF34 repair: lifetime counts no longer emit unconditional question directives.
Finalization tracks unmet attempts since successful progress separately, resetting
on achieved/achieved_plan_changes, including successful user questions. Missing legacy
episode counters start at zero; historical totals remain. STOP bypasses escalation.
Finalizer input uses the episode counter; applied question instructions are historical.

ToolResultHandler now propagates abort_task from child manager_aborted results via
structured blackboard state. Both graceful and default manager error exits preserve
that policy. The ordinary result recorder persists denial codes on the attempt;
finalizer context renders the denial explicitly and the judgment transaction enforces
unrecoverable/stop for approval_denied or approval_rejected. No live data cleanup or
process restart is part of this code repair.

DF29/DF34 verification: 124 focused finalizer, atomic result, rendered portfolio,
nested denial, and manager runtime tests passed (one dependency warning). Tests
cover two parent hops, default and graceful abort exits, success resetting only
the episode counter, STOP at the escalation threshold, and historical questions
remaining labeled as prior instructions. No live-model behavior is claimed.


### SH1. Lights tool misses nested partial-failure details and reports full success — FIXED locally

Confirmed source mismatch: smart_home_bridge returns {ok, integration, command,
result: {changed, state, unreachable}}. send_smart_home_command preserves that
wrapper. LightsControlTool.execute reads unreachable and changed from the outer
object instead of result, so its partial-reachability text never runs for the real
bridge envelope. The structured nested data survives in ToolResult.data, but the
content falsely states executed successfully. Live September 19 logs show discovery
timeout at 22:00:21, tool success text at 22:00:25, and a manager claim that all lights
were off at 22:00:31. Finalization then received the overconfident manager result.
Existing partial-reachability tests exercise bridge helpers, not this envelope boundary.
Intended: report devices changed and devices unreachable accurately through the
bridge/tool/manager/finalizer chain. Partial completion must not claim whole-house
completion. This explains the previously reported lights success-reporting finding;
it is not an additional independent bug. No runtime changes in this diagnosis.


SH1 repair: LightsControlTool now reads changed/lights and unreachable from the
bridge's nested result object while preserving the complete envelope in ToolResult.data.
Malformed result envelopes fail visibly instead of yielding success text. Tool-result
prose and follow-up guidance live in prompts/result.j2: report changed-device count,
name unreachable devices with unknown state, preserve exceptions in summaries, stop
this attempt, and do not automatically retry, seek approval/user intervention, or
create follow-up tasks/reminders. The next normally scheduled command may try again.
The tool description reinforces that policy. Eight focused tests passed using fake
devices, covering partial bridge-to-tool power/list results, one command invocation,
full success, malformed responses, and existing all-unreachable/targeted failures.
No real device actions, process restart, commit, or push. The no-retry guidance is
agent-facing policy; these tests do not establish live-model compliance.


Mailbox count repair: dispatch handlers return whether context was actually appended.
Unsupported types and empty/invalid injection entries no longer increase drain_to's
return count; one message targeting multiple agents still counts once. Updated the
old regression assertion that incorrectly counted a rejected blackboard_write.


DF28 atomicity repair (user-approved small scope): _apply_node_wakes now commits
clearing the wait and attaching arrival evidence through one WorkStore.batch call.
The existing transaction rolls back both graph and audit event on failure. An
expected_updated_at fence rejects changes between the post-node read and commit;
missing evidence is rejected and terminal/in-flight nodes are not released here.
The task retains its gate on failure, so normal promotion cannot dispatch it.
Nineteen state-mover/provenance tests passed, including evidence mutation failure,
failure after SQL persistence, successful subsequent retry, missing evidence, and
concurrent gate replacement. No live database repair or process restart.

Remaining DF28 scope: evidence is still appended to the task content, as before.
Moving it to attributed provenance and matching the exact pre-LLM gate snapshot
remain follow-up work alongside DF27; the post-read version fence does not claim
to validate the model's earlier input snapshot.


### DF35. One-shot informational reminder is retried and escalated despite an explicit frequency limit — PROMPT POLICY UPDATED; LIVE VALIDATION PENDING

September 20 live read-only review: a morning reminder's objective and main-task
content explicitly permit one brief notification and no additional prompt before
lunch unless requested. Tickets were sent at 06:00 and 07:01 local, expired without
acknowledgment, and the finalizer recommended another fresh notification. The
second unmet judgment triggered ask_user; an approval-to-remind ticket was sent at
09:00. The finalizer's own outcome acknowledged both earlier attempts and the
frequency constraint, yet proposed another. The process was the existing PID from
the previous evening; judgments predate the latest escalation repair. However,
DF29's historical-counter reset alone does not address this failure: neither
attempt achieved a judged success and the finalizer chose retry, not stop.

Intended: distinguish an informational notification with no required reply from a
question requiring an answer. Nonresponse must not override an explicit one-shot
or do-not-repeat constraint. A permission-to-send-another-reminder is itself an
additional interruption. Inspect ticket outcome semantics and finalizer handling
of notification frequency limits; do not claim user acknowledgment without evidence.
No runtime changes or live task/ticket mutations made during diagnosis.

Repair: finalizer, architect, and steward now include a shared Jinja policy for
unanswered contact. Routine/stale/one-shot prompts stop; an important still-relevant
retry must respect contact limits and the retry bound. Silence does not establish
receipt, consent, or danger. Permission-to-remind loops and recreation of stopped
contact are explicitly disallowed. The finalizer uses unrecoverable/stop rather
than claiming an expired goal was achieved, and chooses stop at the retry bound.
The existing store guard preserves stop instead of overriding it with ask_user.
Architect guidance also covers old finalizer instructions asking to repeat these
prompts. No new runtime classifier, chat-path work, or live task/ticket mutation.
These are model instructions; model compliance requires subsequent live validation.

Related confirmed off-by-one: finalizer_input previously warned only after the
prior unmet count already reached the limit, although finalize_task increments
before comparing. The warning now includes the current candidate judgment, so
at one prior failure it explains that a second unmet result reaches the bound.
Focused render regression covers prior counts zero, one, and two.

### DF27 / DF28 source-context repair checkpoint — 2026-09-20

This supersedes the earlier remaining-scope notes. External waits now receive full
main-task directives and structured intake candidates (no arbitrary first-30 cap).
Matched source_item_id must resolve to the prepared intake snapshot. Persistence
copies attribution and exact pod_id from that source; it cannot be replaced by a
model-generated pod reference. Gate release and an owned evidence record commit
atomically against the pre-LLM work version. Original directives remain unchanged.
The wake record is excluded from node_result so arrival context cannot masquerade
as a completed tool call. Worker/finalizer source sections include source excerpts;
architect/steward retain concise attribution and source handles. Work-level source
context survives takeover and new-node replanning. Existing intake age/eligibility
limits and historical records remain unchanged; semantic approval matching still
requires conservative model judgment, not just a source ID.

Validation covers actual Jinja views, source transfer from originating intake,
replan/takeover context, invalid source and stale gate/task rejection, atomic failure
rollback and retry, no duplicate receipt, and a source beyond the old 30-item cap.
No live database changes or external actions were performed.

Validation also corrected a stale strategic-render test assertion from before DF29: lifetime failure history must be visible without issuing another escalation directive.

### DF36. Ticket choices are fixed by category and response semantics collapse — FIXED locally; live semantic validation pending

Confirmed by reading ticket_builder/composer/agent_form.py and prompts/system.j2,
create_dayflow_ticket._ui_policy_for_ticket_kind and composer-output extraction,
static/js/proactive_popup.js, and TicketService.respond. The composer chooses
notify/advice/decision and writes title/message, but cannot supply button count,
labels or action scope. Advice always renders Acknowledge / Will Do / No / Later;
decision always renders Allow / Deny, even when the requested answer is not an
authorization. Notify and decision popup layouts do not offer a free-text field.
This prevents users from qualifying those responses through that popup.

The service preserves the clicked label and typed text together in user_text when
supplied, and separately keeps user_action, but maps done, acknowledge, willdo,
answer and accept to accepted with user_response_parsed.decision=accept. The richer
original response is therefore available to some readers, while a consumer that
reads only state/parsed decision loses the distinction between receipt, intention,
completion and permission. No new live misinterpretation is claimed by this review.
The same-state text-loss defect is already recorded under Remaining findings.

Proposed direction, not implemented: extend the existing composer with a validated
list of contextual choices and explicit meanings/scopes; render escaped labels;
resolve submitted choice IDs against the stored ticket; preserve exact button,
meaning and typed text separately; propagate them consistently into finalizer and
planner context. Keep free text available and authoritative over generic button
meaning. Audit approval consumers separately so acknowledgement cannot become
permission. Preserve compatibility for old tickets and non-popup response surfaces.

DF36 repair checkpoint: existing composer now supplies validated contextual choices;
persistence, popup, response service, tool results and strategic views preserve their
meaning and scope alongside authoritative typed text. Acknowledgment does not imply
commitment or completion. No new agent call or automatic approval is introduced.
Contextual replies use atomic receipts and follow-up history; the previously recorded
legacy same-state text-loss defect now rejects different fields instead of false success.

Related findings during integration: proactive_popup._resolveLayout ignored the
button_layout supplied by create_dayflow_ticket for dayflow_orchestrator tickets;
those tickets fell through to advice buttons. It now honors the validated/persisted
kind for that ticket type. Popup network-error rollback retained _processing and lost
the typed draft, preventing a successful retry. Retry rollback now clears that flag
and restores the draft. Both changes are covered by isolated browser verification.
State-mover response rendering omitted acknowledgments and response details; it now
renders all prepared response categories with the exact response history. Historical
accepted/declined buckets are display groupings, not independent authorization.

### DF37 / WO1. Timeout can start recovery without stopping the original worker — FIXED locally (2026-09-20)

2026-09-20 read-only lifecycle review, following the user's question about killing
hung workers. Confirmed code path: dayflow_tick._sweep calls
sweep_stuck_work_nodes then recover_pending_finalizations. The sweeper waits for
subtree inactivity beyond ASK_WINDOW_HOURS (1 hour) plus 20 minutes, records a
failed ToolResult with aborted=True, but sends NO cancellation request. The flag
is result evidence, not a signal to the running manager. Recovery explicitly allows
finalization of dispatch_sweeper results while their original session is alive.
A retry/replan can consequently dispatch a successor before the old worker exits.

_start_registered_thread rejects duplicate registration only for the SAME epoch.
For a newer epoch it replaces the registry entry even if the old thread is alive.
An isolated AST execution of the actual function with fake threads reproduced:
old_thread_alive=True, replacement_alive=True, one registry entry tracking only
replacement. No real threads, tools or live databases were exercised.

MAMInstanceManager.cancel sets cancelled on ONE manager's own blackboard; it does
not propagate to nested manager invocations. An isolated execution with two fake
manager records confirmed parent_cancelled=True and child_cancelled=False.
MultiAgentManager._run_loop checks that flag at cycle boundaries, so even requested
cancellation cannot interrupt an already-running agent/tool call. ManagerInterface
creates separately invoked nested managers; no shared work-attempt cancellation
mechanism was found. The generic tool execution paths have no work-attempt ownership
check before each external action. DayflowToolCaller does check the epoch before
entering the top-level tool call, but that does not guard subsequent nested calls.
WorkContext and WorkPlanner/helper mutations also lack a captured main-attempt fence
(the remaining WO1 finding). Refreshing a prompt from current graph state does not
revoke a stale worker's authority to execute.

Impact: a timed-out worker may resume, add history, invoke further tools, or finish
an external action while a successor is working. The final-result epoch fence
protects graph result commits; it cannot undo an email/device action already sent.
This review establishes a lifecycle gap, not an observed live duplicate action.
Comments in dispatch_sweeper claiming there is no in-process liveness to consult
are misleading: a session registry exists, though liveness alone cannot prove
progress or cancel a hung call. Closing a work object likewise does not cancel the
worker (previously noted under WO6); it requires this shared lifecycle policy.

Recommended repair order: bind the main dispatch attempt to a shared cancellation
signal inherited by its nested managers; request cancellation on timeout/abandonment;
check cancellation/ownership before new tool execution and worker graph writes;
retain old-attempt liveness until exit; coordinate replacement so an unresolved
in-flight external action cannot be blindly repeated. Cooperative cancellation
must not be described as force-killing a Python thread. Already-started external
operations need outcome reconciliation or explicit tool-level idempotency before
retry. Keep timeout evidence and normal finalizer judgment; do not add a second
result/finalization path. No runtime repair or live-state mutation made in this review.


#### DF37 / WO1 implementation and verification — 2026-09-20

Immutable main-attempt ownership now propagates through managers, agent activations,
helper WorkContext and monitored background execution. A queued descendant is registered
before submission and retains the attempt barrier after its parent returns. Worker
writes and tool admission check durable revocation/epoch. Timeout and abandonment
revoke execution without erasing live calls. Replacement claims (including new node
IDs) are held while execution or unknown external outcomes remain. Finalization uses
the existing path and remains the only failure-counter owner.

The implementation review also found the Orchestrator child cancellation path wrote
only a blackboard flag; it now calls MAM cancellation using the invocation ID. Direct
ManagerInterface node entry and legacy discharge now establish the same attempt
registration. An existing inline worker directive in ManagerInterface was moved to
the existing discharge Jinja view. The runtime dashboard now treats cancelling or
unknown execution as a hold rather than displaying an unconditional healthy badge.

Verification: isolated fake blocked tools, stale writes/epochs, inherited attribution,
post-approval/model cancellation, uncertain receipts across connections, queued child
cleanup, additive migration, restart identity, existing ticket recovery, result/finalizer
paths, and browser rendering with synthetic data. See EXECUTION_OWNERSHIP.md for
limitations: cooperative cancellation, conservative unknown classification, inability
to reconstruct receipts for actions performed by older releases, and no exactly-once
claim. No live actions or databases were used to verify this repair.

Additional DF37 runtime inspection: cancelled executor futures could remain recorded
as submitted, and finishing a never-started task decremented another task's running
count. Completion accounting is now idempotent, decrements only a running task,
and closes cancelled/failed submission records as well as execution reservations.

DF37 integration finding: MCP transport exceptions are converted to returned
`mcp_call_failed` errors, so exception-only receipt handling would incorrectly settle
them. Missing returns, this explicit transport failure, and `outcome_unknown=True`
now retain uncertainty. Ordinary returned failures keep normal finalizer semantics.

Execution verification follow-up (2026-09-20): all 149 focused assertions passed, but
test shutdown logged a late finalizer lookup after fixture deletion and an event-hub
submission after executor shutdown. This demonstrates a harness/background-lifetime
race, not a confirmed live deployment failure. The review also removed an unnecessary
global Dayflow signal from generic execution cleanup: the owner supplies its completion
callback, so standalone/scenario stores cannot wake unrelated Dayflow work. Production
EventHub shutdown ordering is a separate follow-up if reproduced outside this harness.


DF33 verification also reproduced a pre-existing assertion mismatch in
`test_email_pod_handle.test_the_goal_line_carries_the_pod_handle`, without the DF33 overlay.
The source renderer prefers `email_summary` over the generic subject-style `summary`;
the test still required the latter. Updated the assertion to require the informative
email summary alongside the exact pod handle. No source-rendering runtime change.


### DF38. Evaluator closure failures are swallowed and cancellation can be lost — FIXED locally

Confirmed in `work_persist.persist_steward_output`: complete/abandon exceptions were
logged as warnings and the pass continued, leaving failed closures active and eligible
for dispatch. The decision lived only on the tick blackboard. Closure and concern
feedback shared one try block, although concern_feedback already handles its own
post-commit errors. This is the remaining closure finding above, now repaired.

All explicit terminal intents are saved atomically in existing work-object constraints
before applying closures or creating/changing other work. Pending closure blocks readiness,
claims, revisions, worker writes and new tool calls. Terminal mutation removes its intent
in the same commit; failure preserves it and raises to stop the pass. Evaluator prep
retries durable intents before presenting the portfolio, including after restart.
Concern feedback runs only after successful closure and logs a separate failure category.
Completed closures are not rolled back if a later closure or feedback fails.

Limits: if the database cannot save the initial intent batch, the pass raises but there
is no durable request to recover. Already-started external calls cannot be recalled.
At DF38 completion concern feedback remained best-effort; DF40 below adds durable delivery. No live
work-object cancellation, stored-data backfill, restart, commit or push performed.

Same-pass source/objective updates for a closing work object are committed with its
closure intent, preserving incoming user context before the object becomes terminal.
Verification: injected terminal-write failures, restart/second-connection recovery,
blocked claims/tool calls, atomic intent batches, partial closure progress, separate
feedback failures, and existing intake/execution/finalization regressions.


### DF6 duplicate-validation checkpoint — FIXED locally

`_dedupe_prunes` previously skipped invalid mappings while the batch could still consume
the finalizer's instruction. It now rejects blank/self/unknown/finished/non-main task
references and survivors also being abandoned or deduplicated. This matches the existing
agent form's rule: keep exactly one survivor, without chains or cycles.

Also confirmed in the same path: `work_architect_node._duplicate_pairs` silently skipped
malformed pairs and overwrote repeated duplicate IDs before graph validation. Conversion
now rejects malformed shapes, non-string/blank/self IDs and repeated normalized IDs.
The entire mapping is validated before writes. Tests compare graph snapshots and audit
events after rejection, including mixed valid/invalid pairs with a pending finalizer
instruction and proposed new tasks. Valid deduplication still redirects prerequisites
and dependents, preserves history and acknowledges the instruction in one batch.
This validates structure, not the model's judgment that two tasks mean the same thing.


### DF39. Concern feedback misses current ticket replies and conflates words with decline — FIXED locally

`concern_feedback._last_user_reply` required evidence author `reply`, while the result
recorder uses the tool name or `ask` during recovery. Current replies therefore reached
finalizers in prose but were omitted from concern feedback. Evidence now also stores
an explicit `user_reply` payload for actual ticket responses from the ticket tool or
recovery path, preserving ticket/question, text, exact choice meaning/scope and history.
Timeouts, creation receipts and arbitrary manager output are not attributed to users.
The reader selects by response/evidence time rather than node dictionary order.
Historical explicit `reply` records remain readable; no historical prose is parsed or
backfilled. Both done and abandoned concern outcomes journal the structured response.

Related confirmed bug: `apply_work_outcome` treated any nonempty user words on abandoned
work as a decline and parked the concern. That would turn acknowledgments into refusals
once missing text was restored. Only an explicit `decline` choice without qualifying
typed text uses that automatic path. Typed text, generic No/OK and other meanings are
preserved for the existing noticer to interpret; they do not imply permission, completion
or rejection. No new agent/model call. DF40 below subsequently adds durable delivery and visible handling history.


### DF40. Automatic completion loses concern feedback; noticer cannot see handling history — FIXED locally

Confirmed: `WorkStore._rollup` can finish a concern-linked work object without
calling `propagate_work_outcome`. Explicit closures alone attempted delivery, and
a register failure lost it. `_render_concern_summary` and `_render_closed_concern`
also omitted reinforcement journals, including user replies saved by DF39.
The 48-hour recent-items window could not substitute for lasting concern history.
Four-day addressing pressure then allowed a settled concern to resurface without
its handling context. The reported repeat checkup notices fit this failure mode;
the specific live event has not been reconstructed.

Fix: terminal transitions atomically enqueue a snapshot in `work_concern_feedback`.
Finalizer/explicit closure deliver after commit; evaluator prep recovers outstanding
receipts. Register receipts make partial delivery and crash retries idempotent.
Failed or unresolved deliveries remain pending. Generic WorkStore never starts a
noticer or touches the register. The register retains structured per-work outcomes,
finalizer summaries and attributed replies outside the bounded reinforcement journal;
active/addressing/closed projections expose this history and the journal.

Noticer Jinja instructions distinguish acknowledgment, user ownership, scheduling
and actual completion. Old observations/silence/age alone cannot justify another
notice; reopening requires new evidence, a new due occurrence or agreed follow-up.
Review pressure remains a decision request. `keep_active` now timestamps addressing
reviews: previously it reset only reinforcement counts, so the age test demanded
another disposition every tick. Dormant projection no longer mislabels all user
declines as accepted chronic conditions. Pressure instructions moved from Python
into the existing Jinja policy.

No historical backfill or live concern cleanup. A completed notification does not
automatically resolve the underlying need. Noticer triggering is best effort;
durable register delivery remains usable by its next scheduled run.


### DF41. Readiness skips lose the next Dayflow tick — FIXED locally

`DayflowScheduler._execute_tick` returned for incomplete setup or empty history
before reaching the finally block that schedules the next tick. The consumed
one-shot job was not replaced. Setup completion writes its flag without explicitly
poking Dayflow; progress then depended on an external event or restart.

Both skips now call the existing `_arm_ceiling_tick` helper: a 30-minute readiness
recheck, preserving any sooner scheduled tick and respecting scheduler stop. These
paths still do not invoke agents or arm work-node execution. Incomplete setup also
continues to skip the history query. No change to ordinary chat processing.

Regression coverage: both gates, repeated empty-history checks, preservation of a
sooner external wake, and no rearming after stop. Existing scheduling-failure and
work-job lifecycle tests remain applicable. No live restart or data changes.


### WO5 repair checkpoint — FIXED locally

The standalone driver now promotes ready main tasks, atomically claims them, runs
the existing discharge/result-recorder path, and invokes the standard finalizer
agent and writer against the caller's store. Recorded results awaiting judgment
are finalized before more work starts. Finalizer retry/replan/stop outcomes halt
this runner; it never invents a plan or retries a judged failure. Dependency/time/
external-event gates still apply and worker provenance is never dispatched.
Explicit-node calls support ready, preclaimed and recorded-result tasks.

The finalizer accepts an explicit store for isolated scenarios without calling the
global Dayflow store or concern delivery. Production control-node behavior stays
the same. The smoke scenario now verifies closed tasks, evidence, finalizer epochs
and goal completion; old done/verified/passed checks were not the current contract.
Regression tests fake only the worker/model boundaries; no real model run was made.


## Beliefs and KG audit — 2026-09-20

All findings below are OPEN; this pass changes audit documentation only. Full assessment, source links, design concerns and repair order: [Beliefs and KG audit](beliefs_kg_audit_2026-09-20.md).

### MEM1. Noticer belief updates are logged and counted, never applied — OPEN (P1)

The noticer schema and prompt permit belief_updates, and its routine reports their count. The writer reads that list but only counts it and includes the original output in the tick log; it never invokes a belief writer. The routine calls no subsequent belief-application stage. A user correction understood by the noticer can therefore fail to become memory.

Confirmed by call-chain inspection and a temporary-register/database probe: one reported update, no belief row. Apply through the normal belief writer with evidence and a durable delivery receipt; distinguish emitted, applied and failed counts.

Evidence: [app/assistant/subconscious/persist.py:169](../../app/assistant/subconscious/persist.py#L169).

### MEM2. Ticket outcomes are converted into stronger user-intent claims — OPEN (P1)

The aggregate collector maps expired to snoozed and emits “User snoozed/deferred ... without completing.” It also maps accepted to acceptance without reading the contextual response meaning, scope or typed text introduced by DF36. An acknowledgment can therefore become acceptance; absence of a reply becomes a claimed user decision. Thresholds use event count, not distinct days, despite the cross-day description.

Expiry misclassification reproduced with three expired tickets. Accepted-response ambiguity confirmed by the collector fields it reads. Use explicit response semantics, retain silence as silence, and separate receipt from commitment/completion.

Evidence: [belief_engine/pipeline/steps/collect_evidence.py:212](../../belief_engine/pipeline/steps/collect_evidence.py#L212).

### MEM3. Evidence polarity is assigned before the belief it is evidence for exists — OPEN (P1)

The updater attaches the source signal unchanged to every cited belief. rejects becomes contradict in the store, even when the belief is “the user does not want this suggestion.” That observation supports the negative preference. Conversely, a chronic daily-insight item defaults to confirms even when it contradicts the specific existing belief being updated. The output schema has evidence indices but no per-belief evidence relationship.

Reproduced a negative preference supported by rejection: recompute gives it zero support, positive contradiction and contested status. Valence must be a property of evidence-to-claim attachment, separate from the source event’s sentiment/action.

Evidence: [belief_engine/pipeline/steps/update_beliefs.py:62](../../belief_engine/pipeline/steps/update_beliefs.py#L62).

### MEM4. Reprocessing old evidence manufactures fresh observation metadata — OPEN (P2)

Evidence insertion deduplicates unchanged observations, but every upsert still increments observation_count and sets last_confirmed from the request. The nightly updater supplies today, including no_change with no new evidence. Live retrieval ranks by those fields. In addition, rolling ticket aggregates with overlapping constituent events produce new summaries and therefore new weighted evidence rows as the count/window changes.

Reproduced one evidence row but two observations and a new confirmation date. Exact duplicate evidence weight is protected; recency/frequency are not. Aggregate overlap is confirmed structurally, not measured in live data. Count new source observations and retain stable source-event identities; do not sum overlapping rollups as independent observations.

Evidence: [belief_engine/store/belief_store.py:316](../../belief_engine/store/belief_store.py#L316).

### MEM5. Owner corrections and suppression are not enforced at the write boundary — OPEN (P1)

The central upsert has no locked guard. The updater checks locks only while action is update/deprecate, then later converts create on an existing key into update. That path overwrites a locked statement. Other callers can also upsert it. Suppression deprecates a row; the archive later removes it from the live table, and creation does not consult an enduring suppression record or archive. Old evidence can recreate the same key under a new id.

Reproduced both the actual updater create-path lock bypass and archive/same-source recreation in temporary databases. Enforce owner authority transactionally in the shared writer and preserve suppression/correction tombstones independently of active rows.

Evidence: [belief_engine/store/belief_store.py:322](../../belief_engine/store/belief_store.py#L322).

### MEM6. A belief can commit before its evidence, and an embedding failure interrupts the evidence write — OPEN (P1)

upsert_belief commits the claim, performs the Chroma upsert, then inserts evidence in separate transactions. A failed embedding call leaves a committed active claim with no newly attached evidence. Recompute explicitly skips beliefs with no usable evidence, so it cannot subsequently age such a row out through this model. Empty/invalid evidence refs on create and reevaluator-created split rows are additional evidence-free paths.

Injected an embedding failure: the method raises, but an active row remains with no evidence. Commit claim/version and evidence atomically, then process a durable vector-sync job. Reject unsupported inference writes or represent their provenance/derivation explicitly.

Evidence: [belief_engine/store/belief_store.py:329](../../belief_engine/store/belief_store.py#L329).

### MEM7. Contested beliefs can fall out of the reevaluation work queue — OPEN (P1)

Reevaluation consumes only keys on the current run context. Recompute scans active rows and reloads persisted contested keys only when that run reports a new contested flip. Write-time dedup can mark another stored belief contested without adding its key to that context. Canonicalization can contest both sides after the reevaluator has already run. Failed reevaluations also lack an unconditional persisted-queue drain. Active-only retrieval then hides these beliefs while they wait.

Reproduced a persisted contested belief with no fresh flip: the next recompute/reevaluate sequence reports no contested beliefs and leaves it stranded. Drain persisted contested work independently of whether new conflicts were discovered, with bounded retries and visible failure state.

Evidence: [belief_engine/pipeline/steps/reevaluate_beliefs.py:72](../../belief_engine/pipeline/steps/reevaluate_beliefs.py#L72).

### MEM8. The main KG question-answering payload omits structured validity dates — FIXED locally (P1)

Both global and node-anchored ask_kg build node evidence containing label/type/description/aliases/optional attributes, omitting first-class start_date, end_date and observation fields. Edge evidence includes updated_at, a maintenance/write time rather than validity. The RAG agent is told to answer solely from that payload. Dates might occur incidentally in prose, but the authoritative temporal distinction can be unavailable. This finding concerns handle_ask_kg, not every KG reader: shared card/wiki bullets do render temporal information.

Confirmed by both payload builders and the shared ask_kg prompt; no live wrong answer claimed. Include structured validity, observation time, confidence and closure reason in every selected claim, and teach current-versus-historical interpretation.

Evidence: [app/assistant/lib/core_tools/kg_search/knowledge_graph_search.py:509](../../app/assistant/lib/core_tools/kg_search/knowledge_graph_search.py#L509).

### MEM9. One late re-observation can prevent KG state decay indefinitely — OPEN (P1)

The expiry anchor is start_date/first_observed/created_at plus the estimated duration. A last_observed timestamp at or after that fixed expected_end causes an unconditional skip. As time advances the same old timestamp continues satisfying the test. Re-observation does not establish a new expiry deadline here.

Reproduced a three-day state observed on day four and otherwise silent for roughly 296 days: it is still skipped as recent activity. Define renewed freshness relative to the latest genuine observation, without equating silence-based expiry with proof that an event actually ended.

Evidence: [app/assistant/pipelines/kg_maintenance_pipeline/step_state_decay.py:148](../../app/assistant/pipelines/kg_maintenance_pipeline/step_state_decay.py#L148).

### MEM10. A transient KG extraction failure is persisted outside the retry queue — OPEN (P2)

The pending-window query excludes every window with any extraction row. An extractor exception is caught and persisted as verdict=error, then counted as processed. The next run excludes that window, and the generic runner does not see the exception. Thus a transient model/provider error can permanently omit that window from automatic extraction until explicit repair.

Confirmed by exception, selection and pending-count paths; no automatic error requeue found in the reviewed live pipeline/routes. Retain failed work as retryable with bounded backoff, attempt count and an operator-visible dead-letter state.

Evidence: [app/assistant/pipelines/kg_pipeline/steps/critique_and_extract.py:45](../../app/assistant/pipelines/kg_pipeline/steps/critique_and_extract.py#L45).

### MEM11. Qualifiers and confidence/decay metadata have inconsistent contracts — OPEN (P2)

An update that omits conditions clears the stored conditions; no_change constructs such a request. The live retrieval API reads extraction-time confidence while the export prefers current_confidence_band. The evidence half_life_days_snapshot is stored with comments promising historical preservation, but recompute does not select it and instead applies the current belief kind to all evidence.

Qualifier erasure reproduced; confidence and half-life inconsistencies confirmed by reads. Preserve omitted qualifiers, expose a named effective-confidence contract, and decide explicitly whether reclassification recomputes historical decay or honors evidence snapshots.

Evidence: [belief_engine/store/belief_store.py:315](../../belief_engine/store/belief_store.py#L315).

### MEM12. Write-time dedup lacks the incoming evidence dates and can admit known-stale beliefs as active — OPEN (P2)

The merge verifier gets the stored belief’s dated trail but only a generic new-statement context for the incoming side. The actual incoming evidence is resolved later. If the verifier nevertheless says the stored side is current, the code deliberately proceeds to write the incoming stale side, ordinarily active, relying on a future sweep. Agent readers can see both meanwhile.

Confirmed by dedup input construction and the supersedes/current_side branch. Pass both source-dated trails before deciding; retain historical claims as historical, not competing current guidance. This is separate from ordinary semantic near-match uncertainty.

Evidence: [belief_engine/pipeline/steps/update_beliefs.py:151](../../belief_engine/pipeline/steps/update_beliefs.py#L151).

### MEM13. Belief pipeline success can conceal per-belief failures — OPEN (P2)

The pipeline marks a step successful whenever it returns without raising. UpdateBeliefsStep catches per-belief errors and returns partial_error; recompute and reevaluation also carry error counters. These do not change the pipeline’s overall success, so the adapter can report success and export a partially updated set.

Confirmed by step and adapter contracts. Preserve partial completion but propagate partial/error health, count outstanding repairs and avoid calling the entire run successful.

Evidence: [belief_engine/pipeline/pipeline.py:108](../../belief_engine/pipeline/pipeline.py#L108).

### MEM14. Additional audit discrepancies — OPEN / scope limited

Jinja-only prompt policy still conflicts with instruction strings in belief updater/
reevaluator, ask_kg and context_activation Python. Belief docs overstate incoming
dedup evidence, current-confidence consistency and historical half-life preservation;
routine JSON still describes per-domain execution. KG docs overstate the promoter's
exclusive write role and general reopening on re-observation. See the report's
additional-discrepancies section for evidence and unresolved policy boundaries.

KG runner classifies all sqlite3.OperationalError exceptions as lock retries;
non-lock operational faults need a different recovery policy. Belief evidence
dedup is check-then-insert across sessions with no matching unique constraint in
the reviewed ORM, leaving a concurrency race (inspection, not stress-tested).
No fixes, live cleanup, commit or push were performed in this audit.


### MEM15. Weekly belief candidates bypass belief reconciliation — OPEN (integration gap, P2)

The weekly synthesizer explicitly emits belief_candidates with confidence, conditions
and evidence for incorporation into the belief engine. The active v1 collector reads
14 days of daily insights and ticket aggregates only; no weekly candidate ingestion
was found. The Dayflow routine writer instead receives the first three weekly
candidates directly, reduced to domain and statement. Thus weekly findings can
influence guidance without stored evidence, reconciliation or belief decay, and
separate candidate conditions/confidence/evidence are omitted from that input.
This is a confirmed code-path gap relative to the user's intended architecture,
not a claim that a particular live weekly candidate caused a wrong action.

Evidence: weekly_insights/steps/synthesize_weekly_insights.py; agents/weekly_synthesizer/
agent_form.py; belief_engine/pipeline/steps/collect_evidence.py;
pipelines/dayflow/steps/dayflow_routine_stage.py::_format_weekly_insights.
Direction: route weekly candidates through the existing belief reconciliation path,
preserving their underlying daily evidence identities to avoid double counting.

### MEM14 follow-up: upstream prompt and consumer projection discrepancies

Daily insight system.j2 describes structured extracted_claim kinds and non-decaying
preferences from the v2 design. configs/subsystems.yaml has beliefs_v2_primary=false;
the active v1 daily collector ignores extracted_claim, using summary, first evidence
string, tags and temporal_scope. Its stable preferences have a 365-day half-life.
The reevaluator prompt promises every historical signal, while its Python formatter
selects only the latest 50 evidence records and truncates each raw quote to 200 chars.
These are confirmed contract discrepancies; neither establishes a live wrong belief.

The Dayflow routine/health formatter projects only domain, confidence, statement and
conditions. It omits exported belief keys, dates and counts. Orchestrator evaluator,
architect and state_mover receive the generated resource_dayflow_routine. This is a
context/provenance design limitation to address when solidifying architecture, not
proof that all possible consumers lack direct access to belief resources.


### MEM16. Merge bookkeeping becomes fresh supporting evidence and hides original lineage from ordinary readers — OPEN (P1)

`BeliefStore.merge_belief` appends a `canonicalization` row dated at the merge,
with signal `confirms` and weight `2.0 * len(deprecated_keys)`. It also advances
last_confirmed and transfers observation counters. `decay/recompute.py` prefers a
nonzero stored weight over the zero baseline for canonicalization, so these merge
rows contribute support despite the comment saying bookkeeping is ignored. Semantic
consolidation can therefore manufacture fresh support without a new user observation.
This extends MEM4 with a distinct, confirmed producer of synthetic reinforcement.

The survivor gets a shortened merge summary; original evidence remains attached to
the deprecated predecessor, eventually in the archive. Ordinary get_evidence and
reevaluation do not traverse those predecessor links. The copy-only pilot adds explicit
archived predecessor inspection without assuming the historical merge was correct.

Frozen database inspection on 2026-09-20 found 606 current belief rows: 174 had attached
evidence consisting only of canonicalization records; 3 had no attached evidence.
Of 3,732 current evidence rows, 2,631 were canonicalization records, with total stored
weight 5,386. These counts do not establish that the beliefs are false: original evidence
may survive elsewhere. They establish that bookkeeping is being presented as support
and that direct evidence readers can lack the original source history.

No repair to the production merger or decay path was made in this experiment. Keep
bookkeeping separate from observations, follow provenance without assigning semantic
equivalence, and preserve originals even when a merge is eventually authorized.

### Contextual review pilot (2026-09-20)

Implemented an opt-in copy-only review path under `belief_engine/review`, with standard
`belief_engine::context_search` and `belief_engine::context_review` agents, Jinja prompts
and structured forms. LLM-selected context can expand to exact archived predecessor IDs
and original master-room chat/ticket source days. No merge operation exists. Revisions
and their source-linked explanations commit with before/after receipts; source dates,
counts and legacy evidence weights are unchanged. Runtime pipeline behavior is unchanged.

The seven-belief, three-case authorized model evaluation produced four proposed changes
and three preservations on a copied database. This is exploratory model behavior, not
proof of correctness or production readiness. Integrity tests cover rollback, replay,
locks, stale context/evidence, citations, archive inspection and bounded investigation.
Personal source material and detailed results remain outside the repository. See
`belief_engine/review/README.md` for scope and explicit production-integration limits.


### MEM14/MEM15 projection follow-up (2026-09-20) — projection repaired; upstream gaps OPEN

The routine writer now receives belief keys, conditions, kind, confidence and observation
dates, with instructions to retain citations. Health formatting and full source-evidence
retrieval are unchanged. Weekly projection now retains all candidates with conditions,
confidence, evidence and the historical date range. This repairs qualifier loss and the
arbitrary first-three limit; weekly ingestion/reconciliation into beliefs remains open.

### MEM17. Contextual beliefs excluded and resolved guidance cached — FIXED (2026-09-20)

The routine stage admitted only certain kinds or routing tags, excluding potentially
relevant preferences in other domains. Its fingerprint ignored current status,
milestones and weekly context, so completed preparation could leave obsolete guidance.
_read_daily_context also ignored an explicitly empty authoritative expected_schedule,
retaining cancelled events from older daily context. Writer instructions and its output
schema conflicted over past/completed activity and did not preserve belief identifiers.

Repair: standard LLM selector reads the full active export; Python validates returned
identities without semantic classification. Writer uses original selected records,
qualifiers and citations, places preparation before events, and suppresses resolved asks.
The cache includes all contextual inputs, and an authoritative empty calendar clears the
old schedule. Prompt behavior lives in Jinja. Existing hourly eligibility remains;
ordinary chat performs no new model calls. A changed projection adds one selector call.

Verification: isolated unit checks cover context invalidation, unknown keys, cache reuse,
empty calendars and qualifiers. Synthetic model cases placed lesson preparation at 16:00
for a 17:00 event, moved it to 11:00 for noon, and suppressed another reminder after explicit
packing/delivery evidence. These examples are not a guarantee of model correctness.
See contextual_belief_projection_2026-09-20.md. No live resources or belief rows regenerated.


### MEM18. Candidate discovery has narrow coverage and inconsistent score contracts — OPEN (P2)

UpdateBeliefsStep.run concatenates the entire evidence batch into one embedding query,
then shows the updater at most eight hits. dedup_candidate searches only three neighbors
and stops at the first same verdict. Neither query embeds separate conditions or evidence.
BeliefStore.find_similar filters status after the limited Chroma query, so inactive hits
can consume its small candidate budget without refill. These are confirmed coverage
limitations; the number of missed duplicates in the live catalog has not been measured.

The weekly canonicalizer compares normalized-vector cosine >=0.80, plus shared stemmed
English words appearing in 2–12 beliefs with cosine >=0.50. Write-time dedup reuses the
0.80 constant against Chroma's 1/(1+distance), a different scoring contract unless the
configured distance happens to make it equivalent. Actual collection metric/recall was
not measured in this audit. The weekly pass excludes beliefs lacking embeddings.
Evidence: update_beliefs.py::_SIMILAR_K/_DEDUP_K/run/dedup_candidate;
canonicalize_belief_set.py::_embeddings_for/_keyword_pairs/_run_verifier_dedup_pass;
belief_chroma.py::search; belief_store.py::find_similar.

### MEM19. Pair decisions omit qualifiers and cache incomplete judgments — OPEN (P2)

belief_context_block supplies six recent evidence summaries, truncated to 160 characters,
plus dates/kind/domain/counts. It omits structured conditions, scope and original source
text; evidence lookup errors become an empty-history block. The updater's existing-belief
formatter omits these qualifiers too. A same verdict can consequently ignore conditions
held outside the statement. merge_belief does not pass conditions to upsert_belief, which
clears them on conflict: this extends MEM11 to confirmed merge-time qualifier loss.

Distinct-pair cache invalidation hashes only statement text, ignoring changes to evidence,
conditions, scope and verifier policy. An unusable verifier relation is coerced to different
and can be durably cached, suppressing a later valid review. Context-dependent distinctions
can therefore remain unreviewed after their basis changes. No live wrong merge established.
The verifier also lacks an explicit insufficient-context outcome; its fuller-version merge
instruction needs clarification against its specialises rule.
Evidence: canonicalize_belief_set.py::belief_context_block/verify_relation;
store/distinct_pairs.py; merge_verifier/prompts/system.j2; store/belief_store.py::merge_belief.

### MEM20. Weekly pair backstop can silently omit the remaining candidates — OPEN (P2)

_run_verifier_dedup_pass truncates sorted pairs to MAX_PAIRS=4000 before cached distinct
pairs are skipped. Unlike its 400-model-call limit, this truncation does not set truncated.
A sufficiently large candidate set can repeatedly revisit the same capped prefix and
stamp a full sweep complete without reaching the tail. Confirmed control flow; not measured
as an active problem at the current catalog size. Missing embeddings similarly are not
reported as incomplete coverage. No runtime repair performed during this audit.


### Incremental matching repair (2026-09-20)

MEM18/MEM20: replaced live embedding/keyword admission and capped weekly pair sweeps with
LLM catalog discovery, per-input page receipts, versioned pair queues and explicit pending
counts. No unchanged-night pair comparisons. Legacy helpers remain historical only.
MEM19/MEM12: reviewers receive full stored evidence, conditions and source dates after new
records are persisted; malformed answers remain pending, unresolved is explicit, and cache
versions include evidence/qualifiers/policy. Complete records replace 160-character/six-row
context limits; reevaluation's 50-row/200-character limits are removed as well.
MEM16: new atomic merges produce no supporting observation; complete before/after receipts
and redirects preserve evidence, which ordinary readers and decay now traverse through live
and archived predecessors. Exact and explicitly reviewed same-source observations count once.
Legacy bookkeeping no longer contributes weight. Existing historical mistakes are not reversed.
MEM11: merge conditions are explicit; updater no-change and absent conditions preserve the
stored qualifiers. Remaining confidence/half-life issues are unchanged.
MEM7: reevaluation now includes persisted contested records, including prior-run matching
conflicts. MEM13: pipeline propagates reported per-step errors and includes step results so
pending matching work is visible. MEM5: updater lock check now includes create-on-existing.

Five additive matching tables are initialized by production first use, not this code edit.
Tests use isolated databases/fake agents. Ordinary chat has no matching calls. Oversized model
input fails pending without truncation; multi-call investigation of a single oversized packet
and source documents absent from stored evidence remain explicit limitations. No live database
cleanup, restart, commit or push accompanies this implementation.


### MEM21. Full lineage repeated across candidate-discovery pages — FIXED locally (2026-09-20)

Fresh dry-run preparation counted 579 active beliefs, 8 catalog pages and 454 focal pages,
requiring 3,632 discovery calls before review because every focal contained all archived
evidence. Discovery does not decide merges; it now uses complete belief records, retaining
entire statements/conditions, while the decisive pair reviewer still reads full evidence.
Evidence-aware version invalidation remains intact. This repairs an implementation cost
problem discovered before spending model calls; no production belief rows were changed.


### Belief source accounting follow-up (2026-09-20)

MEM4: ordinary upsert/no-change rereads now derive counts and dates from original sources in the same transaction. Processing time no longer reconfirms claims. Overlapping ticket rollups remain OPEN.

MEM5: central upsert now honors owner locks. Suppression tombstones across archival remain OPEN.

MEM6: claim and complete evidence batch now commit atomically before indexing. Injected evidence failure rolls back both; index failure retains both and raises. A durable ordinary-write index repair outbox and historical provenance repair remain OPEN.

MEM11: omitted conditions are preserved; explicit valence governs confirmation; decay honors saved half-lives (-1 explicitly means durable, legacy NULL uses kind). Zero weight stays zero; undated/missing evidence produces unverified snapshots without deprecating the claim. Legacy missing policy/date information remains unknown.

MEM15: global collection now reconciles complete weekly candidates through the updater with zero independent support. Dayflow no longer injects candidates around reconciliation. Daily source refs are unchanged to avoid migration duplicates.

New confirmed extension to MEM4/MEM11: `flag_stale_chronic_beliefs` writes `decay_review` bookkeeping with a nonzero historical weight. It is now excluded from observations and decay alongside merge/deprecation receipts. Staleness is not new evidence about the user.

Verification details and remaining work: [belief_source_accounting_2026-09-20.md](belief_source_accounting_2026-09-20.md). Copy-only provenance audit: 131 active claims lack reachable original observations; none has a same-key archived candidate carrying original observations. No live data changed.


### Evidence interpretation follow-up (2026-09-20)

MEM2/MEM3: rolling ticket context now preserves complete dated records rather than deriving refusal/snooze/acceptance meanings from transport state. The updater explicitly assigns support/contradict/qualify to each cited source for the particular claim. Missing/invalid relationships fail the write instead of falling back to source sentiment. Deprecation attaches cited original evidence before the bookkeeping transition. Existing incorrectly assigned historical valences are not rewritten automatically.

MEM4: new rolling ticket windows carry source_type ticket_context and zero independent weight/count. They can inform the LLM alongside original daily observations without counting the same underlying events again each time a window changes. Existing legacy weighted rollups remain a provenance review concern; no speculative historical rewrite was performed.

Updater prompts now preserve uncertain duplicates and remove rules that inferred pacing preferences from expiration counts. Full replies and qualifiers are retained. 64 isolated tests passed, including negative evidence supporting a negative claim, invalid citation rejection, and changed ticket windows with no added support.


### MEM22. Discovery rejects valid supplied focal pairs and stalls on self-pairs — FIXED (P2)

The full copied-catalog evaluation produced 527 self-pairs and 147 pairs rejected by the original focal-to-catalog-only validator. The model can legitimately compare two supplied focal records. Self-pairs carry no work. Discovery now validates that both IDs were supplied, normalizes orientation when one is focal, drops self-pairs, and deduplicates exact pairs. Valid pairs within either supplied set are safe candidates; unknown IDs still fail. This changes identity validation only; the reviewer still decides semantic relationships. No live belief changes.

Discovery also produced 34 unknown IDs and five pairs without a focal record. An invalid page gets one bounded retry using short page-local labels, mapped back by Python. Invalid retry outputs still leave discovery pending. No semantic fuzzy ID repair is performed.


### MEM23. Model citation transcription errors stop an otherwise valid comparison queue — FIXED (P2)

The full copy-only evaluation exposed invalid evidence UUIDs in review results. The validator correctly refused them but had no bounded correction path. Review now validates source citations and observation-equivalence groups before application and retries once using short exact source labels. Python maps labels back to original IDs; it never uses fuzzy matching. Invalid retries leave the work pending. No evidence text is shortened.

### MEM24. A reviewer can mistake a compound superset for an equivalent belief — FIXED with conservative LLM merge check (P1)

In the copied catalog, a general research preference was proposed for absorption into a recipient-specific research-and-delivery rule. Shared source material and nonconflicting additions were being treated as equivalence. A separate standard merge_check agent now inspects both complete claims, sources, and proposed wording for changed applicability, added independent obligations, exceptions, and permissions. Only valid explicit approval permits a merge. A negative or invalid approval preserves both claims as unresolved, retaining the complete proposal, reasoning, and diagnostics. The check interprets context, including harmless paraphrases; Python enforces the result. This adds calls only for proposed merges in the background belief pipeline.

### MEM25. Oversized evidence investigations cannot progress without a single large prompt — FIXED with bounded continuation (P2)

Large packets now receive full-coverage source-page reading by a standard evidence_page agent, with exact fragment IDs and durable per-page receipts. All serialized source text is partitioned losslessly. The final reviewer and merge checker receive complete claims and every page finding. Missing coverage is refused. If the complete findings still exceed the final budget, the comparison stays pending without clipping. Cross-fragment uncertainty prevents a confident merge. Whole-packet review remains the path for ordinary-sized records.

Copy-only provenance investigation additionally found 81 of the 131 missing-source claims have potential original-source records explicitly named in old merge summaries. These are retrieval candidates, not automatically restored evidence; incorrect historical splits/merges require source-based review.


### MEM26. Superseding one clause can discard a compound belief's independent rule — FIXED with retirement check (P1)

Copy-only review found an old lighting record with separate on/off schedules. A dated off-time correction established replacement of that clause, but deprecating the whole old record would also remove the on-time clause. The independent final check now covers `supersedes` as well as `same`. It requires dated source support and preservation of independently valid clauses; partial replacement stays unresolved for a source-based split/revision. No live record was retired during evaluation.


#### MEM6 historical provenance investigation — copy-only review

The new opt-in provenance proposal service distinguishes historical retrieval leads
from source-to-claim attribution. Old compound predecessors can contain sources that
are irrelevant to a surviving split claim. In the frozen baseline, a browser-automation
news-site request appears among a timesheet-trigger predecessor's evidence; it does not
establish when a timesheet job may run. Some daily-insight summaries also repeat a broad
claim before a much narrower user quote. Recovery must assess the original source and
its interpretation limits instead of treating the repeated claim as fresh support.
The service reviews each full source against the full current claim, preserving partial
support as qualify and ambiguity as insufficient. It proposes selective original-ID
links only; no live source links, observation counts, weights or dates are changed.
Historical repair remains OPEN pending reviewed application and source-event accounting.

MEM23 extension: provenance search can also return out-of-page record IDs. The proposal
service validates exact supplied identities, retries once with concrete validation
errors, and rejects an invalid retry. Complete source coverage is required for source
attribution. Validated full-context receipts permit unchanged pages to skip model calls.
Nine isolated tests cover identity rejection, complete coverage, bounded repair,
source-content preservation, receipt integrity and context-based invalidation.

Provenance identity follow-up: some search outputs used an explicitly supplied belief key instead of its short ID. Exact unique keys now resolve to that record; duplicate historical keys remain invalid. This is identity lookup only, never semantic matching. Tests cover both unique and ambiguous keys.

MEM6 investigation limitation: source-review spot checks sometimes cited the predecessor
claim as proof of added clauses despite the prompt's warning. The copy-only evaluation
therefore independently checks proposed full support/contradiction using the complete
source without predecessor claim text. No links are applied automatically. Many current
statements also combine facts with derived assistant instructions; partial source support
is not evidence that those instructions are wrong. Keep their inferential status visible.
MEM14 historical-data extension: at least one stored manual-seed source excerpt is already
cut off mid-sentence. Passing its full stored contents cannot recover missing original text;
this remains a coverage limit rather than permission to invent the omitted source.


### MEM27. Copy-only provenance evaluator regrouped unchanged sources across restarts — FIXED in evaluation runner (P2)

The evaluation script iterated a set of candidate record IDs while building source pages.
Python hash ordering changed across processes, so identical source sets could form different
pages and miss the otherwise correct page cache. This affects the private evaluation runner,
not the nightly matching service. Candidate IDs and source IDs are now sorted. Completed
judgments were recovered by exact current-claim/source context into a per-source index,
retaining original full page receipts. Disagreements remain pending instead of selecting an
arbitrary result. Recovery made zero model calls. Future continuation must schedule only
missing indexed sources/checks, not rerun the complete archive.

The copied-data review was interrupted by API credit exhaustion: 20,159 of 20,762 source
relationships have a completed initial review; 121 of 131 claims have at least one relevant
reviewed source (often partial). Remaining: 603 initial reviews, 31 source-only checks and
155 conflicting judgments. No live evidence, weights, counts or dates were changed.


#### MEM6/MEM27 provenance continuation after credit top-up

The missing 603 source relationships and 31 source-only checks are now reviewed. The
copied baseline has 20,762/20,762 initial source assessments and relevant source proposals
for 122/131 claims (often partial). The 155 reviewer disagreements span four beliefs.
Repeating continuation schedules zero model jobs and preserves the review-index hash.
No live claim, evidence, confidence, count or date changed.

Additional source-review limitation (MEM6): a reviewed venue quote was described as a
grocery-list record, apparently importing predecessor context instead of reading the actual
source. Disagreement with another review preserved it for inspection. A source-only check
also flagged general proactive nudging as contradicting a narrower hydration cadence; the
quoted general rule does not establish that conflict. This is held for contextual review.
Positive-only source checks do not establish correctness of all negative or partial labels.
Review-policy finding: demanding separate evidence for an ordinary explicit-user-override
clause is overly literal; reasonable operational guidance must not be mistaken for an
invented factual preference. Five whole-belief cases are prepared for review, not application.


### Restart observation: missed device actions dropped as expired — OPEN (2026-09-20)

The 22:19:14 strategic_planner_wo output in logs/emi_logs_35600_agents.log abandoned
work_14776c57b4a1 (21:00 cooling/manual fallback), work_ef391b5418c9 (22:00 lights off),
and a dog-walk reminder together as expired/no longer useful. The subsequent portfolio
records the device goals dropped, not completed. Offline delay alone should not establish
that a still-useful device target is obsolete; evaluate present state and remaining benefit.
DayflowScheduler._arm_work_node_wakes can arm overdue eligible nodes for now plus a small
delay, but skips abandoned work, so scheduler catch-up cannot rescue the dropped objective.
No code or live state was changed during this log investigation.

A separate context issue preceded restart: set_nest_cooling_20260920--14776c was abandoned
with dispatch attempt 0, and replaced with manual guidance on the premise that Nest OAuth
was expired/revoked. The pre-restart projection cites home.climate.nest_integration.oauth_expired_2026_08.
This is stored belief/context, not evidence of a fresh failed Nest call tonight. Recent
reported device success makes stale capability context a concern; actual current OAuth
validity was not tested. Historical failure beliefs need reconciliation with newer successful
execution before suppressing a current device action. Preserve bounded retries.

User clarification on the restart observation: Nest OAuth really was expired. The
manual-guidance fallback was therefore appropriate; the suspected stale-capability
explanation is not supported for this occurrence. The separate missed-window abandonment
finding remains. The user reports repeated manual OAuth reauthorization as a pain point;
refresh-token handling versus provider-required reconsent has not yet been investigated.

### MEM28. KG processing batches become irreversible context boundaries — OPEN (P1)

Inspection 2026-09-20: segment_messages.py::_load_day_to_segment loads only resolved, unsegmented messages from one local date. run_one_cycle permits segmentation at six ready messages even while resolution is incomplete, or at any positive count once the resolver is caught up. Catching up is not a conversation-completion signal. Previously segmented context is excluded on subsequent passes; _persist_windows always writes related_previous_window_ids=None. resolve_messages.py also restricts overlap to ten already-resolved messages within the same date. Consequently worker timing and midnight can separate a reply from its antecedent, with no continuation repair in this path. Intended: processing pages preserve access to prior context and provisional tails; semantic conversation boundaries are model judgments. Confirmed structural limitation, not a measured live error rate.

### MEM29. Boundary output is grouped without validating its semantic range contract — OPEN (P1)

segment_messages.py::_collapse_to_windows groups individual output entries by (start ID, end ID, should_process), but actual membership is only the IDs explicitly present in those entries. It does not validate complete one-time coverage, endpoint existence/order, or consistent membership across ranges. _persist_windows replaces proposed endpoints with first/last listed members. Omitted messages remain pending and may later be processed without their earlier context; duplicated message entries can fail the membership uniqueness constraint. should_process is stored as standalone, which critique_and_extract.py never consults. A requested drop/context-insufficient decision is therefore not enforced or routed for context repair. Intended: validate identity/coverage atomically, represent context sufficiency separately from extraction eligibility, and preserve explicit context links. Code-path finding; no malformed model response injected in this audit.

### MEM30. KG critique can discard useful contextual user statements — OPEN (P1)

knowledge_graph_add/window_critic_v2/prompts/system.j2 excludes user instructions even when they contain strong autobiographical information, and calls the assistant side irrelevant. The caller supplies assistant turns as a separate block without their original interleaving. This can prevent accurate interpretation of short replies and discard facts embedded in requests. critique_and_extract.py::run_one_cycle also skips the entire window when every user line merely contains the substring '(unknown)', even if those lines contain other clear facts. Intended: interpret full ordered exchanges, distinguish factual clauses from requests, and defer only unresolved claims rather than blanket-dropping the source. Prompt-policy mismatch and deterministic rejection risk confirmed; individual semantic mistakes remain unmeasured. Its malformed dict response path can also produce a permanent rejected extraction from missing/invalid extractable_lines instead of surfacing invalid output for retry.

### MEM31. Daily insight raw-chat input omits assistant context — OPEN (P1)

ArchiveDailyTimelineStep.run selects only role=user rows for master_room and emits those raw messages into timeline_merged.json. ArchiveDailyInsightsStep passes that timeline to daily_timeline_insights without loading KG conversation windows or attaching matching assistant turns. Other normalized context/ticket entries may provide some context, but do not guarantee the exact question or proposal a short answer refers to. Thus fixing KG segmentation alone will not repair daily insight interpretation. The raw-chat query also silently caps at 10,000 rows without pagination; a larger day loses the remaining raw chats. Intended: preserve complete attributable exchanges and source identities for daily insights; page by identity without dropping source content. Inspection finding, no claim that the cap was reached live.

#### MEM14 / MEM10 follow-up: chat boundary audit (2026-09-20)

Boundary and critique persistence still truncate reasoning to 1,000/240 characters in segment_messages.py and critique_and_extract.py, contrary to the no-truncation requirement. ResolveMessagesStep's module docstring says ten new messages, while BATCH_NEW_SIZE is twenty. KG architecture eligibility still lists removed kg_maintenance_resolution and omits accepted empty room IDs. ArchiveDailyInsightsStep and ArchiveDailyTimelineStep still prepare behavioral task instructions as Python string literals rather than Jinja. MEM10's terminal extraction-error behavior remains present. SegmentMessagesStep additionally catches model exceptions and returns zero, hiding quota/provider failures from the runner's halt classification while pending_count can keep the work outstanding. These are recorded for later repair; this audit made no runtime changes or model evaluations.
#### MEM23 live extension: belief selection auto-disables two routines — OPEN (2026-09-21)

Process 35600 logs and resource_routine_status.json confirm belief_engine failed at update_beliefs at 00:31:55, 00:34:14, and 00:37:31 PDT, then auto-disabled. select_for_evidence (belief_engine/matching/service.py) rejects any returned belief_key outside the supplied catalog page with a generic ValueError. Unlike discover() in the same file, this selection path has no compact identity labels or bounded validation-repair retry. Valid pages are cached and retries advanced, but each attempt ultimately encountered another invalid selection. The logs do not identify the actual offending keys for this path; a typo versus an out-of-page reference cannot be distinguished from this error alone.

Dayflow's _select_beliefs (app/assistant/pipelines/dayflow/steps/dayflow_routine_stage.py) independently has the same fragile exact-key output contract and no validation-repair retry. Three consecutive failures at 05:00:51, 05:02:46, and 05:05:48 PDT disabled dayflow_pipeline. Logged unknown selections include home.nest.default_night_schedule_70f_9pm_75f_6am and general.work_hours.protected_window. These are absent from the supplied eligible catalog according to the validator, not proof of absence from all historical beliefs. The 05:05 selector request was logged at approximately 119,733 input tokens; prompt size is a cost/context concern, not an established cause of invalid output. Suggested repair: exact request-local ID handles, preserved complete context, bounded model correction on invalid selection, durable valid progress; never fuzzy-map semantic keys or silently discard potentially relevant selections.

Both routines use default max_failures=3, exponential backoff, disable_with_ticket, and auto_retry_after_seconds=0. The watchdog behaved as configured. The disabled dayflow_pipeline is the background context/guidance producer; dayflow_orchestrator_manager and dispatch were still active at 07:28. Daily insights completed at 00:06:57. Morning briefing was already disabled on September 13, not a new overnight shutdown.

Diagnostic follow-up: PipelineRoutineRunner reports only top-level failed_steps, while this Dayflow failure surfaces as generic 'pipeline status=error' in the status/ticket. Preserve the failed stage and underlying validation reason in user-facing routine diagnostics. Investigation only: no re-enable, code repair, database writes, or provider calls performed.
#### MEM23 selector repair — FIXED on disk; runtime recovery pending restart (2026-09-21)

Both evidence_match and dayflow_belief_selector now return request-local selection_id handles in belief_ids, mapped exactly to the supplied original records by belief_engine/matching/selection.py. No semantic key guessing or text truncation. Invalid schemas/IDs receive one correction request carrying the complete original input, prior response, validation error, and allowed IDs; repeated invalid results still fail explicitly. Provider errors propagate. Prompts and correction instructions live in each agent's Jinja templates; both config files declare selection_retry. Evidence-selection cache identity was versioned and includes the output schema; only valid canonical-key results are cached.

Verification: 19 focused tests passed, including both production callers, invalid-ID correction, persistent invalid response rejection, full qualifier preservation, and zero-call cache reuse. Isolated real-model checks through the configured provider succeeded: Dayflow selected 29 valid records from the complete 579-entry exported catalog; evidence matching selected 10 valid records from an 84-entry catalog page with copied original evidence. These checks prove the selection paths execute successfully, not correctness of every relevance judgment or completion of a full nightly pipeline. No live belief writes or routine execution occurred. Tests use isolated service registration rather than live bootstrap.

The running app process still has the old Python modules loaded. Leave the two routines disabled until restart loads the matching code/schema/prompts together; then enable them using RoutineManager's supported admin route and verify full runs. No restart, live routine toggle, commit, or push performed here. Generic Dayflow error-message propagation remains a separate OPEN diagnostic follow-up.
#### MEM25 live extension: updater reassembles paged selection into an oversized request — OPEN (P1, 2026-09-21)

After the MEM23 selector repair, live run d62cc274 reached belief_engine::belief_updater at 07:59:37 PDT in process 22080. The client estimated 910,365 input tokens and the provider rejected the request with context_length_exceeded at 07:59:43. update_beliefs.py::run serializes the entire EvidenceBundle plus encode(select_for_evidence(...)) into one updater call. select_for_evidence pages discovery, then returns full packet() records for every selected belief, including complete lineage/evidence. Discovery paging therefore does not bound the final updater prompt. Existing paged comparison review does not cover this production update path. The failed call occurred before this step's mutation loop; selection cache writes may already exist. Dayflow routine run 3d50a3d5 succeeded at 07:59:29, so this is a distinct downstream belief-update failure, not evidence that local selection IDs failed.

Required repair: bounded update investigations with complete source coverage, original evidence-reference mapping, preserved cross-page qualifiers/contradictions, and final whole-belief adjudication before mutation. Do not truncate evidence, drop selected beliefs, or naively perform destructive updates independently per page. Verify complete nightly flow on an isolated database copy, including updater, reconciliation and export, before claiming recovery. The preceding 19 tests and isolated selector evaluations did not cover updater prompt assembly at production scale. No runtime fix made in this investigation.

#### MEM25 updater repair follow-up — code installed; copy stages validated (2026-09-21)

select_for_evidence now returns complete current records without automatic packet()
history hydration. UpdateBeliefsStep batches whole evidence records chronologically,
uses batch-local citations, reloads current selection between batches, and applies a
pre-call input-size guard with recursive whole-record subdivision. Single-source
overflow remains explicit pending work; no clipping or omitted candidates. Source-aware
pair reconciliation still reads historical evidence. Dayflow relevance selection now
pages the complete current catalog, preserving the same day context on every page.
The hour-slot writer receives the selected union. Twenty-six focused tests passed.
The copied 39-source workload completed updater and snapshot stages: two updater calls
estimated at 42,438 and 21,413 tokens, compared with the previous failed 910,365-token
request. Remaining nightly stages are being exercised before reporting recovery.

### MEM32. Dayflow writer failure can be reported as a successful step — FIXED (P2)

Read during the projection audit: DayFlowRoutineStep.run returns
StepResult(output={status:error,...}) when _call_agent returns no markdown. The common
step_runner.run_once only applies state_updates and records success for any non-raising
step, ignoring output.status. Thus a failed writer can advance step scheduling and appear
healthy without refreshed guidance. Intended: propagate an explicit stage failure.
This is an inspected contract defect, not evidence that the observed 07:59 successful
Dayflow run actually took this failure branch. Fixed: an empty/failed writer now raises before state updates. A regression covers the failure contract.


#### MEM25 copy validation: oversized final pair findings (2026-09-21)

The full copied nightly workload passed update, recompute and reevaluate, then found
another context boundary: complete evidence-page findings for one pair exceeded the
160,000-character final-review budget. investigation.review_pair correctly refused to
truncate, but service.run let that exception abort canonicalization and prevent export.
Fixed by a dedicated ReviewBudgetExceeded exception and a version/policy/budget-scoped
pending receipt. Both beliefs remain intact; other pairs and export continue. Unchanged
blocked pairs spend no new model calls. Invalid model output and other failures still
propagate. Result stats expose blocked_review_budget and pending_comparisons. This does
not resolve the oversized pair: a larger-evidence investigation strategy remains TODO.
The focused regression suite now passes 58 tests, including the blocked-pair continuation,
unchanged retry and changed-source reopening cases. Copy validation resumed at the failed
canonicalization stage rather than replaying already committed upstream updates.


#### MEM25 final copy-validation outcome (2026-09-21)

- 58 focused regression tests passed. No source clipping was introduced.
- Production collector supplied 39 sources. Real updater calls used approximately
  42,438 and 21,413 input tokens; 2 created, 6 updated, 9 no-change, 0 errors.
- Snapshot recomputation and reevaluation completed (12 qualified, 16 confirmed,
  0 errors). Dayflow separately read all 579 existing exported beliefs in pages,
  selected 89 and produced 10,558 characters of cited hourly guidance.
- The first full-copy run failed at the final pair-findings budget boundary described
  above. After the fix, resumed comparisons progressed. The broader comparison run
  was deliberately stopped; a final continuation with discovery_limit=0 and
  comparison_limit=1 exercised the pending-budget branch and normal export.
- Export succeeded: 599 unique active belief keys. Across copy comparison receipts:
  1 same/approved merge, 1 contradiction, 30 different, 6 specialises, 2 unresolved,
  6 stale, 1 budget-blocked and 96 not yet decided. Final matching result remains
  pending: 561 discovery items and 97 comparisons. This is not a completed dedupe sweep
  or an uninterrupted full-default-budget nightly success claim.
- Evaluation used the real configured models, isolated SQLite backup, an in-memory
  Chroma client and isolated export paths. Bootstrap omitted canonical identity
  resources. Live beliefs and exports were unchanged; runtime restart is still needed
  to load the installed code. No commit/push or routine re-enablement was performed.


### Chat tab takeover fails to disconnect the displaced socket — FIXED (2026-09-21)

Observed in live PID5944 logs at 09:38:16 and 09:38:54: chat registration binds the
new socket, then app/socket_handlers.py calls socketio.disconnect(displaced).
The Flask-SocketIO instance has no such method, raising AttributeError and leaving
the old transport connected. The preceding socket_hijacked notification can succeed;
the warning's wording did not establish notification failure or duplicate delivery.
Fixed by calling socketio.server.disconnect with the displaced SID and request namespace.
The disconnect handler uses unbind_by_socket_id; bind already removed the displaced
SID's reverse mapping, so cleanup preserves the newly registered room owner.
Regression uses two real Flask-SocketIO test clients and the production handlers. It
reproduced the old connected socket before the fix, then checks disconnection,
takeover notification, delivery to the new client, repeated registration and final cleanup.
Validation: all 10 socket ownership tests passed. No live app restart or push performed.
The test run also exposed asynchronous logging writing to pytest capture after it closed
(ValueError in UnicodeStreamHandler.emit); this is a separate test teardown issue,
left open. A requests dependency-version warning also appeared; no HTTP failure was observed.


#### MEM8 controlled wrong-answer demonstration — 2026-09-21

Reproduced with fictional KG retrieval rows and the unmodified production global
handle_ask_kg evidence builder plus configured shared::ask_kg model (gpt-5.6-luna).
Ground truth: vacation state valid September 1–5, 2026; edge maintenance timestamp
September 21; question asks whether the vacation applies on September 21.
Current builder omits node validity dates. Real answer: "Yes. The evidence states
that Morgan Vale is on vacation and unavailable for work meetings. The record was
updated on September 21, 2026." Model confidence: 1.0.
Second call used the identical evidence plus the stored node start_date/end_date,
without changing the answering prompt. It correctly identified the expired vacation
and noted that no separate current unavailability was established (confidence 0.94).
This is a controlled synthetic reproduction, not a demonstrated wrong answer from
live personal KG records. Search/SQL retrieval used fixed fixture rows; payload
construction and model execution were real. Neither production code nor live data
was changed. Original payloads and responses are saved in the task workspace under
kg-validity-demo/. The paired calls demonstrate the failure mechanism, not a statistical
reliability estimate or proof that every expired fact produces a wrong answer.


#### MEM8 repair and verification — 2026-09-21

Both ask_kg builders now use a shared node projection including exact/prose validity
dates, date confidence, observation timestamps/count, valid_currently, validity_reason,
goal_status and claim confidence/tier. Edge maintenance timestamps and confidence are
explicit. The anchored base-node projection uses the same fields. Full descriptions
are preserved instead of the former 400-character truncation. No time-based filtering
was added: answering semantics are in shared/ask_kg Jinja prompts. A record edit is not
reconfirmation; missing dates do not establish current validity; an expired state does
not prove its opposite; decay closure is distinguished from a confirmed ending.
The static task instruction moved out of Python into the user Jinja template.
Eight regression cases cover both retrieval modes with expired, future, uncertain and
undated nodes, including complete descriptions and confidence/closure preservation.
All passed. The original real-model fictional vacation example was rerun through the
installed fixed builder and prompt: it now states that the record does not establish
current vacation/unavailability and explicitly rejects updated_at as proof of current
validity. This is a synthetic model check, not an exhaustive answer-quality guarantee.
No schema migration or KG content rewrite is needed; restart loads the Python changes.
