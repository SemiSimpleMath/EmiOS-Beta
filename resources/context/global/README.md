## `resources/context/global/`

Global context (non-user-specific) resources.

Example: `resource_weather.json`.

Keyword-triggered room resource subscriptions live in:
- `resource_subscriptions.json`