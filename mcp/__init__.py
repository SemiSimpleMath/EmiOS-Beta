"""
MCP support scaffolding (metadata + validation).

This package currently contains:
- Canonical schema files under `mcp/schemas/`
- Curated server entries under `mcp/servers/`
- A validator (`python -m mcp.validate_servers`) to keep entries consistent
"""

