Read a work object as text — its goal, every step + status, and all results/findings — so you can answer questions about it or reuse its content in another task. Read-only. If you don't have the work_id, call search_work_objects first.
