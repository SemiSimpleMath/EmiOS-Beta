Notify the OWNER one-way about a work-object node (a UI notification, NO reply, never blocks). Give it the work_id and the notify node_id; it folds in the node's upstream results off the graph and phrases a warm heads-up. This is how a work object reaches the owner — never send_email for the owner.
