Compile natural-language task requests into workflow IR (`task_ir_v1`) via the task compile manager.
Use this when the user explicitly asks to compile/convert/create a task workflow/spec output,
not when they want to execute a task immediately.
