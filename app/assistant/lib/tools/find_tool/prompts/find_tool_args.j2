Arguments are:
query the string to try to match to the tool name or description.
tool_kind = all | local | installed_mcp | mcp_available (default all). Enforce one of these. If wrong argument given force all.
limit = how many results to display (default 8, max 200).
