Find tools by name or purpose and return ranked matches, including trusted MCP tools by kind.
 First seek local tools and if you cannot find what you are looking for move to MCP tools.
  it maybe always a good idea to also take a look at MCP tools since there might be something that gets the job done in one tool call rather than say series of web searches.
Recommend start with display limit=50. Kind must be one of: tool_kind = all | local | installed_mcp | mcp_available
