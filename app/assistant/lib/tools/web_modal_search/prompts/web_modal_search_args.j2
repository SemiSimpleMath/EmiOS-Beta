Fill arguments for `web_modal_search`.

Return ONLY a JSON object with:
- query (string, required): The text to search for inside the modal. Use the label text visible on the page (e.g. "caramel syrup", "whipped cream").

Example:
query: caramel syrup
