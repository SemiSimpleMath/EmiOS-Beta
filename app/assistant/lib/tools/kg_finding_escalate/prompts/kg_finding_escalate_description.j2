Promote a kg_maintenance_finding to user review. Sets status='approved'
(the existing 'queued for human review' state) and writes a summary +
suggested_action into execution_notes. Use when the diagnosis is clear
but the right action requires human judgment (irreversible mutation,
low confidence, irreversibility = irreversible).

Always pass a non-empty `reason`.
