Specialized KG manager — investigates and mutates the live emi.db knowledge graph. Use whenever a task involves the KG (find/inspect nodes, follow provenance, merge duplicates, rename labels, close states, create/delete edges, resolve findings). emi_team and master_room should delegate KG work here rather than reasoning about the KG themselves. Pass a fully disambiguated instruction; for mutations include concrete node ids and a reason.
