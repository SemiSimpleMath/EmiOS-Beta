Read-only KG investigation + targeted mutation surface for the dev console. Runs a planner loop over kg_query, pod_search, pod_fetch and the typed KG mutators (kg_merge_nodes, kg_create_edge, kg_create_state_node, kg_close_state, kg_rename_label, kg_update_node_field, kg_delete_edge). Pass a fully disambiguated instruction; for mutations, include concrete node ids and a reason.
