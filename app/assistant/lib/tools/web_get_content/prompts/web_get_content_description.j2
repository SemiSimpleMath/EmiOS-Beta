web_get_content:
Extract readable text content from the current browser page.

Use this for reading, research, and content extraction — not for navigation.
Returns the page's visible text with scripts, styles, and nav elements removed.
Ideal for extracting headlines, article text, prices, or any textual data from a page already loaded in the browser.

Arguments:
- max_chars: maximum characters to return (default 15000)
