Capture a single high-resolution frame from a LAN-side RTSP camera (Tapo, Wyze, Reolink, etc.). Use this when Ring's 640×360 thumbnail is not enough resolution and a local camera is configured. The frame is saved to disk and an event fires that downstream analyzers (sleep_analyzer, etc.) pick up automatically.
