{
  "name": "get_news",
  "description": "Fetch recent news articles based on query/topic inputs and return structured results.",
  "inputs": [
    {
      "name": "feed_urls",
      "type": "array",
      "required": true,
      "description": ""
    }
  ],
  "outputs": [
    {
      "path": "content",
      "type": "text",
      "description": "Human-readable tool result text."
    },
    {
      "path": "data",
      "type": "object",
      "description": "Structured tool result payload."
    }
  ],
  "metadata": {
    "min_authority": 10,
    "approval_min_authority": 10,
    "domain": "news",
    "actions": [
      "read",
      "fetch"
    ],
    "selectors": [
      "feed",
      "query"
    ],
    "risk_level": "low",
    "side_effects": "read_only",
    "requires_auth": [],
    "requires_network": false,
    "cost_level": "low",
    "latency_class": "fast",
    "front_door": false,
    "room_visibility_default": "conditional",
    "approval_required": false
  },
  "arguments_prompt": "Arguments for get_news:\n- feed_urls (required): list of RSS/Atom feed URLs to pull headlines from. Pass the user's configured feeds when known; each URL is fetched and parsed independently."
}
