- **Purpose**: Load a job spec file and run it through an orchestrator.
- **Arguments**:
  - `job_file` (str, required): Path to the job spec markdown file.
  - `orchestrator` (str, optional): Orchestrator type to use (default: orchestrator_test).
