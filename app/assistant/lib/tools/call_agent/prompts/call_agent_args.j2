Fill arguments for `call_agent`.
Return a JSON object with:
- agent_name (string, required): Name of the agent to call.
- task (string, optional): Instruction for the agent.
- inputs (object, optional): Named inputs (artifacts, facts) for the agent.

agent_name: str
task: str
inputs: dict