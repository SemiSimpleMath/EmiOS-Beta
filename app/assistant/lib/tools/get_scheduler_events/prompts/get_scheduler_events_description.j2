Fetch time-based reminder alerts (APScheduler), not calendar events or meetings. Returns active reminders by default, or filter with optional start_date and end_date in ISO 8601; prefer a one-week window starting today, or a single day when only that date is needed.