Edit a Google Docs document using a diff-style approach. Operation 'diff': provide a find block (exact text to locate, with enough surrounding lines to be unique) and replace_with (the replacement). Returns AMBIGUOUS error with context excerpts if find matches multiple locations - extend the find block using those excerpts and retry. Operation 'append': add text to the document end.