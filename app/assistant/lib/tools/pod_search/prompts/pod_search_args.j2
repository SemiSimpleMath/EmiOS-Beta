Arguments for pod_search tool:

- tags (optional): list of tag names (OR match). Example: ['food']
- scope (optional): room_id filter (e.g. 'master_room')
- since (optional): '24h' / '3d' / '2w' / '1m' / 'today' / ISO timestamp
- limit (optional): max headers to return, default 20
