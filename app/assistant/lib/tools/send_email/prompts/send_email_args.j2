Arguments for sending an email:
to: str
subject: Optional[str]
body: Optional[str]
pod_ids: Optional[List[str]]  # Pod URIs to attach as files (e.g. ['datapod:image:abc...']). Find pod_ids first via pod_search.
