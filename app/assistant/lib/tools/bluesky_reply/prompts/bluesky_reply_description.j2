Reply to a Bluesky post by its ref (e.g. b1, from bluesky_timeline) with your reply text (max 300 characters). The reply is attached to exactly the post you name by ref — you never copy a URL, so the reply cannot land on the wrong post. Make sure the ref you pass is the post your text is actually about.
