Delete one relationship (edge) from the knowledge graph. Does NOT
delete the endpoint nodes.

Audited: requires a non-empty `reason`; refuses user-locked edges and
edges whose endpoint nodes are user-locked; snapshots the edge into
kg_revision_log so the deletion can be reverted. Pass `dry_run: true`
to preview without committing.
