Use kg_delete_edge when you need to:
- Remove an incorrect or invalid relationship
- Delete duplicate edges between the same nodes
- Clean up relationships that are no longer relevant

This is safer than kg_delete_node because it only removes the connection, not the entities themselves.
