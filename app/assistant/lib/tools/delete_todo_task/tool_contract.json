{
  "name": "delete_todo_task",
  "description": "This is a tool for deleting todo tasks.  It needs a todo task id and optionally task_list_id for efficiency.",
  "inputs": [
    {
      "name": "task_id",
      "type": "string",
      "required": true,
      "description": "The todo task ID to delete"
    },
    {
      "name": "cascade",
      "type": "boolean",
      "required": false,
      "description": "If True, also delete all linked children (reminders, sub-events) from the event hierarchy"
    }
  ],
  "outputs": [
    {
      "path": "content",
      "type": "text",
      "description": "Human-readable tool result text."
    },
    {
      "path": "data",
      "type": "object",
      "description": "Structured tool result payload."
    }
  ],
  "arguments_prompt": "## delete_todo_task\n\nDelete a todo task from Google Tasks.\n\n### Arguments:\n\n- **task_id** (required): The todo task ID to delete\n\n- **cascade** (optional, default: false): If true, also delete all linked children \n  (reminders, sub-tasks) from the event hierarchy. Use this when deleting a task \n  that has related reminders or sub-tasks that should also be removed.\n\n### Example without cascade:\n```json\n{\n  \"tool_name\": \"delete_todo_task\",\n  \"arguments\": {\n    \"task_id\": \"task_abc123\"\n  }\n}\n```\n\n### Example with cascade (deletes task AND all linked reminders):\n```json\n{\n  \"tool_name\": \"delete_todo_task\",\n  \"arguments\": {\n    \"task_id\": \"task_abc123\",\n    \"cascade\": true\n  }\n}\n```",
  "metadata": {
    "domain": "todo",
    "actions": [
      "delete"
    ],
    "selectors": [
      "task_id"
    ],
    "risk_level": "high",
    "side_effects": "destructive",
    "requires_auth": [
      "google"
    ],
    "requires_network": true,
    "cost_level": "low",
    "latency_class": "fast",
    "front_door": false,
    "room_visibility_default": "hide",
    "approval_required": true
  }
}
