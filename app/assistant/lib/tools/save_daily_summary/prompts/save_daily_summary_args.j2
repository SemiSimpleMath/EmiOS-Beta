Fill arguments for `save_daily_summary`.

Return ONLY a JSON object with:
- summary_data (object, required — the daily summary dict)
- date_str (string, optional — YYYY-MM-DD, defaults to today)

Example:
{"summary_data": {"narrative": "...", "schedule": []}, "date_str": "2026-04-15"}
