Fetch important inbox emails for a time window. Each message is scored 0-10 by an LLM; only importance >= 5 messages are returned. Spam, bulk, and low-priority mail is excluded automatically.
Results are ephemeral - nothing is stored.
For query-driven search, use get_email_messages. For full thread context, use get_email_thread.
