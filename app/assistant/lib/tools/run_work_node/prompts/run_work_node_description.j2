Execute ONE work-object node through the work-object runtime (work_on) — sets the work context, runs work_emi_team, records the result on the graph. The switchboard's WORK client: route a ready WORK node (not a notification) here with its work_id + node_id.
