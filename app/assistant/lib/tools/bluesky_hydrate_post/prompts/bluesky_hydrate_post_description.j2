Open a single Bluesky post by its ref (e.g. b1, from bluesky_timeline) on its own, with its image shown to you directly when it has one. Use this before replying to a post that has an image or quotes another post, so you react to what is actually there rather than guessing. Returns the post's full text and, if present, the image itself plus its alt text.
