Personal-assistant manager that runs tightly-scoped shell commands on the user's local machine. Use whenever a task involves local filesystem inspection — listing folders/files, finding a specific file, reading a file's contents — anywhere under the user's allowed paths (v1: ~/Documents). master_room and emi_team should delegate filesystem work here rather than reasoning about it themselves.
