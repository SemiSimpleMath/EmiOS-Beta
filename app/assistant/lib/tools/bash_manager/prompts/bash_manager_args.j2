bash_manager arguments:
- task: the disambiguated filesystem request, e.g. "List the top-level folders in ~/Documents".
- information: supporting context (prior chat, constraints). Empty string allowed.
