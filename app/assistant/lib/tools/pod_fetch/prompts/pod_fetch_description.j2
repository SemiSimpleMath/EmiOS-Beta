Fetch the FULL body of one or more pods by pod_id. Use this after
pod_search to open the pods you've decided are worth reading.

Returns each pod's header plus its body, source_refs, and metadata.
Pod_ids that don't exist come back in the 'missing' list — no error.
