Run a compiled task (a work object) by file or task name; a legacy `task_ir_v1` file is refused (recompile it first).

Use this when the user asks to run/execute a compiled task, or to resume a paused run (pass run_id, optionally event_name).
