Today is {{ date_time }}.

Task:
{{ task }}

{% if information %}
Information:
{{ information }}
{% endif %}

{% if web_summary_trigger_reason %}
Summary trigger reason: {{ web_summary_trigger_reason }}
{% endif %}

{% if summary_payload %}
Summary payload:
{{ summary_payload }}
{% endif %}

Create action lists over history ids.
Use summary_pairs for entries to compress (objects with "history_id" and "summary").
Use hide/unhide/pin/delete lists for visibility/lifecycle controls.
Prefer summarizing older ids first.
Prefer ids in `unsummarized_ids` before touching `already_summarized_ids`.
Do not prioritize summarizing summaries; only re-summarize when a prior summary is clearly missing key evidence.
If an item is especially useful evidence, keep it visible (pin/unhide) rather than hiding it.
For each summary, carry over important evidence links (URLs) and key numbers.
