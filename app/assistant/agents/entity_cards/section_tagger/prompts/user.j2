## Entity descriptor

{{ entity_descriptor }}

## Available sections (pick from these, or 'reject')

{{ section_list }}

## Candidate facts to classify

{{ candidate_facts }}

## Your job

Return one TaggedFact per fact_id above. Use 'reject' for facts that
belong on the wiki (closed states, past events that aren't definitional,
abandoned goals).
