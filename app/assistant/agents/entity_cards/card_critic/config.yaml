name: entity_cards::card_critic
class_name: Agent
llm_params:
  engine: gpt-5-mini
  model_tier: "mini"
  llm_provider: "openai"
allowed_tools: []
except_tools: []
user_context_items:
- card_brief
system_context_items: []
type: agent

# Judgment agent: blank input looks like conservative judgment to an
# LLM, not an error — refuse loudly instead (2026-07-08 guard sweep).
required_context_items:
  - card_brief
