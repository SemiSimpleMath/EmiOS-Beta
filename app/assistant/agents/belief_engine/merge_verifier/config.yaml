name: belief_engine::merge_verifier
class_name: Agent
action_required: false
llm_params:
  llm_provider: "openai"
  engine: "gpt-5.2"
  model_tier: "strong"

allowed_tools: []
except_tools: []
allowed_nodes: []
entity_scan_keys: []
entity_card_level: 0

user_context_items:
  - phrase_a
  - phrase_b
  - ctype
  - ctype_noun

system_context_items: []

# Judgment agent: blank input looks like conservative judgment to an
# LLM, not an error — refuse loudly instead (2026-07-08 guard sweep).
required_context_items:
  - phrase_a
  - phrase_b
