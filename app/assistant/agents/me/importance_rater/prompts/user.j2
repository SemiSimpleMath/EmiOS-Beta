User: {{ resource_user_data.full_name if resource_user_data else "the user" }}

Rate each of these KG nodes 0-10 by importance to the user.

{{ task }}

{%- if information %}

{{ information }}
{%- endif %}
