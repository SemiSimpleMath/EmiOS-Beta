## Proposed Edge Predicate to Verify

**Proposal**: {{ proposal_text }}
**Domain Type**: {{ domain_type }}
**Range Type**: {{ range_type }}

## Existing Canonical Predicates (in same domain->range bucket)

{% if candidates and candidates|length > 0 %}
{% for candidate in candidates %}
{{ loop.index }}. **{{ candidate.text }}** (ID: {{ candidate.id }})
{% endfor %}
{% else %}
No existing canonical predicates in this bucket.
{% endif %}

## Policy Constraints

{{ policy }}

---

**Task**: Decide if the proposal should map to an existing canonical predicate, create a new canonical, or be queued for human review.

Consider:
- Is the proposal semantically equivalent to any existing canonical?
- Is it the inverse direction of an existing canonical?
- Does it represent a distinct relationship that warrants a new canonical?
- Is there significant uncertainty requiring human review?

Return: decision, target_id (if mapping), confidence, reason

