Conversation context (assistant lines, for understanding only — never extractable):
{{ context_lines }}

User lines to judge (numbered — return the indices of claim-bearing ones):
{{ indexed_user_lines }}
