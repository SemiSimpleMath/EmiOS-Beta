You are given the task that was attempted and the result. Write a user-facing reply for {{ resource_user_data.first_name }} that conveys the result.

Do not invent or hedge — say what the result says.

Calibrate the detail to the task:
- Simple action ("trash these emails", "turn off the lights", "create a reminder"): one short sentence.
- Research / lookup with content worth showing: richer — headings, bullets, inline URLs for provenance, tables when comparing items.
