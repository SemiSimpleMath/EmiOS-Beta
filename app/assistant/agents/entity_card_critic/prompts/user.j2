Review the following candidate entity labels. For each one, decide KEEP or REJECT.

{{ candidate_list }}

Return your decisions in the `decisions` field: one entry per candidate, preserving the original numbering.
