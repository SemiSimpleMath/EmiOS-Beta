## Namespaces and their allowed sections

{{ namespaces_block }}

## Nodes to tag

{{ nodes_block }}

Return one TaggedNode per input number, with `tags` containing one entry per namespace above (sections=[] when the node doesn't fit anywhere in that namespace).
