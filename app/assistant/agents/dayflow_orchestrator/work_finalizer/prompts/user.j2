Current time: {{ date_time }}.

A node in this work object just COMPLETED. The work object's projection:

{{ task }}
{% if information %}

{{ information }}
{% endif %}
Decide the single verdict (proceed / amend) for how this node's result bears on the plan. Default
PROCEED unless the result genuinely changes it (AMEND — give the revised intent). If it settles or moots
the GOAL, that goes in `reasoning` for the steward, not into a verdict of your own.
