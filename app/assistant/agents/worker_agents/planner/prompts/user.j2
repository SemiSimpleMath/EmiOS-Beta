Today is {{ date_time }}.

{{ work_projection }}

## What you have already done on this node (your prior turns + their tool results)
{% if recent_history %}{{ recent_history }}{% else %}(this is your first turn on this node){% endif %}

{% if action_count %}**Action count:** {{ action_count }}/20 (you have {{ 20 - action_count }} left){% endif %}

Decide your next single action. Update your `checklist` from the projection + the latest tool
result, then choose ONE action. If every checklist item is done/abandoned and the deliverable is
produced, `return_control` now — do not repeat finished work.
