name: room::response_formatter
class_name: Agent
action_required: false
llm_params:
  llm_provider: "gemini"
  engine: "gemini-3-flash-preview"
  model_tier: "mini"

allowed_tools: []
except_tools: []

user_context_items:
  - task
  - information
  - tool_result_for_formatter

system_context_items:
  - room_participant_facts
  # Slack uses its own mrkdwn dialect — skill loads always; agent applies
  # the substitutions only when room_surface == "slack".
  - resource_slack_format
