name: room::response_formatter
class_name: Agent
action_required: false
llm_params:
  llm_provider: "gemini"
  engine: "gemini-3-flash-preview"
  model_tier: "mini"

allowed_tools: []
except_tools: []

user_context_items:
  - task
  - information
  - tool_result_for_formatter

system_context_items:
  - room_participant_facts

# Skills (agentskills.io SKILL.md format). Loaded by SkillRegistry,
# exposed in templates via {{ skills["<name>"] }}.
skills:
  - slack-formatting
