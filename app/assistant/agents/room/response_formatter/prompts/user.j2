User asked: {{ task }}
{%- if information %}

Context: {{ information }}
{%- endif %}

Raw result:
{{ tool_result_for_formatter }}
{%- if accumulated_pod_references %}

Saved findings (pods — the full research/work behind this answer is stored in these):
{%- for p in accumulated_pod_references %}
- {{ p.pod_id }} — {{ p.one_liner }}
{%- endfor %}
{%- endif %}

Format this as a response for the user.
