Today is {{ date_time }}.

{{ work_projection }}

{% if entity_card %}
## Entity context
{{ entity_card }}
{% endif %}

## What you have already done on this node (your prior turns + their tool results)
{% if recent_history %}{{ recent_history }}{% else %}(this is your first turn on this node){% endif %}

{% if action_count %}**Action count:** {{ action_count }}/20 (you have {{ 20 - action_count }} left){% endif %}

Decide your next single action. Update your `checklist` from YOUR CHECKLIST above + the latest tool
result (echo existing items by id+text, add new milestones with an empty id), then choose ONE action.
If every milestone is done/abandoned and the result is in `findings`, `return_control` now — do not
repeat finished work.
