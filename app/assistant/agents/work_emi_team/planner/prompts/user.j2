{{ work_projection }}

{% if entity_card %}
## Entity context
{{ entity_card }}
{% endif %}

## What you have already done on this node (your prior turns + their tool results)
{% if recent_history %}{{ recent_history }}{% else %}(this is your first turn on this node){% endif %}

{% if action_count %}**Action count:** {{ action_count }}/20 (you have {{ 20 - action_count }} left){% endif %}

Today is {{ date_time }}.

Decide your next single action. Update `checklist` from YOUR CHECKLIST above — echo existing surfaces by
their id+text, add new ones with an empty id, and mark exactly ONE `in_progress`. Then choose ONE action:
delegate the in_progress surface (it nests under that item), or — if every surface is `done`/`abandoned`
and the result is in `findings` — `return_control`. Do not repeat finished work.
