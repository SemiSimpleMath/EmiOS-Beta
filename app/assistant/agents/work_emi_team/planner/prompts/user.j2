Today is {{ date_time }}.

{{ work_projection }}

{% if entity_card %}
## Entity context
{{ entity_card }}
{% endif %}

## What you have already done on this node (your prior turns + their tool results)
{% if recent_history %}{{ recent_history }}{% else %}(this is your first turn on this node){% endif %}

{% if action_count %}**Action count:** {{ action_count }}/20 (you have {{ 20 - action_count }} left){% endif %}

Decide your next single action. Keep `checklist` updated as your private running plan of the remaining
surfaces (YOUR CHECKLIST / THE WORK TREE above already shows the delegations that returned, with their
results). Then choose ONE action: delegate the next surface, or — if every surface is done and the result
is in `findings` — `return_control`. Do not repeat finished work.
