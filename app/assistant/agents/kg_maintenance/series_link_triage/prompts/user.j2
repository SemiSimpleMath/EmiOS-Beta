Triage the following batch of candidate event-series clusters. For each, decide whether the events are instances of a real recurring concept that deserves a parent Entity, or whether the cluster is generic-label noise / unrelated events sharing a label.

{{ clusters_batch }}

Return one verdict per cluster, identified by `cluster_index`.
