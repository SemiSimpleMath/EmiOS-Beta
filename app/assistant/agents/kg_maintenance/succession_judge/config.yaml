name: kg_maintenance::succession_judge
class_name: Agent
action_required: false
llm_params:
  llm_provider: "openai"
  engine: "gpt-5.4-mini"
  model_tier: "fast"

# Pure judgment: one role-reference entity's evidence in, a
# stable / succession / unclear verdict out. No tools.
allowed_tools: []
except_tools: []
allowed_nodes: []
accept_auto_skills: false

user_context_items:
  - agent_input

system_context_items: []
