name: kg_maintenance::duplicate_cluster_resolver
class_name: Agent
llm_params:
  llm_provider: "openai"
  engine: "gpt-5.4"
  model_tier: "powerful"
allowed_tools: []
except_tools: []
user_context_items:
  - cluster_brief
system_context_items: []
type: agent

# Judgment agent: blank input looks like conservative judgment to an
# LLM, not an error — refuse loudly instead (2026-07-08 guard sweep).
required_context_items:
  - cluster_brief
