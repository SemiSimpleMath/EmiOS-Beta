Adjudicate this candidate duplicate cluster. Decide which nodes (if any) are genuinely the same thing and should merge, and which must be kept separate.

{{ cluster_brief }}

Return merge_groups + distinct_node_ids covering every node id above exactly once.
