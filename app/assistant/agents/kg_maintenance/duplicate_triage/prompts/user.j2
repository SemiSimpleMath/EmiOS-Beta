Triage the following batch of candidate duplicate pairs. For each pair, decide whether the two nodes are confidently `distinct` (drop) or you need the downstream detector to look more carefully (`uncertain`).

{{ pairs_batch }}

Return one verdict per pair, identified by `pair_index`.
