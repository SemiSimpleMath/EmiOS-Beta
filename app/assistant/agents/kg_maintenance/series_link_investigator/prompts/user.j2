Investigate this candidate event-series cluster and decide whether to approve the link or reject it.

## Cluster brief

{{ cluster_brief }}

## Existing parent Entity (if any)

{{ existing_entity_brief }}

Return an `AgentForm` with verdict, canonical_label (when approving), reasoning, cross_instance_participants, and cadence_note.
