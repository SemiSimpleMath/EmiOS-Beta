name: wiki::fact_question_writer
class_name: Agent
action_required: false
llm_params:
  llm_provider: "openai"
  engine: "gpt-5.6-luna"
  model_tier: "fast"

# Pure judgment: a synthetic-fact proposal in, worthiness verdict + a
# natural confirmation question out. No tools.
allowed_tools: []
except_tools: []
allowed_nodes: []
accept_auto_skills: false

user_context_items:
  - agent_input

# Prompt guard: judged blank input undetected until 2026-06-12 —
# refuse loudly instead.
required_context_items:
  - agent_input

system_context_items: []
