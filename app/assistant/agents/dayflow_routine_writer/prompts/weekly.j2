Historical weekly context: {{ weekly.week_start }} through {{ weekly.week_end }}.
Weekday references belong to that dated period. The patterns below are interpretations,
not new standing instructions or proof that the underlying events are happening today.
{% set insights = weekly.insights | default({}) %}
{% if insights.sleep_pattern %}Sleep: {{ insights.sleep_pattern }}{% endif %}
{% if insights.health_pattern %}Health: {{ insights.health_pattern }}{% endif %}
Weekly belief candidates are reconciled through the belief engine. Use the supplied
belief catalog for those claims; this historical context does not add standing instructions.
