!! INCOMPLETE VIEW — {{ unreadable|length }} active work object(s) could not be read ({{ unreadable|join(', ') }}). Work exists that is NOT listed below. Do NOT conclude that an objective is uncovered; prefer changing or re-planning existing work over creating any new work this cycle.

{{ portfolio }}