The user said: "{{ user_message }}"

Below is the Knowledge Graph convergence briefing — the most relevant nodes and connections linking {{ seeds }}.

{{ kg_briefing }}

Key people: {{ visitor_label }} and {{ primary_user }}.
