name: web_manager
class_name: MultiAgentManager
description: "A team manager that controls team of web search agents."
display_name: "Webby"
max_cycles: 120
role_bindings:
  delegator: web::delegator
  tool_selector: shared::tool_selector
  planner: web::planner

agents:
  - name: web::delegator
    class: Delegator
  - name: web::planner
    class: Planner
  - name: web::critic
    class: PlaywrightAgent
  - name: web::web_summary
    class: Agent
  - name: shared::final_answer_lite
    class: Agent
  - name: shared::mathematician
    class: Agent

control_nodes:
  - name: tool_return_router
    class: ToolReturnRouter
  - name: critic_pre_node
    class: CriticPreNode
    config:
      enabled: true
      critic_agent: "web::critic"
      continue_agent: "tool_caller"
  - name: critic_post_node
    class: CriticPostNode
    config:
      critic_agent: "web::critic"
  - name: summary_pre_node
    class: SummaryPreNode
    config:
      enabled: true
      summary_agent: "web::web_summary"
      resume_agent: "web::planner"
      planner_agent: "web::planner"
      # Tuned for research loops: scrapes are legitimately large, so the
      # large-result trigger sits above a normal page rather than at the
      # 1000-char default (which would summarize after every scrape), and
      # the three freshest results stay verbatim for citation.
      cadence_every_actions: 4
      min_messages: 30
      trigger_on_large_result_chars: 6000
      allow_summarize_latest_tool_result: false
      protect_last_n_results: 3
  - name: summary_post_node
    class: SummaryPostNode
    config:
      summary_agent: "web::web_summary"
      resume_agent: "web::planner"
  - name: tool_caller
    class: ToolCaller
  - name: tool_result_handler
    class: ToolResultHandler
  - name: return_control
    class: ReturnControlNode
  - name: manager_exit_node
    class: ManagerExitNode
  - name: graceful_exit_control_node
    class: GracefulExitControlNode

tools:
  allowed_tools:
    - ask_user
    - find_tool
    - install_tool
    - search_web
    - scrape_url
    - http_request
    - mcp::npm/server-google-maps::maps_distance_matrix
  except_tools: []

scope_contract:
  tools:
    allowed_tools:
      - ask_user
      - find_tool
      - install_tool
      - search_web
      - scrape_url
      - http_request
      - mcp::npm/server-google-maps::maps_distance_matrix
    blocked_tools:
      - mcp::npm/playwright-mcp::browser_run_code
    requires_approval_tools: []
    allow_external_side_effects: false

flow_config:
  strict_routing: true
  tool_return:
    tool_call_result_handler_node: "tool_result_handler"
  state_map:
    "web::delegator": "web::planner"
    "web::planner": "critic_pre_node"
    "web::critic": "critic_post_node"
    "tool_result_handler": "tool_return_router"
    # The summary path was declared but unreachable until 2026-08-23: the
    # router jumped straight to the planner, so this manager's long research
    # loops ran with no compression at all.
    "tool_return_router": "summary_pre_node"
    "summary_pre_node": "web::planner"
    "web::web_summary": "summary_post_node"
    "summary_post_node": "web::planner"
    "web::planner_return_control": "shared::final_answer_lite"
    "shared::final_answer_lite": "manager_exit_node"
    "graceful_exit": "graceful_exit_control_node"
    "graceful_exit_control_node": "shared::final_answer_lite"
